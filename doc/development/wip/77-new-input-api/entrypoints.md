# Feature #77 — Open entrypoints

*Maintained list of actionable next steps for this topic. On session boot, offer
these as a selectable menu (recommended-next first). Keep it current: close
entries when done, add new ones as they open.*

Legend: ▶ recommended next · ○ open · ◐ in progress · ⏳ blocked (hard — see depends-on) · ✓ done

Soft edges (in *Depends on*): `X (soft)` = **soft-block** — prefer-after, not required · `X (soft-gate)` = **proceed-at-risk gate** — skipping leaves silent debt / an approval gap, not a hard stop.

---

| # | Action | Status | Depends on | Where |
|---|---|---|---|---|
| E7 | **Close M2 — run M2-02.** The corrective take **M2-01 code was approved**, C-1 + C-2-runtime confirmed, and **M2-02** unit test successfully implemented and reviewed. The C-2 acceptance gap is closed, M2-01 signed off, and E7 closed. | ✓ | — | `implementation/prompts/M2-02-submit-path-test.md`, `design/spec/M2-02-submit-path-test.md` |
| E9 | **Architect call — commission `M4-0` + confirm the test-first split + resolve the M2-review open design questions** (E14 approved the path). Resolve: (1) the `M4-0` feature-global safety-net spec — its **test-infra feasibility** (can busted drive a project-level input flow via `mock.keystroke`/`EditorSession`?), coverage scope (examples + editor `is_at_limit` block-nav + D-9 coexistence), acceptance criteria; (2) confirm the per-milestone **test-first acceptance step preceding implementation** (M5/M6/M7); (3) M4 black-box vs. escalated (the net makes black-box **safer**); (4) M7 in parallel? **(5) the open design questions surfaced by the M2 human review** ([`M2-human-review.md`](implementation/reviews/M2-human-review.md)): **A1** project-event hooking, **A5** keep-or-refactor the `love.state.user_input` overlay-flag contract, **A6** combo-string serialize-vs-match (**decide before M5**), **A8** the M4-0/test-strategy design + whether the bottom sink should receive `keys_pressed` (**A2**, deferred to m4/m5). **(6) the E20 hand-off** ([`assessment.md`](notes/stakeholder-3-input/assessment.md)): the one open design Q = **combo-tier repeat semantics** (do `handlers[combo]`/`framework_handlers` fire on key-repeats or only fresh presses? — sits next to A6; human-provisional default: handler fires once, `on_key_pressed` fires on repeats); plus `isrepeat` threading (regression undo, M4-0 assert); P1 = M4-0 must not assume event order (no runtime guarantee owed); **P4** = the real leak is **T3 raw-`love.*` global state** (sandbox deep-clones the `love` table but shares leaf C functions, so imperative calls hit real SDL/LÖVE state) crossing run boundaries → **framework snapshot/restore on stop, not a project hook** (crash/force-exit-robust, sandbox-safe; also dissolves the P2 edge-tracking) → parked under A1 as a project-run-lifecycle concern outside #77's keyboard-widget scope. A `before_exit` hook **can** fire on `Ctrl+Esc` (it's framework-invoked code), but is needed only for project-*internal* save/memoize, not global-state restore; narrowed interrogation = confirm T3-global-restore (→ framework) vs internal-save (→ hook); adopt `keyboard`/`maze` as named M4-0/migration targets. Sizing here feeds the E16 recalc. **✓ Done (session 20)** — see [`entrypoints/E9-architect-call.md`](entrypoints/E9-architect-call.md): M4-0 **commissioned** (harness-extension + characterization; infra **feasible** — base is the **raw-handler pattern** per `keys_pressed_spec`, *not* `EditorSession`; add keypress-level driver + textinput/isrepeat emission); test-first **confirmed**; M4 **black-box** (guarded by M4-0); M7 **sequential**; **A6** = keep serialize + scratch-buf, **noop-index** (no `if`) + **return-propagate** (`or`-chain), `__matcher` seam kept; **combo-repeat** = fresh-only via **`handlers[isrepeat][combo]`**, `on_key_pressed` sees repeats; **A5** overlay-flag kept-documented; **A1/P4** two layers — project `before_exit()` hook (enabled-not-enforced, near-term, M6-family) + framework T3 snapshot/restore (guaranteed, postponed); **isrepeat** threaded in M4, M4-0 asserts. | ✓ | E20 ✓ · E11 (soft) | [`entrypoints/E9-architect-call.md`](entrypoints/E9-architect-call.md), `design/spec/M4-0-characterization-net.md`, `implementation/reviews/M2-human-review.md`, `notes/stakeholder-3-input/assessment.md` |
| E8 | **Continue the roadmap M4 → M8** — commission each milestone's prompt per the black-box model when reached. Adjacent **closure specs already commissioned**: `M6-01-oneshot-snapshot` (rides M6), `M7-01-retarget` (rides M7), `M8-01-dead-text-input` (rides M8). M5 must also weigh the **anticipated** debt (`combo_string` allocation, `gui_k` consumer) in `implementation/technical_debt.md`. M4/M6 may warrant **escalation** to a managed subtopic (integration-heavy). **Now recommended-next (E9 ✓ session 20, E16 ✓ session 21):** resume at **M4** — first commission the **`M4-0`** execution prompt (test-first / cheaper-model: harness extension + characterization net, the M4 guardrail), then the **M4** black-box implementation prompt (which threads `isrepeat`, flipping M4-0's one red assertion green). Sequential M5a→M5b(deferred)→M6→M7 after, each with its Tier-2 test-first step. **◐ In progress (session 22):** M4-0 + M4 implementor prompts **commissioned**, plus their reviewer prompts (filled `review-prompt.md` clones) and repo-root launchers `agents/dev.md`/`agents/review.md`. Forward `isrepeat` assert rides as `pending` (red-then-green→pending principle). **M4-0 APPROVED (session 23)** — outcome (717 green / 1 pending) + Opus review ingested; verdict: approve with non-blocking follow-ups (all carried to M4 test-first: consume `input_session.lua`, update `tests.md`). Two reframes from session-23 review-of-review: (1) **keyboard excluded from Tier-1 char net** — now forward Tier-2 acceptance showcase (two threads: isrepeat/M4+M5b + before_exit/M6-02); (2) **maze = textinput-channel anchor** — `is_empty` overlay coverage accepted, Lua-command path is M8 scope. Session-23 roadmap pivots: **M5 split** (M5a callbacks / M5b handlers sugar deferred → E23 + `spec/M5-01-split.md`); **`compy.before_exit` hook** slotted as M6-02 adjacent spec (`spec/M6-02-before-exit.md`); **API demo** authored (`notes/talk/api-demo.md`). **Next = run M4** (implementor prompt already commissioned). **Session-24 update (E22 ✓):** M4-0 reframed char-net → **M4 front-tests** (corrective `M4-0-01` spec+prompt written, **not yet executed**); M4 is now **test-first** (its prompt updated to "green the Group-2 `pending` tests"). Forward run order: **execute `M4-0-01`** (rewrite the routing spec, rename file) → **run M4** (greens Group-2). **Session-25 update:** M4-0-01 **implemented + LLM-reviewed (approve)**, but assessment found its tests are **implementation-coupled** (would be rewritten in lockstep with the M4/M5 routing rewrite → guard nothing). **M4-0-01 sign-off DEFERRED**; the M4 run now sits behind **E24** (formalize input contracts) → **E25** (commission `M4-0-02`, the contract-level test rewrite). **Session-31 pivot (E26 ✓ closed):** instead of the two-plane model (cold Sonnet per milestone, brainlab ingests after), human commissioned a **Fable-governed sweep** — `agents/sweep.md` (boot pointer) + `implementation/prompts/M4-M8-sweep-mandate.md` (the workflow: a self-contained project-space copy of the brainlab session OS, since the M0 image has no brainlab topology). Fable 5, inside the M0 image, runs **M4 → M5a → M6 → M7 → M8** (M5b deferred) as **serial milestone sessions** under `implementation/sessions/sessionNN/` (prompt/track/report, handover prompts written to itself, cold restart per milestone), spawning Sonnet subagents for grind. **Hard human-review stop at EVERY milestone boundary** (unconditional gate — approval before wrap), plus mid-milestone stops on any genuine judgment call (M4/M6 explicitly named cognition-heavy-escalation candidates in `implementation/README.md`, honored). Not-yet-commissioned M5a/M6/M7/M8 prompts are authored just-in-time from the frozen `design/spec/MN.md` slices, **each shown to the human for approval before execution**; `design/` may be *extended* (adjacent slices) only with prior human approval, never retroactively rewritten. Behavioural remarks stay raw in the tracks (no observation distillation — that happens at brainlab reimport). `session01/prompt.md` (M4) pre-authored. **Not yet launched.** **Session-32 update — SWEEP PAUSED:** the sweep launched and ran M4 (project `c6b5b30`); the human's M4 pushback (`c537909`) exposed a day-one derivation mutation in the design corpus (chain-vs-replace-sink semantics). Root cause + rulings + the revalidate-not-redesign decision are **E29** — the sweep does not resume until E29's re-derived spec/plan + amended sweep-mandate land (E29 Stage 3 gate). Do **not** commission M5a against the current spec. | ⏳ | E9 ✓ · E16 ✓ · E11 (soft) · E12 (soft-gate ✓) · E22 (soft) · E24 ✓ · E25 ✓ · E26 ✓ · **E29 (blocks — sweep paused)** | `implementation/prompts/M4-M8-sweep-mandate.md`, `agents/sweep.md`, `implementation/prompts/M4.md`, `agents/dev.md`, `agents/review.md`, `design/spec/M4…M8.md`, `design/roadmap.md` |
| E22 | **M4-0 tests rework decision** — session-23 review-of-review surfaced that the keyboard rows in `characterization_spec.lua` (`:195-231`) are wrong-tier artifacts: they reimplement debounce test-locally and characterize routing only (a D-9 duplicate). Now that keyboard is definitively a Tier-2 forward acceptance showcase (not Tier-1 char), these rows should be reworked. Human wants to review the tests and may provide nitpicks. Three outstanding items: (1) keyboard debounce rows — strip to routing-only, remove entirely, or replace with a `pending` scaffold for forward Tier-2 suite; (2) M4's test-first step must **consume `input_session.lua`** (kill the inline copy in the net) — reviewer's explicit recommendation; (3) `tests.md` not updated — needs `mock.textinput` emitter + `keystroke` opts documented. **✓ Resolved (session 24) — went much deeper than nitpicks.** Human's full review re-grounded M4-0: under withdrawn product-BC, characterizing per-example behaviour (which M8 migrates) is the wrong target; the real untested surface is the integrated `love.handlers` routing/slot M4 rewrites. **Decision: rewrite M4-0 → M4 front-tests** (corrective slice [`design/spec/M4-0-01-front-tests.md`](design/spec/M4-0-01-front-tests.md) + [`implementation/prompts/M4-0-01-front-tests.md`](implementation/prompts/M4-0-01-front-tests.md)): Group-1 regression smoke (green) + Group-2 new-behaviour `pending` → M4 greens. Char-net spec/prompt marked **superseded**. Knock-ons landed: **C3** convention (no milestone ids in tests — `process.md §9.3`/`sdlc.md`), **M4 now test-first** (M4 prompt + roadmap + §9.2 updated), **M7 editor-cursor open item** recorded (placement undecided, `technical_debt.md`). Driver-unused + keyboard-reimpl debt rows fold into M4-0-01. (No re-estimate — human call.) | ✓ | E8 (soft) | `design/spec/M4-0-01-front-tests.md`, `implementation/prompts/M4-0-01-front-tests.md` |
| E24 | **Formalize the framework's input contracts** (precondition for M4-0-01 sign-off + M4/M5). Session-25 reframe: the landed M4-0-01 front-tests are pinned to *implementation* (flag-poking + singleton construction + concrete-instance spies), so they'd be rewritten in lockstep with the M4/M5 routing rewrite → guard nothing. Goal = pin `⟨activation⟩→⟨event⟩→⟨consumption⟩` **outcome** contracts invariant under the rewrite. Premise: arch is ad-hoc, **current impl = canonical**; *can't guarantee the new API won't break the framework without contracts written down.* Governing cut: **OUTCOME (contract) vs MECHANISM (replaceable)**; two orthogonal families: **mode** activation (`app_state`→base sink) × **widget** activation/reset (overlay override). Run as **two cold Opus prompts** (kept cold to avoid orchestrator-context distortion): **P1** verified `file:line` inventory → `notes/input-contracts-inventory.md`; **P2** distil contracts (fresh note) → `notes/input-contracts.md`. **◐ In progress (session 25):** both prompts commissioned; **P1 launched**. Next: ingest P1 → run/ingest P2 → (optional cold revalidation). | ◐ | E8 | `implementation/prompts/input-contracts-01-inventory.md`, `implementation/prompts/input-contracts-02-formalize.md`, `notes/input-contracts.md` (forthcoming) |
| E25 | **Decide M4-0-01 fate + commission `M4-0-02` (contract-level test rewrite).** After E24's contracts note lands: M4-0-01 is implemented + LLM-reviewed (approve) but its tests are implementation-coupled; sign-off was **deferred** (session 25). **✓ Done (session 26) — but partial, and that's the finding.** The contract record (E24) was validated → cold-revalidated (P3) → all human rulings landed (R1 wheel pass-through/noop; R2 inspect = console-owns-input, **provisional not pinned**; R6 force reconfig durable-but-scope-forward). `M4-0-02` ran cold (Sonnet, `30cd9ca`, test-only): **714/0/0/5 pending**, 6 perturb→red→restore, added the genuine gap (**textinput + keyreleased EXCLUSIVE**), inspect provisional `pending`, mousepressed BOTH guard — supersedes M4-0-01 for the kbd/text matrix. **HOWEVER:** scoped as an additive gap-fill on the existing `input_routing_spec.lua`, it left the suite still implementation-shaped. The contract record was never *projected onto* the suite; `singleton_spec`/`overlay_spec` (M2-era) still assert the **mechanism** (`love.state.user_input.C`, identity) not the contract. → **E26 opened** for the cold restructure. `internals/` promotion + §5 m6/m7 enumeration still deferred to feature close (R7). | ✓ | E24 | `implementation/outcomes/M4-0-02-contract-routing-suite.md`, `notes/input-contracts.md`, `notes/input-contracts-revalidation.md`, `tests/input/input_routing_spec.lua` |
| E27 | **Normalize the canonical current-behaviour doc + split current-reality from designed-API.** Fold the 2026-07-01 assessment + session-30 gaps into a normalized **doc (A) = how the input subsystem works today**; **doc (B) = the designed API** stays the `design/` corpus; classify out-of-#77-blast-radius findings (four `reset()` impls, `inspect` suppression, `search` sub-widget, `keyreleased` console-only fork, cursor two-layer split) as foundation for the future console/editor adoption — captured, not dropped. **✓ Done (commit `51d173e`, ingested session 31)** — doc A normalized to the §1–§9 shape (§2 keypressed-vs-textinput, §4 completeness table, §8 out-of-radius classification, §9 open questions) + the suite↔doc bridge **`notes/input-suite-validation-map.md`** (one row per contract: present/add/pending/mechanism-guard/out-of-scope). Human verdict: tolerable, not yet comprehensive — comprehensiveness debt rides R7 (promotion to `internals/` at feature close). | ✓ | E24 ✓ · E25 ✓ | `notes/input-contracts.md`, `notes/input-suite-validation-map.md`, `notes/plan.md` |
| E28 | **Reconcile `implementation/prompts/M4.md` before M4 is commissioned.** The drafted M4 prompt still named the renamed `input_routing_spec.lua` + the superseded Group-1/Group-2 vocabulary — "will mislead the M4 cold-implementer" (Opus M4-0-04 review). **✓ Done (session 31, Fable pass)** — prompt repointed to `input_contracts_spec.lua` + the Bucket A–D model, doc A added to Read-first, "adapt pending bodies to the landed API shape" guidance added; both **acceptance lines folded in**: step 3a *no-op-when-hidden on the keypressed path* (extend/sibling the §3(C) row), step 3b *M4↔M5 `keyreleased` tier* — recorded default **descope-with-note**, ⚠ **human ruling still required at M4 launch** (do-not-invent-the-tier guard in place). | ✓ | E27 ✓ | `implementation/prompts/M4.md`, `design/spec/M4.md`, `design/spec/M5.md` |
| E26 | **Cold restructure: make the input test suite outlive the implementation.** **Session-26 finding** (human verdict on M4-0-02: *"came to where we started from — more tests of implementation, not contract"*). The contract record (`notes/input-contracts.md`) is sound, but the **suite was never authored from it** — it grew incrementally, so it still tests this sprint's *mechanism*: `singleton_spec` asserts `love.state.user_input`/`.C`/`.V` + identity and pokes `show/hide/get_text` (its own line-34 admits "internals"); `overlay_spec` is mixed; `routing_spec` is ~80% contract-level but carries ~175 ln boilerplate + a `record_calls(obj,name)` method-name spy. **Hypothesis (human):** tests must be useful outside the current sprint context — a restructure/rewrite authored *from* the contract record, separating **behavioural contracts** (rewrite to public surface + observable effect) from genuine **NFR/mechanism guards** (singleton identity / no-realloc — keep, label honestly). **Feasibility gate to resolve first (open A8):** does a public `compy.input` / real-flow surface exist to drive these through *today*, or is it gated on M4/M7? **Human wants this re-examined COLD** — `session27/prompt.md` presents the problem + evidence + gate; the session-26 diagnosis is a *read to re-check*, not a plan to execute. **◐ In progress (sessions 27–28):** the cold re-examination escalated (session 27) into a stakeholder-intent audit that localized the drift to the contract record `notes/input-contracts.md` §3/§4 (mechanism-framed) and produced a **remediation chain** — the restructure is being delivered as *correct-the-record-then-regenerate-from-it*, not restructure-in-place. **Session 28:** prompt12 correction + prompt13 review run out of band + **approved** (F1/F2 record hygiene landed, F3 cleared benign); the suite-regeneration prompt **`M4-0-04` rewritten** from a 5-row patch → full re-derivation from the corrected record (two-filter spine: subtract change-intents · state kept behaviour at intent altitude), **launch-ready, not launched.** Next (session 29): pre-launch **revalidation** of the F1/F2 edits + the M4-0-04 prompt (overcorrection watch) → launch → ingest → run M4. **Session 29:** revalidation **postponed** by the human (implementation already running) — side-tracked into agent-entrypoint hardening instead (`agents/development.md`/`review.md` empty-test gate, new `agents/inspect.md`). **Session 30 update (reimported from a side-channel `codeinspect` conversation, see `sessions/.../session30/`):** confirmed M4-0-04 **launched and landed out of band** (commit `0e66cb9`, no pre-launch revalidation ran) and **independently reviewed** (Opus, `implementation/reviews/M4-0-04.md`: corrective-take, 2 small unfixed findings — editor route missing keypressed-EXCLUSIVE coverage; `F.reset()` over the 14-line limit). Suite renamed `input_routing_spec.lua`→`input_contracts_spec.lua`. Human's own manual pass added ~20 inline `-- REVIEW:` comments on top; session 30's own read was a **fourth** pass, adding one new gap (§3.3 keyreleased characterization misses the console/editor internal fork) + a bucket-mislabeling + two recommendations (explicit no-op-when-hidden M4 acceptance line; legibility/mode-subgrouping pass on the spec file, reclassified first-order not cosmetic). **Session-31 re-plan ([`notes/plan.md`](notes/plan.md)):** broadened from "consolidate four passes into a punch list" to the **convergence pass** governed by an explicit **definition-of-done** (complete-by-construction/visible · every assertion carries provenance · architect-legible cold · every gap dispositioned) — the reframe: the "safety net" is **black-box characterization / M4-integration-risk retirement**, not test ceremony ([`notes/talk/safety-net-as-characterization-replan.md`](notes/talk/safety-net-as-characterization-replan.md)). After **E27** normalizes doc (A): **validate the suite against (A)**, then **clean it to DoD in one pass** — resolve the ~65 `-- REVIEW:` remarks (a couple pin real misunderstandings, most cosmetic), apply the two Opus findings (editor keypressed sibling + retitle; extract `restore_native_slots()` → `F.reset()` ≤14 ln), per-mode `describe` nesting, strip session IDs (`D-α`/`R1`–`R7`/`F-A`–`F-E`) → plain rationale + doc-links, one keypressed-vs-textinput explainer, relabel held-key-set block as mechanism, tighten pointer-selection causality, explicit `pending` for the `search` gap. DoD satisfied ⇒ gate closed ⇒ **run M4** (via E28-reconciled prompt). **Session-31 Fable pass — cleanup EXECUTED, verification pending:** the suite was rewritten to the DoD directly in the orchestration plane (E27's validation map made it mechanical): all 65 `-- REVIEW:` remarks resolved+removed (each answered in place or absorbed into doc A refs), both Opus findings applied (new editor keypressed-EXCLUSIVE test + retitle; `restore_native_slots()` extracted, `F.reset()` ≤14 ln), per-mode `describe` nesting (console/editor/search/project — the full mode×channel grid, holes as named `pending`s: console keyreleased, editor pointer, search routing), session IDs stripped → "doc A §N" provenance refs, keypressed-vs-textinput explainer added, held-key trio relocated under mechanism/NFR, pointer causality precondition added, legacy submit made event-driven, stop test strengthened (project handler receives nothing after stop). **✓ Closed (session 31, verify ingested):** `M4-0-05` ran (Sonnet, lua env) — **718/0/0/12, exact
match to expected, no mend needed** (`dfd95c7`). The three open dispositions ((1) editor block-nav
row, (2) inspect row live-vs-skip, (3) `on_text_entered` per-char/per-block) are **not launch
blockers** — (1)/(2) stand as already-kept/OPEN, (3) is explicitly parked at the m5a pending; none
required a ruling before M4. The one item that *was* gated to launch — the `keyreleased`-tier
decision (E28 3b) — **ruled by the human: ship the pre-authorized default, descope-with-note.** E26
closes. | ✓ | E24 ✓ · E25 ✓ · E27 ✓ | `notes/plan.md`, `notes/input-contracts.md`, `notes/input-suite-validation-map.md`, `tests/input/input_contracts_spec.lua`, `implementation/outcomes/M4-0-05-suite-cleanup-verify.md` |
| E29 | **Revalidate the core design + re-derive spec/plan** (session 32 — the M4 pushback resolution). The sweep ran M4 (project `c6b5b30`); the human pushed back hard (`c537909`, ~48 `>> REVIEW` remarks). Root cause diagnosed (**not** implementor hallucination): a **derivation mutation dated to day one** — commit `207bb01` holds the architect's core solution (`notes/routing_unification.md`/`solution_sketch.md` §4: four-level chain, sink *always terminal*, `on_key_pressed` default = noop+log, truthy = stop-propagation) **and**, same commit, `design.md`/`spec.md` asserting the opposite (default *is* the sink, override *replaces* it). No pass ever diffed the two; the sweep built M4 to the mutated letter. Full classification + root cause + 5-step path: [`reviews/m4-architect-pushback.md`](reviews/m4-architect-pushback.md); provenance + platform check + the architect's five rulings: [`notes/talk/m4-pushback-rulings-and-provenance.md`](notes/talk/m4-pushback-rulings-and-provenance.md). **Architect ruled: the chain is ratified** (+ predictability meta-rule; PIC disconnected outside `'running'`; keyreleased unified; binding glossary). **Direction: revalidate the core (never falsified) + re-derive spec/plan — NOT re-design** (re-design would re-run the failure mode; kept only as the Stage-1 tripwire). **The sweep is PAUSED** — do not commission M5a against the current spec (§3/§4 superseded). **○ Open (session33 commissioned):** 3 gated stages — (1) claim-by-claim revalidation of `routing_unification.md` vs platform (LÖVE+compy code, incl. empirical checks: non-blocking-run handlers, the D-9 pong-paddle leak) + a one-page ratifiable model; (2) bounded re-derivation of `design.md`/`spec.md` (claims-diff vs upstream, deviations table, binding glossary, old docs = salvage not authority); (3) roadmap re-cut (chain as one slice + M7 + M8) + sweep-mandate guardrails (no in-slice rulings · citation-or-stop ledgers · surprise-first reporting). Hard human gate at each stage. **◐ Session33 executed all three stages:** Gate 1 **RATIFIED** (2026-07-05) — `design/notes/ratified-model.md` = **canonical authority** (R1–R14 precision remarks + five rulings folded; no core claim falsified, tripwire did not fire); Gate 2 **APPROVED** (2026-07-06) — `design.md`/`spec.md` re-derived from bounded inputs (`*.versions/version02`; outgoing docs demote-frozen), approved at review-surface level, **closing scoping ruling:** no ideal-architecture pursuit in one pass — console/editor migration = the next simplification venue + stakeholder talks on dev-facing facets; Stage 3 landed the re-cut — **M5c → M7 → M8** (`design/spec/M5c-dispatch-chain.md` supersedes M5/M5-01-split/M6 as implementation targets; keyreleased unified per ruling 4; turtle/maze migration moved M8→M5c; ~18 QUALITY remarks fold in-slice with per-remark dispositions; M6-01/M6-02 ride M5c), amended mandate `implementation/prompts/M5c-M8-sweep-mandate.md` (**6 guardrails**: no in-slice rulings = gate failure · citation-or-stop · surprise-first · REVIEW-as-design-input · no unconsumed surface · pinned-remark dispositions), E11 recalc **≈ 77 h / ≈ 45 h** (`estimates.versions/version04`) — **Gate 3 PRESENTED, awaiting approval → session34 closes it, then resumes the sweep.** | ◐ | E26 ✓ · E8 (blocks) | `sessions/.../session33/prompt.md`, `reviews/m4-architect-pushback.md`, `notes/talk/m4-pushback-rulings-and-provenance.md`, `design/notes/routing_unification.md`, `design/notes/solution_sketch.md` |
| E23 | **Commission M5a — callbacks** (post-M4). M5 is split (session-23 decision, `design/spec/M5-01-split.md`): **M5a** = `on_key_pressed` + `on_text_entered` exposed + `framework_handlers` slot established (empty; M6 fills it). The Tier-2 test-first step for M5a is the natural home for the **keyboard forward-acceptance isrepeat thread** (once-per-press suppression via `on_key_pressed`). Author M5a test-first prompt + implementor prompt (+ reviewer). The keyboard Tier-2 `pending` scaffold for `before_exit` can ride the M5a or M6 acceptance step — decide at commission time. **M5b** (handlers sugar: `handlers[combo]` + normalisation + dispatch, deferred) does not block M5a or M6/M7/M8. **Session-32: now blocked behind E29** — the dispatch-chain semantics M5a implements are being re-derived (the ratified chain supersedes `spec.md` §3/§4). Do not commission until E29's re-cut plan lands; M5a likely folds into a single "dispatch chain" slice. **Session33: confirmed — M5a/M5b dissolved into `M5c`** (`design/spec/M5c-dispatch-chain.md`); this entrypoint is consumed by the M5c commissioning, which is the first sweep session after Gate 3 closes (session34 task). | ⏳ | E8 · E29 (blocks) · E22 (soft) | `design/spec/M5-01-split.md`, `design/spec/M5.md` (frozen ref) |
| E5 | **Apply the E4 revalidation cleanups** (session 04 findings; optional, **non-blocking** — pure consistency tidy on a frozen design). F1: sweep stale bare `decisions.md` refs in live chain docs → `notes/decisions-record.md`/`status.md`. F2: fix `process.md` §1/§4 `decisions.md`→`status.md`. F3: narrow-or-sweep the SR/VR over-claim. Plus: add `Derived from:` to the 5 pipeline docs **or** document the intentional non-wiring in `sdlc.md`. | ○ | — | `sessions/.../session04/report.md` |
| E6 | **Build the agentic dev image (M0)** — `just`/`lua5.1`/`luarocks`/`busted`+`luautf8`+`luafilesystem`/`love2d` **+ Claude Code** add-on; prereq for running the suite. | ✓ | — | `implementation/outcomes/M0.md` |
| E3 | **Implement M1 (`keys_pressed` table)** + the **M2a** follow-up hygiene — both **landed and reviewed** (M1 ship-it: [`reviews/M1-01.md`](implementation/reviews/M1-01.md)). | ✓ | — | `implementation/outcomes/M1.md`, `reviews/M1-01.md` |
| E4 | **Establish the design lifecycle's process/rules** — `design/` enrolled as a canonical-SDLC instance; per-milestone spec slices; canonical shape. **Revalidated (session 04).** | ✓ | — | `design/agents/`, `entrypoints/E4-restructure.md` |
| E1 | **Revalidate the design's convergence.** | ✓ | — | `sessions/.../session02/report.md` |
| E2 | ~~Resolve the genuinely-open decisions.~~ **Dissolved by E1** (D-1…D-7 stakeholder-settled; D-8/9/10 architect discretion). A *contingency* only: re-fit round **if** stakeholders object after seeing implementation. | ✓ | — | `design/decisions.md` |
| E10 | **Operational — extract estimates into a first-class doc.** Done (session 11). Per-milestone PERT moved to `design/estimates.md` (+ `estimates.versions/version01` genesis baseline); `roadmap.md §Estimates` trimmed to the **frozen design-phase total** (historical artifact) + an **append-only total-estimated log** + pointer. Rule `process.md §7` + binding `sdlc.md` amended to the frozen-total/log shape (human add-on: total frozen, recalcs logged not overwritten). | ✓ | — | `design/estimates.md`, `design/roadmap.md`, `design/agents/process.md §7`, `design/agents/sdlc.md` |
| E11 | **Operational — estimate maintenance (recurring, beyond milestones).** Now live (E10 landed). Whenever the milestone set changes (a milestone added or **pivoted** — e.g. `M3-01`) or on periodic review: estimate the new/changed milestone → recalc the total in `estimates.md` → write an `estimates.versions/` baseline (demote-by-freezing the prior latest) → **append one line to the roadmap's total-estimated log** (never touch the frozen design-phase total). Standing action, not a one-off. **First recurring fire: E16 (session 21)** — sized `M4-0`, recomputed the total (≈74 h / ≈44 h), wrote `estimates.versions/version02` (demote-froze version01), appended the roadmap log line. Stays open (recurring). | ○ | E15 (soft) | `design/estimates.md`, `design/estimates.versions/`, `design/roadmap.md §Estimates`, `design/agents/process.md §7` |
| E13 | **Brief me on the stakeholder input & how it landed.** LLM walks the human through the recent stakeholder feedback (SR1 = `notes/input.md` 2026-06-06; SR2 = `notes/input/stakeholder2_notes.md` 2026-06-10) and **how exactly each point landed across the specs + decision ledger** — explaining/reminding the **meaning** of each change, *not* juggling ref-ids. Orientation that informs E12 and E14. **Done (session 12).** | ✓ | — | `design/notes/input.md`, `design/notes/input/`, `design/status.md` (decision ledger), `design/spec/` |
| E12 | **Code-review M2, then LLM-reevaluate it.** Done (session 17). *Human* manually code-reviewed M2 (three inline-remark commits on `topics/git`); the LLM reevaluated *incorporating that review* → triage doc [`M2-human-review.md`](implementation/reviews/M2-human-review.md): ~70 remarks classified (`open` A1–A8 / `policy` C1–C2 / `verify` / straight-`fix`), all **6 `verify` checks ran ⇒ M2 code sign-off-clean**, durable fixes landed + every open/ephemeral remark carried as an in-code **`DEFERRED (0.1.0-mN)`** marker. C1 (semver-not-milestones) + C2 (warn-don't-swallow) settled; suite **701** green. Human sign-off loop on M2 **closed**. | ✓ | — | `implementation/reviews/M2-human-review.md` |
| E14 | **Understand & approve the M3→M4 path.** Done (session 12). The human understood and **approved** the path, reframed as the **two-tier test strategy**: a feature-global characterization net (`M4-0`, covering M4+M6, own spec-design cycle) + per-milestone **test-first** acceptance steps preceding implementation. Corrected the revival note's "tests can't precede code" mis-framing (TDD against the spec). Unblocks E9; propagation → E16. | ✓ | — | `notes/talk/two-tier-test-strategy.md`, `notes/talk/m3-revival-tdd-for-m4.md` |
| E15 | **Sanity-check the estimates state & format.** Review the freshly-extracted estimates (E10 output): `estimates.md` + `estimates.versions/version01` + the roadmap **frozen-total / append-only-log** shape — confirm the format is what you want and there are no surprises in the numbers before E11 starts appending to it. *(Operational; off the substantive thread — the human's chosen forward path is E20 → E9.)* | ○ | — | `design/estimates.md`, `design/estimates.versions/`, `design/roadmap.md §Estimates` |
| E20 | **Assess pre-feature input-behavior *pain statements* + two new examples against the design direction.** Done (session 18). **Verdict: design direction holds — validated, not altered; no milestone added/pivoted ⇒ no E11/E16 estimate impact.** Under a human-led *symptoms-not-requirements* reframe, the 4 pains (`compy-input-quirks.md`) collapsed to **one open design Q** + housekeeping for E9: P1 (event-order) = no obligation, only M4-0 test hygiene; P2 (`isrepeat`) = incidental strip (`controller.lua:554`) to thread back + combo-tier repeat semantics (the open Q); P3 (chords) = solved by combo dispatch; P4 (exit hook) = solved-by-dissolution (singleton owns input state), general hook → A1. Examples = M4-0 (`keyboard`) + D-9/M8 migration (`maze`) anchors. **Revalidated clean (session 19)** — see E21 register for the live SR3 carry-list. | ✓ | — | [`notes/stakeholder-3-input/assessment.md`](notes/stakeholder-3-input/assessment.md) |
| E21 | **Operational — maintain the late-input register.** Living index of stakeholder inputs received after the initial session (SR1) + where each is absorbed, so late inputs aren't lost during M4–M8 spec/test refinement and arch calls. **SR2** (round 2) = **fully reincorporated** into design/specs (`ProjectInputController` rename, `show({force=true})`, D-5 horizontal/line `is_at_limit`, `keys_pressed` read-proxy — ✓ landed). **SR3** (round 3) = **resolved at E9** (session 20) — all 6 items decided/parked/landed (combo-repeat + isrepeat + P1 + A6 + T3-park + `keyboard`/`maze` anchors); see the SR3 table + `entrypoints/E9-architect-call.md`. Carried items now flip from at-risk to landed. Estimates-class operational: update when new input arrives or a carried item lands. Not a re-fit (that stays the E2 contingency). | ○ | — | `notes/late-input-register.md` |
| E16 | **Operational — propagate the two-tier test-strategy decision** (settled session 12; see `notes/talk/two-tier-test-strategy.md`). Three parts, to land **atomically after E9** so there's no half-applied state: (1) **roadmap** — retire the M3 tombstone, add **`M4-0`** (feature-global net) + the per-milestone **test-first acceptance step** for M5/M6/M7, repoint cross-refs; (2) **codify the conventions** into `design/agents/process.md` / `sdlc.md` — the `-0` precondition-slice rule and the test-first split; (3) **E11 recalc** — size M4-0 (needs E9's infra answer) + the test steps, recompute the total, write an `estimates.versions/` baseline, append the roadmap log line. Recalc sub-step **depends on E9** (M4-0 sizing hinges on the infra-feasibility outcome). **E9 ✓ (session 20) — now recommended-next:** M4-0 is sized as *harness-extension (3 capabilities) + characterization suite*; codify the `-0` precondition rule + test-first split + the A6/repeat dispatch decisions (ride an adjacent M5 slice — frozen `M5.md` not edited). **✓ Done (session 21)** — landed atomically across 6 files: roadmap M3-slot→**`M4-0`** section + summary row + M4 black-box/`isrepeat` guardrail note + M5/M6/M7 Tier-2 test-first lines + M8 xref repoint; **`process.md §9`** (slice taxonomy `MN`/`MN-0`/`MN-NN` + two-tier/test-first/cheaper-model + C1/C2/DEFERRED) + **`sdlc.md`** config bullets; **E11 recalc** sized `M4-0` (no-LLM **≈74 h** / LLM **≈44 h**, +≈8/+≈5 vs frozen 66/39; M5/M6/M7 test-first = re-sequenced, no volume delta) → `estimates.versions/version02` (version01 frozen as full snapshot), roadmap log appended. Frozen design-phase total untouched. | ✓ | E9 ✓ · E11 (soft) | `design/roadmap.md`, `design/agents/process.md`, `design/agents/sdlc.md`, `design/estimates.md`, `notes/talk/two-tier-test-strategy.md` |
| E17 | **Operational — MCP-LSP tooling for the agent fleet** *(conceptual assessment; per-agent value stands)*. Per-agent payoff: **reviewer** (Opus, find-references/impact = highest) > **developer** (Sonnet) > read-only **Q&A** (Sonnet/Haiku); **not** the orchestrator. **Architecture pivoted (session 15):** the socat multi-container blueprint is **superseded** by a single human-driven `codeinspect` container (local stdio) — **realized as E18**. **Off the #77 critical path**, operational class like E10/E11. | ○ | — | `notes/talk/mcp-lsp-tooling.md`, `notes/talk/codeinspect-harness.md` |
| E19 | **MCP-LSP adherence — reframe landed; observe during use.** Confirmed (session 16): getting the LSP used is an **adherence**, not capability, problem — MCP was healthy/usable, agent judged it not worth it; the skipped doc-first rule already exists in two rule files. **Reframe authored (session 16):** `orientation.md` MCP/doc nudges moved **token-churn → correctness** (exploratory-grep / known-symbol-LSP / impact-find-refs+grep-backstop / architectural-doc triggers; "you CAN use it when unsure"; docs = right-first-source for architectural/intent Qs). Human's call: **stop tuning, start using.** Remaining = **observe during real use**; decision-time hook (`PreToolUse` on bare-identifier grep) is **contingency only**, build iff it still reverts. Multi-agent shape collapsed to one re-roled agent. | ◐ | E18 (soft) | `notes/talk/mcp-lsp-adherence.md`, `implementation/docker/src/agent/orientation.md` |
| E18 | **Build & verify the `codeinspect` harness** — single-container, human-driven dev/inspection stack: three interchangeable CLIs (`claude` · `cursor-agent` · `agy`), Lua MCP↔LSP (`isaacphi/mcp-language-server` → `lua-language-server`) **over stdio**. Justified as the **human's own comprehension instrument** + CI-matched toolchain, **not** a #77 blocker (E17-class, off critical path). Scaffold drafted + committed under `implementation/docker/`; build fixes landed (Go 1.24, HOST_UID/GID, tmpfs, `/impl` shortcut). **Pending (human, after host disk cleanup):** finish build + smoke-test — LuaLS asset/arch, `--logpath/--metapath` passthrough, watcher→diagnostics. | ◐ | — | `implementation/docker/`, `notes/talk/codeinspect-harness.md` |

## Dependency edges — updated (session 23)

The forward feature path sits behind the **architect call (E9)**, now the recommended-next. The
design-input reassessment (E20) that softly preceded it is **done** — verdict: direction unchanged, so
E9's scope is unshifted (its inputs are merely sharpened). The standing estimate chain remains soft.

- **E20 →(soft) E9 — SATISFIED + REVALIDATED:** reassessment complete; no pain alters the design
  direction. E20's output is now an *input* to E9 (the combo-tier repeat-semantics question +
  `isrepeat`/test-hygiene/A1 items), not a blocker. **Revalidated clean (session 19)** — citations
  verified, hand-off maps without invented scope; two glitch-patches applied (this row's P4 framing +
  assessment `:824`→`:807`). Validation-economics ruling: no requirements re-fit now; `keyboard`/`maze`
  fold in as M4-0 characterization anchors, empirical recheck built into M4-0 (E2 contingency is the
  escape valve). E9 inherits a *sharpened, not reopened* design.
- **E11 →(soft) E9 · E8 · E16** — these reshape **scope/roadmap**, so the estimate-maintenance machinery
  should be *armed* first. (The M4-0-specific recalc still happens **inside E16**, after E9 supplies the
  infra/sizing answer — "E11 soft" = *machinery ready*, not *all numbers final*.)
- **E15 →(soft) E11** — confirm the estimates format before the recurring recalc starts appending.
- **E12 soft-gate on E8 — SATISFIED:** M2 is sign-off-clean (E12 ✓), so building M4→M8 no longer sits
  atop an un-signed-off M2.
- **E9 ✓ (session 20) — architect call held:** M4-0 commissioned + spec written, test-first confirmed,
  M4 black-box, M7 sequential, A6/repeat dispatch decided, A5 kept, A1/T3 parked. Record:
  [`entrypoints/E9-architect-call.md`](entrypoints/E9-architect-call.md). Unblocks **E16**.
- **E16 ✓ (session 21) — propagation landed atomically:** roadmap M3-slot→M4-0 + M5/M6/M7 test-first
  steps, conventions codified (`process.md §9` + `sdlc.md`), E11 recalc (≈74/≈44, `version02`). Unblocks
  **E8**.
- **E8 ◐ (session 22) — forward slice commissioned:** M4-0 + M4 implementor prompts + their reviewer
  prompts (filled `review-prompt.md` clones) + repo-root launchers `agents/dev.md`/`agents/review.md`.
  Forward `isrepeat` assert = `pending` (red-then-green→pending). Next is execution-plane (human-driven
  implement→outcome→review), ingested back here.
- **E8 ◐ (session 23) — M4-0 APPROVED + roadmap pivots:** M4-0 outcome + review ingested; verdict
  APPROVE with non-blocking follow-ups (input_session.lua consumer + tests.md → M4 test-first step).
  Two reframes: keyboard excluded from Tier-1 char net (now Tier-2 forward acceptance — isrepeat + before_exit
  threads); maze = textinput-channel anchor (is_empty overlay coverage accepted; Lua-command path M8 scope).
  Session-23 roadmap pivots: M5 split (M5a/M5b — `spec/M5-01-split.md`); before_exit hook slotted
  (`spec/M6-02-before-exit.md`); E11 recalc fired (≈76/≈45, `version03`); API demo authored
  (`notes/talk/api-demo.md`); E22 (M4-0 tests rework) + E23 (M5a commissioning) opened.
  **Next: run M4** (implementor prompt committed session 22; prefer E22 resolved first — soft).
- **E8 ◐ (session 24) — M4-0 reframed char-net → M4 front-tests (E22 ✓):** human review re-grounded
  M4-0 under withdrawn product-BC; corrective slice `spec/M4-0-01-front-tests.md` (+ prompt) written,
  char-net superseded. M4 now **test-first** (Group-1 smoke green + Group-2 `pending` → M4 greens; M4
  prompt/roadmap/§9.2 updated). **C3** convention codified (no milestone ids in tests). M7 editor-cursor
  open item recorded (placement undecided). No re-estimate (human call). Forward run order: **execute
  `M4-0-01` (Opus) → ingest → run M4.**
- **E8 ◐ (session 25) — M4-0-01 executed + reviewed, but a deeper reframe parked the M4 run:** the landed
  front-tests are **implementation-coupled** (flag-poking + singleton construction + concrete-instance
  spies) → they'd be rewritten in lockstep with the M4/M5 routing rewrite, guarding nothing. New primary
  goal **E24** = formalize the framework's input contracts (`⟨activation⟩→⟨event⟩→⟨consumption⟩` outcomes,
  outcome-vs-mechanism, mode×widget families) via **two cold Opus prompts** (P1 inventory launched, P2
  formalize commissioned). Then **E25** = commission `M4-0-02` (contract-level test rewrite). M4-0-01
  sign-off deferred until then.
- **E8 ◐ (session 26) — contract record landed; M4-0-02 ran but exposed the real debt:** E24 done
  (contract note validated → cold-revalidated → R1/R2/R6 rulings landed), E25 done (M4-0-02 green-with-
  teeth, supersedes M4-0-01 for the kbd/text matrix). **But** the suite is **still implementation-shaped**
  — the contract record was never *authored into* the tests; M4-0-02 was an additive gap-fill on an
  impl-coupled file. **E26 opened** to restructure the suite so it outlives the implementation; human wants
  it re-examined **cold**. `internals/` promotion deferred (R7).
- **Net effect:** **E24/E25/E26 ✓** — the contract-shaped suite landed, converged to DoD, and
  verified green (`M4-0-05`, session 31: 718/0/0/12). **Execution model pivoted (session 31):** rather
  than brainlab commissioning each of M4→M8 individually, a **Fable-governed sweep** (E8's row) runs
  them as serial milestone sessions inside the M0 image — brainlab-style handover prompts under
  `implementation/sessions/`, **hard human gate at every milestone boundary** plus judgment-call
  stops, just-in-time prompt authoring with human approval before execution. M5b stays deferred. E15/E11 operational, off to the side. **Carried verification items** (contract
  note §6.1, ride into the sweep): R6/M7 force-vs-`configure()`; R2/M4 decide-inspect-when-routing-
  model-lands. (Side thread: possible real bug — balloons end-of-game overlay redraw,
  `internals/user_input.md:227` — chased separately.)
- **E26 ✓ (session 31) — verify ingested, gate closed:** `M4-0-05` ran clean (718/0/0/12, exact match,
  no mend needed, `dfd95c7`); the three open suite dispositions stand as non-blocking (kept-OPEN /
  parked-at-m5a); the one launch-gated ruling (`keyreleased` tier, E28 3b) resolved to its
  pre-authorized default. Immediately followed by the sweep-mandate pivot recorded in E8's row.
- **E26 ◐ (session 28) — remediation executed to launch-ready; regeneration not yet run:** prompt12+13
  (correction + cold review) run out of band + **approved** (verdict APPROVE, F1/F2 record-hygiene fixes
  landed on `notes/input-contracts.md`, F3 cleared benign). The suite-regeneration prompt **`M4-0-04`
  rewritten** (5-row patch → full re-derivation from the corrected record; two-filter spine explicit),
  launch-ready (Opus/cold/test-only), **not launched**. The sibling-mode tests the human asked for ride
  M4-0-04 (not a separate slice). **Next = session 29 pre-launch revalidation** of the two session-28
  outputs (overcorrection watch per the retro two-way-valve rule) → on clean, launch M4-0-04 → ingest
  outcome + review → judge intent-match → run M4 → M5a.

## Operational actions (beyond milestones)

Not every entrypoint is a milestone or a feature step. **Operational** entrypoints maintain the
design lifecycle's own artifacts — they recur as the plan evolves rather than closing once. The
estimates pair (**E10** extract, **E11** recurring recalc) is the first of this class; the **late-input
register** (**E21** — track post-SR1 inputs + their absorption) is another. Estimates were formalised
as a rule in [`design/agents/process.md`](design/agents/process.md) §7 and bound in
[`design/agents/sdlc.md`](design/agents/sdlc.md). When a milestone is added or pivoted, E11 fires
alongside the milestone work — keep it open as a standing reminder, don't close it after one use.

## How implementation runs now — two planes (read before commissioning)

- **Orchestration plane = the brainlab session (this one).** It commissions implementation prompts,
  authors/adjusts specs (corrective + adjacent closure slices `design/spec/MN-NN-<why>.md`, **never**
  editing the frozen `MN.md`), and **ingests outcome + review to decide** approve / corrective-take /
  escalate. It does **not** write feature code or run the review itself.
- **Execution plane = lightweight sessions outside brainlab**, driven by the human, tiny context
  (`/agents/rules.md` + `/agents/development.md` + one task): **implement** (Sonnet) → commits +
  `outcomes/MN….md`; **review** (Opus) → `reviews/MN-NN.md`.
- Full model: [`notes/talk/implementation-orchestration-model.md`](notes/talk/implementation-orchestration-model.md);
  black-box mechanics: [`implementation/README.md`](implementation/README.md).

## Conventions in force

- **Doc versioning (C1 — semver-not-milestones):** persistent docs reference **versions**, not
  milestone ids — milestones are a pre-release suffix `-mN` on the single target `0.1.0` (squashed to
  `0.1.0` at release). Markers: *"since 0.1.0-m2"* / *"planned for 0.1.0-m7"*. (Codified into
  `process.md §9.3` / `sdlc.md` — E16, session 21.)
- **No silent suppression (C2 — warn-don't-swallow):** any silently-dropped action `Log.warn`s, and
  durable instances are covered by a contract test. Settled session 17.
- **In-code `DEFERRED (0.1.0-mN)` markers:** open/postponed review questions live as greppable
  `-- DEFERRED (0.1.0-mN): … — to be resolved in design session` comments at the code site — never a
  raw `REVIEW`, never silently removed. Keeps intermediary decisions *visibly* intermediary.
- **Adjacent specs** (corrective takes, closure slices): `design/spec/MN-NN-<why>.md` — per-milestone
  counter from `01`, short purpose suffix in the filename; in-doc id stays bare `MN-NN`. Corrective
  **prompts and outcomes mirror** the spec filename. The design-time `MN.md` slices stay **frozen**.
- **Debt placement:** `/doc/development/technical_debt.md` holds **persistent** (cross-feature) debt
  only; **#77-interim** debt lives in [`implementation/technical_debt.md`](implementation/technical_debt.md)
  and is **swept or formally accepted before the feature ships**. Closures that are *committed* get an
  adjacent spec; *anticipated/conditional* items stay as ledger notes until a need appears.

## State of play

- **Design** — converged (E1) + canonical lifecycle (E4), both revalidated. Roadmap + per-milestone
  specs `design/spec/M1…M8.md` are the frozen input.
- **Implementation** — M0 ✅, M1 ✅ (reviewed), M2a ✅. **M2 take-1 ❌**, **M2-01 ✅**, **M2-02 ✅**. **M2 human review closed (E12 ✅, session 17) — code sign-off-clean** (triage: `implementation/reviews/M2-human-review.md`). **M4-0 ✅ APPROVED (session 23)**: 717/1 pending, Opus review approve-with-non-blocking-follow-ups; keyboard excluded from char net (Tier-2 forward acceptance); maze = textinput-channel anchor; carry-forwards to M4 test-first. Forward path: **E14 ✅** → **E20 ✅** → **E9 ✅** → **E16 ✅** → **E8 ◐**. **Session-24:** M4-0 reframed → M4 front-tests (`spec/M4-0-01-front-tests.md`). **Session-25:** M4-0-01 executed + reviewed (approve), but its tests are **implementation-coupled** → **M4-0-01 sign-off DEFERRED**. New primary goal **E24** (formalize framework input contracts, two cold Opus prompts — P1 launched / P2 commissioned) → **E25** (commission `M4-0-02`, contract-level test rewrite) → then **run M4** → **E23** (commission M5a). **Session-23 roadmap pivots still standing:** M5 split (M5a callbacks / M5b handlers deferred; `spec/M5-01-split.md`); `compy.before_exit` slotted (M6-02; `spec/M6-02-before-exit.md`); E11 recalc ≈76h/≈45h `version03`. **Session 30 (reimported):** M4-0-04 confirmed **landed** (`0e66cb9`) and reviewed three times over (commissioning + independent Opus + human manual pass); a fourth read surfaced one more gap + two recommendations, none yet applied. **Session 31:** the convergence pass ran (suite rewritten to the DoD, `bc32d89`), verified green (`M4-0-05`, 718/0/0/12, `dfd95c7`) — **E26 closed**. Execution model **pivoted**: rather than the two-plane per-milestone model, a **Fable-governed sweep** (`agents/sweep.md` boot pointer + `implementation/prompts/M4-M8-sweep-mandate.md` workflow) is commissioned to run **M4 → M5a → M6 → M7 → M8** inside the M0 image as serial milestone sessions (`implementation/sessions/sessionNN/`, brainlab-style handover prompts, cold restart per milestone), with a **hard human gate at every milestone boundary** plus judgment-call stops, and just-in-time prompt authoring approved by the human before execution. **Not yet launched** — `session01/prompt.md` (M4) is pre-authored; next actual work happens there. **Sessions 32–33 (E29):** the sweep's M4 **ran and landed** (project `c6b5b30`) but drew the architect pushback (`c537909`, ~48 REVIEW remarks); root cause = day-one derivation mutation, not implementor hallucination. Resolution executed session33: core model **RATIFIED** (Gate 1, `design/notes/ratified-model.md`), `design.md`/`spec.md` **re-derived + approved** (Gate 2, + scoping ruling: next simplification round = console/editor migration), plan **re-cut M5c → M7 → M8** under the amended `implementation/prompts/M5c-M8-sweep-mandate.md`, estimates ≈ 77 h/≈ 45 h (`version04`) — **Gate 3 pending (session34 closes it, then the sweep resumes with M5c)**. Dashboard refreshed: `design/status.md` (`status.versions/version02`).
- **Sweep gate:** `implementation/technical_debt.md` must be clear (closed or accepted) before #77 ships — **C-2** closed; F-5→M7-01, G-1→M8-01, G-2→M6-01 planned; F-4 accepted; `combo_string`/`gui_k`/path-mismatch + new G-A/G-B/turtle-`Esc` anticipated/needs-investigation.
- **Build continuity vs product BC** — product backwards-compat is **withdrawn**, build-time continuity
  is **in force**; `{M,C,V}` is partly transitional scaffolding (read `notes/talk/build-continuity-vs-product-bc.md`
  before M4).
- See topic `README.md` (two-phase model), `design/README.md` (the chain), and `notes/talk/` for how
  we got here.
