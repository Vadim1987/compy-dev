# Implement — M4-0: feature-global characterization net + harness extension (test-first precondition)

> **⚠ SUPERSEDED (session 24, 2026-06-28) by [`M4-0-01-front-tests.md`](M4-0-01-front-tests.md).**
> This prompt produced the characterization net now being reworked into M4 front-tests. Run the
> corrective prompt instead. Kept as the record of what was originally commissioned.

_Generated by LLM(Claude Opus 4.8): 2026-06-23 (session 22); Approved by human?: NOT YET;_
_Black-box execution prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: **Claude Sonnet**, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot execution agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). This is the
**`M4-0` precondition slice** — Tier 1 of the feature's two-tier test strategy: a **feature-global
characterization safety net** plus the **harness extension** that net needs. It is the **guardrail for
M4–M7**: it pins *current observable input behaviour* so the upcoming rework (M4 gate removal, M5
dispatch, M6 boundary model, M7 retarget) cannot silently regress it.

This is a **test / test-harness slice — you change NO production code** (nothing under `src/`). The one
deliberate exception to "everything green" is a single forward assertion that documents behaviour M4
will add (`isrepeat` threading); it is carried as **pending** (see step B-3), so the suite stays green
and the pre-commit hook passes.

Do the job, prove the net has teeth, record the outcome, then **present a summary and wait for the
human to approve** — do **not** blindly exit; the human may contest and demand fixes.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M4-0-characterization-net.md`** — the
   **authoritative** spec. Scope, Part A (harness extension), Part B (characterization coverage), the
   one forward assertion, and the Acceptance list are binding. *(The spec is `Approved by human?: NOT
   YET` — it earns approval at this execution; if you find it wrong, stop and record it, don't patch
   the design.)*
2. **`doc/development/wip/77-new-input-api/entrypoints/E9-architect-call.md`** §1, §6, §9 — *context*
   for **why** this net exists and the load-bearing constraints: the infra-feasibility verdict
   (raw-handler pattern, **not** `EditorSession`), the fresh-only combo-repeat decision, and **P1
   (event order is NOT a runtime guarantee)** — the net must not bake keypressed→textinput ordering in.
3. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (code hard-limits + style, commit
   conventions, doc index). Auto-loaded via the repo-root `CLAUDE.md`. Note **KISS** and **"do not
   fix/overoptimize beyond the current request; report discovered tech debt, don't fix it."**
4. **`doc/development/tests.md`** — the test harness + coverage map; note `mock.keystroke(...)` and the
   `EditorSession` helper.
5. **The live code (read, do not edit):**
   - **The base precedent — `tests/input/keys_pressed_spec.lua`.** This is the pattern you build on: it
     `mock.mock_love({...})`, sets `love.handlers = {}`, `require('controller.controller')`, calls
     `Controller.setup_callback_handlers({ cfg = { mode = 'dev' } })`, then grabs
     `love.handlers.keypressed` / `.keyreleased` and **calls those installed closures directly**. That
     is exactly the production slot M4 rewrites, at exactly this feature's scale.
   - `src/controller/controller.lua` — `setup_callback_handlers` (`:543`) installs the real
     `handlers.keypressed` (`:554`, today `function(k)` — note: **no `scancode`/`isrepeat` param**;
     that's the incidental drop M4 fixes), `handlers.textinput` (`:660`), `handlers.keyreleased`
     (`:669`). **Read these — do not edit them.**
   - `tests/mock.lua` — `keystroke(s, press, hold)` (`:58`) fires **only** `keypressed` per non-modifier
     key (`mods` table folds `C`/`S`/`M`/etc. into held set); it does **not** emit `textinput` and
     **cannot** emit a repeat. Those two gaps are your Part A extension.
   - `tests/helpers/editor_session.lua` — the editor-block-nav helper; its `press` drives the *editor
     controller* method, **bypassing `love.handlers`**. **Reuse it UNCHANGED** for the editor
     `is_at_limit` vertical block-nav coverage item only — it is **not** the base harness.
   - The example projects under `src/examples/` you will characterize: `tixy`, `balloons`, `turtle`,
     `keyboard`, `maze`, `pong` (docs under `doc/development/internals/examples/` + `keyboard.md`).

## Do — in this order

### Part A — harness extension (the net cannot be written without it)

**A-1. New keypress-level driver: `tests/helpers/input_session.lua`.** A small helper on the
**`keys_pressed_spec` raw-handler pattern** (NOT an `EditorSession` generalisation): set up the real
handlers via `setup_callback_handlers`, expose press / release / type / repeat over the **installed
`love.handlers.{keypressed,textinput,keyreleased}`** slots — i.e. drive a real project input flow
through the production slots. Keep it minimal and shaped after the existing spec's setup; do not invent
dispatch infra (it already exists).

**A-2. Extend `tests/mock.lua`:**
- **`textinput` emission** — a way to emit `textinput(t)` that is **independently orderable** relative
  to `keypressed` (a test must be able to assert the two-channel split *without* assuming a sequence —
  see P1). Either a new `mock.type(s,...)` / `mock.textinput(t,...)` sibling **or** a `keystroke`
  option; the binding contract is: **keypressed and textinput are separately injectable and
  order-independent.** Drive the *installed* `love.handlers.textinput`, not `controller:textinput(...)`
  directly (that direct-call shortcut is the A8 anti-pattern we are correcting).
- **`isrepeat` (+ `scancode`) emission** — `mock.keystroke` (or the driver) must be able to emit a
  *repeat* press, i.e. `keypressed(k, scancode, isrepeat=true)`, so the net can assert repeat handling.
- Keep existing `keystroke` callers working (additive extension, no behaviour change to current tests).

### Part B — characterization coverage: `tests/input/characterization_spec.lua` (new; split files if it gets large)

All assertions go through the **A-1 driver** or the **real `compy.input` / public surface** — **never**
assert implementation internals (this is the test-the-contract correction). The oracle is **current
runtime behaviour** (organically grown, no spec) — capture it **green against the code as it is now**.

**B-1. Pin these current behaviours green** (per the spec's Part B list — read it for the intent of each):
- Example input flows: **tixy** `input_code`, **balloons** `input_text`, **turtle** `input_text` + Esc
  dismiss, **editor REPL** submit.
- **`keyboard`** example (the showcase): auto-repeat debounce ("hit"/"knock" **once per physical
  press**) and release-boundary staleness — the exact P1/P2 surface later milestones must keep working.
- **editor `is_at_limit` vertical block-navigation** — pin the existing whole-input vertical semantics
  **now** (M6 rewrites `is_at_limit` to horizontal+line scope and must not regress this). Use
  `tests/helpers/editor_session.lua` **unchanged** for this item.
- **D-9 native coexistence** — **`pong`**'s `love.keypressed` still reaches the project (this is what
  the M4 `ProjectInputController` lifecycle-split wrapper must preserve after gate removal).
- **`maze`** — legacy `user_input()`/`input_text()` + `is_empty()` polling, native `love.keypressed`
  indirection, manual edge-tracking + Shift-held modal + Shift+Enter multiline.

**B-2. P1 — do NOT encode keypressed→textinput ordering as an invariant.** A synchronous harness must
not go false-green on a sequence the real device doesn't honour. Add **one comment** stating the
order-independence is intentional. Where a flow needs both channels, assert the *outcome*, not the order.

**B-3. The one forward assertion — `isrepeat` threading (carried as pending).** Current code **drops**
`isrepeat` (the `:554` wrapper is `function(k)`). Threading it is an M4 change, not yours. Add **one**
test expressing the target — *`isrepeat` reaches the keypressed path* — as a busted **`pending(...)`**
(so the suite stays green and the hook passes), with its intended assertion written in the body and a
greppable marker:
`-- DEFERRED (0.1.0-m4): isrepeat threading — M4 threads isrepeat at controller.lua:554; convert this pending → live assertion then.`
This is the regression-undo guardrail: M4's prompt will convert it to a live, green `it(...)`. Do **not**
make it a hard-failing test (it would red the suite and block the pre-commit hook). Do **not** thread
`isrepeat` yourself — that is M4's job and would be production-code overreach.

### Part C — prove the net has teeth, then verify

**C-1. Run the suite** — `busted tests` (or `just ut_all`). The full existing suite (**701** last known)
plus your new characterization tests must be **green**; the one forward test shows as **pending**, not
failed.

**C-2. Prove at least the load-bearing characterization tests have teeth.** For a representative few
(e.g. the `keyboard` once-per-press debounce and one example input flow), **temporarily** perturb the
relevant production behaviour *in your working tree only* (do not commit it), confirm the
characterization test **turns red**, then **restore the code exactly**. A characterization test that
stays green when the behaviour it claims to pin is broken has no teeth — fix it. Record each
perturb→red→restore in the outcome.

**C-3. Commit locally** — Conventional Commits (`test:` / `chore(test):`); the **`pre-commit` hook runs
the suite**, so it must be green (the forward test being `pending` keeps it so). If the hook isn't
installed, run `busted tests` first.

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M4-0-characterization-net.md`**:

- **Commit(s)** — hash + subject.
- **Files changed** — exact paths (all under `tests/`).
- **Harness shape** — the `input_session.lua` driver API and the `mock.lua` textinput/`isrepeat`
  additions you settled on (so M4 and the M5/M6/M7 test-first steps can build on them).
- **Coverage** — which Part B flows/examples are pinned, and any you **could not** characterize (with
  why — that's an input to the M4 black-box-vs-escalate call, per E9 §3).
- **Verification** — real `busted` counts (green + the one pending); the **C-2 perturb→red→restore**
  records (which assertion fired, where).
- **Surfaced gaps** — anything the spec didn't anticipate, any path/contract mismatch, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- **Test / harness files only** in the committed diff: `tests/helpers/input_session.lua` (new),
  `tests/mock.lua` (extended), `tests/input/characterization_spec.lua` (+ siblings if split), and the
  outcome ledger. **No production-code change** — nothing under `src/`. If you find you *must* touch
  `src/` to characterize a behaviour, **stop** and record it under "Surfaced gaps" (it likely means the
  behaviour isn't characterizable black-box — a signal for the M4 escalate-vs-black-box call).
- Do **not** thread `isrepeat` (M4's job), and do **not** change `tests/helpers/editor_session.lua`.
- The C-2 perturbation **must not appear in any commit**.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — the specs are frozen input you read.
