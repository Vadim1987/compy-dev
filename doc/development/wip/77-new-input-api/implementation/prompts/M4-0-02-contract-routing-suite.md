# Implement — M4-0-02: complete the keyboard/text routing matrix (contract gap-fill)

_Generated by LLM(Claude Opus 4.8): 2026-06-29 (session 26); Approved by human?: NOT YET._
_Self-contained execution prompt. All paths are relative to the repo root (= your cwd)._
_Target executor: **Sonnet** (constrained, spec-bound gap-fill — escalate to Opus only if an
observability call gets subtle). Runs inside the **M0** dev image (lua, luarocks, busted, love2d, just)._

## You are

A one-shot execution agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). A prior slice
(**M4-0-01**) reduced a mis-aimed characterization net into a routing **front-tests** suite —
`tests/input/input_routing_spec.lua` — that drives real production slots and asserts the real consumer.
A contracts chain then produced a verified, validated, **revalidated** record of the framework's
input-routing contracts (`notes/input-contracts.md`). Cross-checking the contract table against the
existing specs shows the stable-now contracts are **almost entirely already covered** — except a small
gap in the keyboard/text **EXCLUSIVE** matrix. Your job is to **fill exactly that gap**, so the routing
matrix is provably complete before M4 rewrites the gate. This is a deliberately **small** slice.

**Test-only — you change NO production code** (nothing under `src/`). Prove the new tests have teeth,
record the outcome, then **present a summary and wait for the human to approve** — do not blindly exit.

## Why this exists (and what is deliberately OUT of scope)

#77 rewrites the keyboard/text routing (overlay-gate removal → `ProjectInputController` + slot
ownership) across M4→M8. M4-0-01 already guards keypressed routing (console/editor/overlay) and overlay
exclusivity for keypressed. The **gap**: the same EXCLUSIVE guarantee for **textinput** and
**keyreleased** is not asserted, so the rewrite could double-deliver or drop them silently.

Explicitly **out of scope** (do not add — these are covered or deliberately excluded; see the contract
note):
- **§4.6 activation/reset** (warn-on-no-op, force→text-only, fresh-empty, identity, submit-reprompt) —
  already owned by `tests/input/singleton_spec.lua` + `overlay_spec.lua`. Do not duplicate.
- **§4.1 held-key lifecycle** — owned by `tests/input/keys_pressed_spec.lua`.
- **§3.7 wheel** — orthogonal to the rewrite (wheel is never gated; no `handlers.wheelmoved` exists), so
  a regression guard buys nothing. **Skip it.**
- **§3.4 `inspect`** — **provisional** (the contract note tags it "kept for now, expected to change under
  routing unification"). Carry it as a **characterization `pending`**, NOT a green regression guard (a
  deliberate inspect change must not red this suite). See the charge.

## Read first

1. **`doc/development/wip/77-new-input-api/notes/input-contracts.md`** — the contract record. The rows you
   make executable: **§3.2 textinput EXCLUSIVE**, **§3.3 keyreleased EXCLUSIVE** (and the §3.1/§3.8
   "EXCLUSIVE — widget XOR base sink" rule they instance). For the optional pointer guard: **§3.5/§3.6
   mouse/touch BOTH** (note: the *order* is **incidental** — assert only that **both** receive, R3). For
   inspect: **§3.4** (read why it is provisional). Honour the **OUTCOME vs MECHANISM** cut — assert who
   receives, never the gate mechanism.
2. **`tests/input/input_routing_spec.lua`** — what you grow. **Keep its spine**: the production-slot
   setup, the `record_calls` spy, `show_input_overlay`, the `must-not-degrade` group, the editor
   block-nav row, and the 4 `pending` forwards. You **add a few rows**, you do not rewrite the file.
3. **`tests/helpers/input_session.lua`** — the gate driver. It already exposes `press / release / type /
   repeat_press` — **enough for the keyboard gap, no change needed there.** Extend it with pointer
   emitters **only if** you include the optional mouse/touch guard (see below).
4. **`agents/rules.md`** — authoritative ruleset (64-char lines, body/param/nesting limits, Conventional
   Commits, KISS, report-don't-fix). Auto-loaded via repo-root `CLAUDE.md`.
5. **Live code (read; edit only the test files named in Boundaries):** `src/controller/controller.lua`
   (the gateway: `setup_callback_handlers` `:543`, the `get_user_input()` gate `:19`/`:20`,
   `handlers.textinput` `:660`, `handlers.keyreleased` `:669`; for the optional pointer guard,
   `handlers.mousepressed` `:689` … `touchmoved` `:784` — each is `if get_user_input() then widget.C:…;
   if love.<ev> then return love.<ev>(…)`, i.e. **BOTH** built on the gate). `consoleController.lua`,
   `userInputController.lua` — the real consumers you assert on.

## The charge — in this order

### 1. Group 1 (required) — complete the keyboard/text EXCLUSIVE matrix, GREEN now
Add to the `must-not-degrade` group, driven through the gate, asserting real consumers — written to
survive the M4/M5 rewrite (outcome-level, no mechanism coupling). Mirror the existing keypressed
exclusivity test:
- **textinput is EXCLUSIVE** — with the overlay active, `session.type('Z')` reaches the overlay sink and
  the native `love.textinput` slot fires **0 times** (no double-delivery); with no overlay, it reaches
  the base sink.
- **keyreleased is EXCLUSIVE** — same shape for `session.release('a')`: overlay-only when active, base
  sink otherwise, never both.

### 2. Inspect (required) — characterization `pending`, NOT a guard
Add `pending('inspect: console owns input — provisional')` under a `describe('inspect ownership')`. In
the body (documented, not executed) state the current behaviour — under `app_state='inspect'` a shown
project widget is **not** honoured; the console/REPL receives — and a marker:
`-- PROVISIONAL (contract §3.4): kept for now, expected to change under routing unification; characterize, do not regression-guard. Revisit when the routing model lands.`
Do **not** make this a green assertion — a deliberate inspect change must not red the suite.

### 3. Mouse/touch BOTH (optional collateral) — include only if cheap and it has teeth
Pointer delivery (§3.5/§3.6) is built on the same `get_user_input()` gate M4 removes, so the widget half
could silently die. If you include it: extend `input_session.lua` with `mousepressed`/`touchpressed`
emitters firing the **real** gateway slots, then assert that with the overlay active **both** the widget
sink **and** the base sink receive (order **not** asserted, R3). If it cannot be made to red by
perturbing the routing (no teeth) or needs `src/` changes to observe, **drop it and record why** — it is
optional.

### 4. Naming & hygiene (C3)
No test/`describe` string contains a milestone/sprint id (`M4`, `D-9`, etc.) — name by **behaviour**.

### 5. Prove teeth, then verify
- **Teeth:** for **each required Group-1 test** (textinput, keyreleased), temporarily perturb the
  relevant production routing **in your working tree only**, confirm it **reds**, then **restore
  exactly**. Record each perturb→red→restore. (The inspect `pending` has no teeth by design; the pointer
  guard, if included, must.)
- **Run** `busted tests` (or `just ut_all`): full suite green; pendings show as **pending**, not failed.
- **Commit locally** — Conventional Commits (`test:` / `chore(test):`). The pre-commit hook runs the
  suite, so it must be green. No perturbation may appear in any commit.

## Record the outcome (required, before you wait)

Create **`doc/development/wip/77-new-input-api/implementation/outcomes/M4-0-02-contract-routing-suite.md`**:
- **Commit(s)** — hash + subject. **Files changed** — exact paths (all under `tests/`).
- **What was added** (textinput + keyreleased EXCLUSIVE; the inspect characterization `pending`; the
  pointer guard if included — or why dropped).
- **Verification** — real `busted` counts (green + pendings); the perturb→red→restore records.
- **Contract-note feedback** — any §3 row that proved **not** black-box observable or contradicted by
  code (a finding for the orchestrator — **do not edit the note**), or "none".

## Then — wait for approval

Present a short summary (suite counts, what landed, the pointer-guard decision, any cell you could not
assert and why) and **stop for human review**. Revise on contest; done only when the human approves.

## Boundaries & rules

- **Test files only** in the committed diff: `tests/input/input_routing_spec.lua`, optionally
  `tests/helpers/input_session.lua` (extended for the pointer guard), the outcome ledger. **No `src/`
  change.** If a Group-1 cell needs `src/` to pass, **stop** and record it — it means the contract is not
  black-box observable, itself a finding.
- **Do not duplicate** what `singleton_spec` / `overlay_spec` / `keys_pressed_spec` already own (§4.6,
  §4.1). **Do not add wheel.** **Do not** turn inspect into a green guard.
- **The contract note + inventory are read-only input.** If a row is wrong or unobservable, **stop and
  record it** — do not patch the note or the design.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — specs are frozen input you read.
