# Normalize the input current-behaviour doc + classify by blast radius (E27)

_Generated by LLM(Claude Opus 4.8): 2026-07-01 (session 31); Approved by human?: NOT YET._
_Cold, self-contained authoring prompt. All paths are relative to the repo root (= your cwd)._
_Target executor: **Sonnet 5** (docs/specs/code coordinates; the strategic calls are already made —
you apply them, you do not reopen them). Read-only on `src/`/`tests/`; you edit docs + emit one map note._

## You are

A one-shot **authoring** agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). Your job:
take the input subsystem's already-mapped current behaviour and **normalize it into a single trusted,
legible, complete current-behaviour document (doc A)**, then produce a **validation map** that bridges
that doc to the existing test suite. You distil and organize; you **do not** redesign the architecture,
the API, or the test strategy — those decisions are made and cited below.

Do the job, then **present a summary and wait for the human to approve** — do not blindly exit; the human
authored the governing plan and will validate the outcome, contest, and may demand corrections.

## Why this exists (governing premise — read carefully)

The compy input architecture was built ad-hoc with **no up-front design doc**, so **the current
implementation is the canonical specification** of its input behaviour. Feature #77 is about to rewrite
the routing (overlay-gate removal, a new `ProjectInputController`, slot ownership/restoration) across
milestones M4–M8. The rewrite cannot be judged "successful integration" vs. "catastrophe" without the
framework's current input contracts **written down, complete, and trusted** first. Prior passes produced
the raw material (a contract record + a deep assessment) but it is **not yet in one trusted, legible
shape** — that is this task. The governing plan is
[`doc/development/wip/77-new-input-api/notes/plan.md`](../../notes/plan.md); the reframe behind it is
[`notes/talk/safety-net-as-characterization-replan.md`](../../notes/talk/safety-net-as-characterization-replan.md).

**Discipline (inherited, non-negotiable): separate OUTCOME from MECHANISM.**
> **OUTCOME = the contract** to preserve: for a given `(mode, widget-state)`, which consumer receives each
> event and under what exclusivity. Binding; the rewrite must keep it true.
> **MECHANISM = current realization**, free to change (the `if love.state.user_input then …` gateway, the
> slot-swap, the `{M,C,V}` shape, `app_state` branching). Document mechanism **only** as "current
> realization (non-binding)", clearly marked.

## Read first (primary sources — build on them, do not re-derive from scratch)

1. **`doc/development/wip/77-new-input-api/notes/plan.md`** — the governing plan. Its **Definition of
   done**, **Scope/blast-radius classification**, and **two-part canonical doc** sections are the rules you
   apply. Do not reopen them; realize them.
2. **`doc/development/wip/77-new-input-api/notes/input-contracts.md`** — the current contract record.
   This is the document you **normalize in place** into doc (A). It is sound in substance but grew
   incrementally: incomplete in spots, carries session-internal shorthand, uneven provenance.
3. **`doc/development/wip/77-new-input-api/notes/assessment/*.md`** — the 2026-07-01 deep assessment
   (keypressed-vs-textinput, keyreleased-isrepeat-events, cursor-and-reset-operations,
   shared-input-widget-singleton, fr2-fr6-fr7-provenance-and-gaps, inspect-mode-current-state) +
   the synthesis `notes/plan-reevaluation-against-session-findings.md` and
   `notes/m4-0-04-safety-net-review.md`. These carry findings to **fold in** (below).
4. **`doc/development/internals/user_input.md`** — the existing descriptive narrative. It stays the
   "how it currently works" prose; doc (A) **cross-links** it. Light edits only (fix cross-links / add the
   pointer to doc A). Do not duplicate it wholesale into doc A.
5. **`tests/input/input_contracts_spec.lua`** (read-only) + **`implementation/reviews/M4-0-04.md`** — the
   suite you build the validation map against, and the independent review naming two still-open findings.
6. **`agents/rules.md`** — repo ruleset + doc-format conventions (auto-loaded via repo-root `CLAUDE.md`).

## The target — apply this, do not improvise around it

### Deliverable 1 — normalize `notes/input-contracts.md` into doc (A)

Doc (A) = **"how the input subsystem works today."** Make it satisfy the plan's four-part
**definition of done**:

- **(DoD-1) Complete by construction, visibly.** The contract table must cover **every** `mode × channel`
  cell — modes {console/REPL, editor, project-running, inspect} × channels {keypressed, textinput,
  keyreleased, pointer} — plus the `search` sub-widget. Every cell is either a stated contract row or an
  explicit **gap/`pending` marker naming what is missing**. No silent absence. Group by mode so a hole is
  visible on sight.
- **(DoD-2) Every contract carries provenance.** Tag each with its source: a `design/` clause (path:§),
  a stakeholder quote, or the explicit tag **`[characterized from current runtime — not yet ratified as
  desired]`**. No untagged assertion.
- **(DoD-3) Architect-legible cold.** Remove session-internal shorthand (`D-α`, `R1`–`R7`, `F-A`–`F-E`,
  bucket-pass IDs) from the contract prose; where a decision id is load-bearing, replace with a
  plain-language statement + a doc-link to where the id is defined. Add, once near the top, a short
  **keypressed-vs-textinput** explainer (control channel vs. character channel; compy convention, not a
  LÖVE guarantee — see `notes/assessment/keypressed-vs-textinput.md`).
- **(DoD-4) Every surfaced gap dispositioned** (see Deliverable 2's classification).

**Findings to fold in** (verify each at the cited `path:line` in the assessment notes before stating it):
- The **`keyreleased` console-only fork** — even with no widget/overlay, `ConsoleController:keyreleased`
  has no `app_state=='editor'` fork, so editor/search instances never receive `keyreleased`
  (`assessment/keyreleased-isrepeat-events.md` §2). The current §3.3 characterization misses this;
  correct it.
- The **`inspect`-mode input suppression** (`get_user_input()` returns nil while inspecting; project
  overlay force-hidden; console owns input) — undocumented in both internals docs today
  (`assessment/inspect-mode-current-state.md`).
- The **four incompatible `reset()`/cancel implementations** and the **cursor two-layer split**
  (`assessment/cursor-and-reset-operations.md`).
- **`search`** as a third full MVC triad reusing the widget, absent from the whole design corpus
  (`assessment/shared-input-widget-singleton.md`).
- The **held-key-set** rows characterize inert, not-yet-consumed internal state → label as
  **mechanism-guard**, not behavioural contract.

### Deliverable 2 — classify every finding by #77 blast radius (a fenced section in doc A)

Per the plan's scope decision (whole-subsystem is already mapped; residual blind-spot risk accepted):
- **In blast radius** (M4–M8 touch it): keypressed/textinput/keyreleased routing across the four modes,
  the overlay gate, submit/cancel/limit. → stated as **binding contracts** now.
- **Out of blast radius — kept as foundation for the stated future console/editor adoption of the new
  input API:** the four `reset()` implementations, `inspect`-mode suppression, the `search` sub-widget,
  the `keyreleased` console-only fork, the cursor two-layer split. → recorded in a clearly-titled section
  **"Out of #77 blast radius — foundation for future console/editor migration,"** each with a one-line
  note of why it matters to that later work. **Captured, not dropped; not fixed now.**

### Deliverable 3 — the validation map (new note `notes/input-suite-validation-map.md`)

A table bridging doc (A) → `tests/input/input_contracts_spec.lua`, so the suite cleanup (next task, E26)
is mechanical. One row per doc-(A) contract:

| Contract (doc A ref) | Suite assertion | Status | Note |

Status ∈ {**present** (cite `input_contracts_spec.lua:line`) · **add** (no coverage yet) · **pending**
(forward contract, correctly parked) · **mechanism-guard** (keep, label honestly) · **out-of-scope**
(foundation, no suite row)}. Explicitly include the two **open Opus findings** from
`reviews/M4-0-04.md` (editor keypressed-EXCLUSIVE sibling missing → **add**; `F.reset()` 16>14 lines →
mechanism/rules, **add**) and the **`search` gap** (→ **add** as `pending`). Do **not** edit the suite —
only map it.

## Boundaries & rules

- **Docs only.** Edit `notes/input-contracts.md` (→ doc A), lightly `internals/user_input.md`
  (cross-links + a pointer to doc A), and create `notes/input-suite-validation-map.md`. **No `src/`
  change. No `tests/` change** (the suite cleanup is a separate task). `git diff --stat src tests` must be
  empty except any file you were explicitly told to touch (none in those trees).
- **Do not redesign.** No new API decisions, no test-strategy changes, no re-litigating the plan's DoD or
  blast-radius calls. If you find something that seems to need a *decision* (not just documentation),
  **report it, do not decide it** — surface it in your closing summary for the human.
- **KISS / report-don't-fix.** Discovered non-blocking debt outside this task's scope is reported, not
  fixed.
- **Doc conventions** per `agents/rules.md` (line length in codepoints, no milestone ref-ids leaking into
  durable docs, doc-links over bare §-ids).
- **Git:** commit your doc changes (conventional `docs(contracts): …`), author from the recent commit
  identity, **do not push**.

## When done

Present a summary: what doc (A) now guarantees vs. what it explicitly marks as gaps; the blast-radius
classification (in vs. out counts); the validation-map disposition counts (present/add/pending/…); and
any decisions you had to surface rather than make. Then **wait for the human to validate** — expect
contest and possible corrections before this is approved.
