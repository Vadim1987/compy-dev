# Implement — M1: `keys_pressed` table

_Generated by LLM(Claude Opus 4.8): 2026-06-16 (session 05); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Assumes the dev image from **M0** (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent working in the **compy** LÖVE2D codebase (this repo, root = your
cwd). Read the spec, implement it, test it, commit it, record the outcome — then **present a summary
and wait for the human to approve**. Do **not** blindly end the session: the human may contest your
work and ask for fixes; the job is done only when they approve.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M1.md`** — the milestone spec slice.
   **Authoritative** for what M1 must deliver: the `Controller.keys_pressed` contract, the
   `combo_string` contract (with worked examples), and the Acceptance criteria.
2. **`doc/development/wip/77-new-input-api/design/spec.md` §1** — the full cross-cutting contract for
   this surface (context; the slice is the binding subset for M1).
3. **`doc/development/wip/77-new-input-api/design/roadmap.md`** → milestone **M1** — description and
   risk ("None — purely additive"; no dependencies).
4. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (code hard-limits + style,
   design principles, commit conventions, doc index). The repo-root `CLAUDE.md` auto-loads it; follow
   it. It supersedes any convention restatement — don't look for rules elsewhere first.
5. **Test harness** (not covered by `rules.md`) — `doc/development/tests.md`: note
   `mock.keystroke("C-return")` (prefixes `C`/`S`/`M` = Ctrl/Shift/Alt) and the existing input specs
   under `tests/input/`.
6. **The live code you will touch:** `src/controller/controller.lua`.
   - NOTE: the design docs say `src/controller.lua`; the **real path is `src/controller/controller.lua`**.
   - The LÖVE key dispatch wiring is already there: `set_love_keypressed` (~L161),
     `set_love_keyreleased` (~L195), and the `'keypressed'`/`'keyreleased'` handler list (~L29).
     Read the whole file first to see how handlers are installed before adding to them.

## Do

1. **Implement exactly the M1 slice's Contract + Acceptance — nothing more.**
   - Maintain `Controller.keys_pressed` (a `{ keyname → true }` set, **precise** LÖVE key names, no
     l/r folding) — add on keypressed, remove on keyreleased, updated **before** any downstream
     handler runs.
   - Add `combo_string(k, keys_pressed)`: modifier-first fixed precedence `ctrl, alt, shift, gui`,
     **l/r-folded** modifier names, triggering key appended, bare key → just the key name. Match the
     slice's worked examples exactly (`"ctrl+s"`, `"alt+shift+f4"`, `"escape"`, `"s"`).
2. **Scope discipline.** M1 is **purely additive, zero behaviour change.** Do **not** implement
   anything the slice defers downstream — no read-only proxy wrapping, no `ProjectInputController`,
   no dispatch changes (M4/M5). Touch only what M1 needs.
3. **Tests.** Add busted tests under `tests/input/` covering the Acceptance criteria: `keys_pressed`
   add/remove on press/release; `combo_string` multi-modifier ordering, l/r folding, bare-key case.
   Use the existing `mock`/`mock.keystroke` harness; follow the patterns in the neighbouring specs.
4. **Run the suite** — `busted tests` (or `just ut_all`). All existing tests must still pass and your
   new tests must pass. Fix until green.
5. **Docs.** M1 is additive with no consumer-facing API change, so a `doc/development/` update is
   likely unnecessary. If you judge one warranted (e.g. `keyboard.md` or `internals/`), make it;
   otherwise state "none needed" in the outcome.
6. **Commit locally.** Use the repo's **existing committer config** (do not change git identity);
   follow the commit conventions in `agents/rules.md` — M1 is a `feat`. The `pre-commit` hook runs
   the suite; if it isn't installed, run `busted tests` yourself first. Keep it to one focused commit
   (or a small, logical sequence).

## Record the outcome (required, before you wait for approval)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M1.md`**:

- **Commits** — the commit hash(es) and subject line(s) you made.
- **Files changed** — exact paths.
- **Verification** — the command run (`busted tests`) and the **real result** (pass counts; the new
  test names). Do not fabricate — report what actually ran.
- **Surfaced gaps** — anything the spec didn't anticipate, any path/contract mismatch, or "none".

## Then — wait for approval

Present a short summary of what you did and **stop for human review**. If the human contests
anything, fix it (amend or add commits) and update the outcome ledger. Consider the job complete
only once the human explicitly approves.

## Boundaries

- Stay within M1's file list: `src/controller/controller.lua` + new test file(s) in `tests/input/`
  (+ docs only if warranted). If you find you **must** touch more to make M1 work, **stop** and
  record it under "Surfaced gaps" rather than expanding scope.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — it is a frozen input.
