# Implement — M4-0-01: rewrite the M4-0 net into M4 front-tests (corrective)

_Generated by LLM(Claude Opus 4.8): 2026-06-28 (session 24); Approved by human?: NOT YET;_
_Corrective execution prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: see the spec's note + the orchestrator's engine call (Sonnet is a candidate — this is a constrained, spec-bound rewrite)._
_Runs inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot execution agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). A prior slice
landed a feature-global **characterization net** (`tests/input/characterization_spec.lua`) that pinned
the wrong thing — per-example input behaviour that product backwards-compat **withdrew** and M8 will
migrate. Your job is the **corrective**: **reduce that net to M4's front-tests** — a small routing-level
suite guarding what M4 must not break and driving, test-first, what M4 must implement.

This is a **test-only slice — you change NO production code** (nothing under `src/`). Group-2 tests below
describe behaviour M4 has **not built yet**; carry them as `pending` so the suite stays green and the
pre-commit hook passes (M4 will convert them).

Do the job, prove the kept tests have teeth, record the outcome, then **present a summary and wait for
the human to approve** — do **not** blindly exit; the human may contest and demand fixes.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M4-0-01-front-tests.md`** — the **authoritative**
   spec for this slice (Scope, the front-tests, Acceptance, Files). Binding. *(Spec is `Approved by
   human?: NOT YET` — it earns approval at this execution; if you find it wrong, stop and record it,
   don't patch the design.)*
2. **`doc/development/wip/77-new-input-api/design/spec/M4.md`** — *context* for the behaviour Group 2
   drives (ProjectInputController, gate removal, slot restoration, D-9 native-coexistence wrapper,
   `isrepeat` threading). You do **not** implement any of it — you only express it as `pending` tests.
3. **`doc/development/wip/77-new-input-api/design/spec/M4-0-characterization-net.md`** — the
   **superseded** spec, for context only. Do not follow it.
4. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (64-char lines, body/param/nesting
   limits, conventional commits, KISS, **report-don't-fix discovered debt**). Auto-loaded via repo-root
   `CLAUDE.md`.
5. **The live code (read; edit only the test files named in Files):**
   - **`tests/input/characterization_spec.lua`** — what you rewrite. Note its current setup: `mock_love`,
     `love.handlers = {}`, `setup_callback_handlers({cfg={mode='dev'}})`, then it grabs the installed
     `love.handlers.{keypressed,textinput,keyreleased}` closures. Keep that production-slot setup.
   - **`tests/input/keys_pressed_spec.lua`** — the raw-handler pattern reference (drives the installed
     `love.handlers.*`). Group-1 tests use this style.
   - **`tests/helpers/input_session.lua`** — the keypress-level driver. **Consume it** from the suite
     (the prior net inlined a copy — dead code). Trim it to what the reduced suite needs.
   - `src/controller/controller.lua` — `setup_callback_handlers` (`:543`), the `get_user_input()` gate
     accessor (`:19`), `handlers.keypressed/textinput/keyreleased` (`:554`/`:660`/`:669`), the
     keypressed harvest wrapper `:554` (`function(k)` today). **Read — do not edit.**
   - `src/controller/consoleController.lua`, `editorController.lua` — the real consumers Group-1 asserts
     receive input. **Read** to drive/assert against the real controllers, not lambdas.
   - `tests/mock.lua` — the `textinput` emitter + `keystroke` `isrepeat`/`scancode` opts already exist.
     **Keep them.** Use them; do not duplicate.
   - `tests/helpers/editor_session.lua` — used **unchanged** for the editor block-nav row only.

## Do — in this order

### 1. Delete the wrong-target coverage
From `tests/input/characterization_spec.lua` remove: the example-BC rows (tixy/balloons/turtle/maze
submit + `is_empty`), the D-9 alias trio (`session.press` → `love.keypressed`, etc. — tautologies), and
the **reimplemented** keyboard once-per-press debounce (it asserted a test-local `held_keys`, not the
example). Keep the production-slot setup and the harness usage.

### 2. Group 1 — regression smoke, GREEN now (assert real controllers, via the gate)
Drive through the installed `love.handlers.*` slots and assert the **real** consumer received — written
so they stay valid after M4 rewrites the gate:
- **console, no overlay** → input reaches `ConsoleController`.
- **editor mode** → input reaches `EditorController`.
- **overlay active** → the singleton sink receives **and** the native handler is **not** also invoked
  (no double-delivery / exclusivity).
Keep the **editor vertical block-nav at-limit** row (`editor_session.lua`, unchanged); reword its
comment to state it exercises block-nav **indirectly** through the at-limit condition.

### 3. Group 2 — new behaviour, carried `pending` (M4 will green)
For each, write a busted `pending('<behaviour>', function() … end)` whose body documents the target
assertion, with a greppable marker
`-- DEFERRED (0.1.0-m4): <behaviour> — M4 implements it; convert pending → live it() then.`:
- project running + singleton active → **project key events reach `ProjectInputController`** (not dropped).
- **slot restoration** — slot occupied by `ProjectInputController` during run; restored to
  `ConsoleController` on project stop.
- **native coexistence** — legacy project (native `love.keypressed`, no `compy.*`): native fires when
  singleton hidden, sink receives when visible.
- **`isrepeat` threading** — `isrepeat` reaches the keypressed path (carry over the existing forward
  assertion).
Do **not** implement any of this in `src/` — that is M4's job.

### 4. Naming — no milestone/sprint ids in tests
No test description or `describe` contains `M4`, `D-9`, "characterization net", or similar. Name by
behaviour. **Rename the file** to a behaviour name (e.g. `tests/input/input_routing_spec.lua`) via
`git mv`; update any require/runner references.

### 5. Prove teeth, then verify
- **Teeth:** for the no-double-delivery test and one Group-1 receive test, **temporarily** perturb the
  relevant production routing *in your working tree only* (do not commit), confirm the test **reds**,
  then **restore exactly**. Record each perturb→red→restore.
- **Run** `busted tests` (or `just ut_all`): full suite green; Group-2 shows as **pending**, not failed.
- **Commit locally** — Conventional Commits (`test:` / `chore(test):`). The pre-commit hook runs the
  suite, so it must be green (Group-2 pending keeps it so). The C-5 perturbation must not be in any commit.

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M4-0-01-front-tests.md`**:
- **Commit(s)** — hash + subject. **Files changed** — exact paths (all under `tests/`), incl. the rename.
- **What was removed** vs **what was kept/added** (Group 1 green, Group 2 pending).
- **Driver disposition** — how `input_session.lua` is now consumed (and what you trimmed).
- **Verification** — real `busted` counts (green + pendings); the perturb→red→restore records.
- **Surfaced gaps** — anything the spec didn't anticipate, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- **Test files only** in the committed diff: the rewritten/renamed routing spec, `input_session.lua`
  (trimmed), the outcome ledger. **No `src/` change.** If you find you must touch `src/` to make a
  Group-1 test pass, **stop** and record it (it means the behaviour isn't observable black-box).
- Do **not** implement Group-2 behaviour, and do **not** change `tests/helpers/editor_session.lua` or
  `tests/mock.lua` (the emitters already exist — use them).
- **Never edit `doc/development/wip/77-new-input-api/design/`** — specs are frozen input you read.
