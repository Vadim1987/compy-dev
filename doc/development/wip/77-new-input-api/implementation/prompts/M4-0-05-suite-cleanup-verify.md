# Verify — M4-0-05: contract-suite cleanup (run + mend)

_Generated by LLM(Claude Fable 5): 2026-07-02 (session 31); Approved by human?: NOT YET;_
_Verification prompt. Self-contained. Paths relative to the repo root (= your cwd)._
_Target executor: **Claude Sonnet**, inside the **M0** dev image (lua, busted, just available)._

## Context — what changed, why you exist

The input contract suite was rewritten **without a lua runtime** (an orchestration-plane
legibility/DoD pass — per-mode `describe` nesting, provenance refs to doc A, resolved review
remarks, two review findings applied). The rewrite intends **zero behavioural change to what is
asserted**, plus one new test and three new `pending` gap markers. Your job: run it, confirm the
intent, mend only mechanical breakage.

Files changed (only these):

- `tests/input/input_contracts_spec.lua` — restructured; all `-- REVIEW:` lines resolved+removed.
- `tests/helpers/input_fixture.lua` — `restore_native_slots()` extracted from `F.reset()`
  (14-line-limit fix, `reviews/M4-0-04.md` finding 2); comment refs updated.
- (Also reconciled, not your scope: `implementation/prompts/M4.md`.)

## Expected result

Baseline was **717 pass / 9 pending** suite-wide, all 9 pendings in this file. The rewrite adds
**+1 test** (editor keypressed-EXCLUSIVE — `reviews/M4-0-04.md` finding 1) and **+3 pendings**
(console keyreleased gap, editor pointer gap, search routing gap). So expect:

- `busted tests` (or `just ut_all`): **718 pass / 12 pending / 0 failures**.
- Hard limits hold: ≤64-char lines, ≤14-line function bodies (check the two changed files).

## Deliberate changes you must NOT "fix back"

1. **Editor keys test is new** (`routes keys to the editor`): type `q`, press `backspace`,
   assert editor empty + console empty. Its sibling is retitled `routes text to the editor`.
2. **Legacy submit test is now event-driven**: text arrives via `F.session.type('4')` /
   `type('2')` through the real gate (widget shown ⇒ overlay diverts text to it), replacing the
   old direct `F.singleton:set_text('42')`.
3. **Stop test strengthened** (`the console receives after stop`): installs a real project
   handler via `F.running_project` first and asserts it receives **nothing** after stop.
4. **Pointer causality**: the console pointer test asserts `is_false(has_selection())` BEFORE
   the pointer events.

## If something fails — triage rules

- **Mechanical breakage** (typo, wrong helper name, a Lua syntax slip, a missed `local`): fix it
  minimally, keep the documented assertion intent, note it in the outcome.
- **Fallbacks pre-authorized** (apply only if the specific test fails, note which you used):
  - (2) legacy submit: if gate diversion doesn't deliver the typed text, revert those two lines
    to `F.singleton:set_text('42')` and add a one-line comment that direct seeding is a known
    concession (gate diversion of textinput to the soliciting widget could not be exercised).
  - (3) stop test: if `stop_project_run()` does not reset `app_state` and routing breaks,
    drop the `F.running_project` wrapper — install `love.keypressed = counter` directly (the
    pre-rewrite shape) but KEEP the `assert.equal(0, got)` line.
  - (4) pointer precondition: if `set_text` itself creates a selection, delete only the
    precondition assert and note it — do not touch the postcondition.
- **Anything semantic** (an assertion that seems to test the wrong thing, a contract question):
  **STOP. Do not weaken or delete an assertion, ever.** Record it and end with a failure report.

## Record the outcome (required)

Fill `doc/development/wip/77-new-input-api/implementation/outcomes/M4-0-05-suite-cleanup-verify.md`:
real busted counts, fixes applied (exact diff-level description), fallbacks used (which, why),
surfaced gaps or "none". Commit as `test(input): verify + mend contract-suite cleanup` — only if
green. Then present a short summary and wait for human review.
