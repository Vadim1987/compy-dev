# Implement — M7: extended widget-surface API (`configure`/`clear`/cursor/`set_text`)

_Generated by LLM(Claude Fable 5): 2026-07-06 (brainlab session34, Gate 3 — pre-authored with the M7-02 re-cut spec, human-ruled); Approved by human?: NOT YET (Gate 3 in review; Opus corpus-validation pass pending);_
_Black-box implementation prompt. Self-contained. All paths relative to the repo root (= your cwd)._
_Target executor: **Claude Sonnet**, inside the **M0** dev image (lua, luarocks, busted, love2d, just available), under the governance of [`M5c-M8-sweep-mandate.md`](M5c-M8-sweep-mandate.md)._

> ⚠ **Commission only after M5c has landed and passed its human gate.** At commissioning,
> spot-check this prompt's file paths and surface names against the M5c outcome ledger; if
> anything moved, reconcile at a gate round before starting.

## You are

A one-shot implementation agent in the **compy** LÖVE2D codebase (this repo, root = your cwd).
This is **M7 — a purely additive surface slice** of feature #77: the live-reconfiguration +
cursor/text API on the input widget. No routing or dispatch behaviour changes. Implement,
test (test-first), commit, record the outcome, then **present a summary and wait for the
human to approve** — do **not** blindly exit; the human may contest and demand fixes.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M7-02-recut.md`** — the **authoritative**
   milestone slice: Contract, the folded-in M7-01 re-target decision, Delta, and the
   **Acceptance criteria (AC-1…AC-12)** your tests transcribe. *(The old `spec/M7.md` is
   superseded as a target — do not implement against it.)*
2. **`doc/development/wip/77-new-input-api/design/spec.md` §3/§6** — the cross-cutting
   contract (widget lifecycle; cursor/text control incl. the consumers note); the slice is
   the binding subset. §7 for the mutable/immutable boundary you must not widen.
3. **`doc/development/wip/77-new-input-api/implementation/prompts/M5c-M8-sweep-mandate.md`** —
   the governing guardrails. Non-negotiables for you: **no in-slice design rulings** (a spec
   gap or contradiction = stop + gate round); **citation-or-stop** outcome bullets;
   **surprise-first** outcome ledger; no new public surface without a named consumer.
4. **`agents/rules.md`** + **`agents/development.md`** — the repo's compiled ruleset (hard
   limits, "no C accent", conventional commits, KISS, report-don't-fix, tests-first).
   Auto-loaded via the repo-root `CLAUDE.md`.
5. **The live code you will touch:** `src/controller/userInputController.lua` (the five
   methods), `src/model/input/userInputModel.lua` (`set_text` must honour `keep_cursor` —
   skip the unconditional jump-to-end), the `compy` namespace setup (expose the five
   callables; assignment to them must error loudly), `tests/input/` (the contract suite's
   m7-family `pending` rows are yours to green).

## Do — in this order

1. **Red suite first.** Transcribe `M7-02-recut.md`'s AC-1…AC-12 into acceptance tests under
   `tests/input/`; convert the contract suite's m7-family `pending` rows into live
   assertions. Run — they must be red for the right reasons before you implement.
2. **Implement** `configure` / `clear` / `get_cursor` / `set_cursor` / `set_text` per the
   slice Contract: live-updatable prompt/highlighter/validator/widget-output callbacks;
   `text`/`cursor` accepted-but-inert on an active session; clamped cursor; `keep_cursor`;
   **warn on every refused call** (hidden no-ops) — never a silent swallow.
3. **Close the M7-01 boundary** exactly as the slice's Riding decision states; document the
   re-target contract in `doc/development/internals/` (no milestone ids in prose); strike
   F-5 from `doc/development/wip/77-new-input-api/implementation/technical_debt.md`.
4. **Scope discipline.** No dispatch/routing work, no M8 legacy removal, no new assignable
   surface. If the M5c-landed code contradicts the slice — **stop**, record, gate round.
5. **Run the full suite** (`busted tests` or `just ut_all`) — all former m7 pendings green,
   everything else stays green.
6. **Manual check:** drive a live widget session in project mode; exercise
   configure/clear/cursor/set_text by hand; confirm REPL and editor modes are untouched.
   Record exactly what you exercised; if you cannot run a mode, say so — do not claim it.
7. **Commit locally** — Conventional Commits; independently revertible commits; the
   pre-commit hook must be green. **This repo only** (mandate guardrail 7 — though M7
   touches no nested checkout, the rule stands).

## Record the outcome (required, before you wait)

Fill `doc/development/wip/77-new-input-api/implementation/outcomes/M7.md`, opening with the
mandatory **"what will surprise the architect"** section (mandate guardrail 3), then:
commit refs · files changed · verification (real busted counts; the manual-check record) ·
per-AC checklist (AC-1…AC-12, each `met` with the test name, or the stop that blocked it) ·
the M7-01 closure record · surfaced gaps or "none". Every non-obvious decision bullet cites
its source (`M7-02-recut.md §…`, `spec.md §…`) — uncitable = it was a judgment call = you
should have stopped.

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the
human explicitly approves.

## Boundaries

- Stay within the slice's Files list; on overreach, **stop** and record it under gaps.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — frozen input you read.
- No M8 work; no dispatch-chain rework; no renaming of landed M5c surface.
