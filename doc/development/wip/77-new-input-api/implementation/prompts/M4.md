# Implement — M4: `ProjectInputController` + overlay-gate removal

_Generated by LLM(Claude Opus 4.8): 2026-06-23 (session 22); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: **Claude Sonnet**, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). This is
**M4 — the largest, highest-risk integration step** of feature #77: introduce `ProjectInputController`
and **remove the overlay gate** so the input singleton is always the sink, never a special-cased
routing destination. You also **thread `isrepeat` (+ `scancode`)** back through the keypressed harvest
wrapper (the E20 regression-undo), which flips the M4-0 net's one pending forward test to green.

You are **guarded by the M4-0 characterization net** — it pins current observable behaviour. M4 must
keep that net green (except the one forward test, which it turns from pending → live-green). Implement,
test, **manually verify all four app modes**, commit, record the outcome, then **present a summary and
wait for the human to approve** — do **not** blindly exit; the human may contest and demand fixes.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M4.md`** — the **authoritative** milestone slice:
   the Contract (activation/deactivation, overlay-gate removal, D-9 native-handler coexistence) and
   Acceptance. *(Spec is `Approved by human?: NOT YET` — earns approval at this execution; if you find
   it wrong, stop and record it, don't patch the design.)*
2. **`doc/development/wip/77-new-input-api/design/spec.md` §6** (`ProjectInputController`) — the full
   cross-cutting contract; the slice is the binding subset.
3. **`doc/development/wip/77-new-input-api/notes/talk/build-continuity-vs-product-bc.md`** —
   **MANDATORY before you touch dispatch.** Product backwards-compat is **withdrawn**; **build-time
   continuity is in force**. The `{ M, C, V }` handle is **partly transitional scaffolding** (`.M` has
   no non-test consumer; `.V` is the current overlay's handle). You **may** rework/drop it — **only**
   with a coordinated consumer sweep **in this same slice**, never narrow-ahead-of-the-sweep (that was
   the M2 take-1 failure that got rejected). If a clean coordinated sweep doesn't fit M4's scope, leave
   it and record it as debt — do not half-change it.
4. **`doc/development/wip/77-new-input-api/entrypoints/E9-architect-call.md`** §3 (black-box guarded by
   M4-0) and §6 (the `isrepeat` threading decision) — *context* for why M4 is black-box and exactly
   what the `isrepeat` thread is (and is **not**: the fresh-only `handlers[isrepeat][combo]` dispatch
   is **M5**, not M4 — see Scope discipline below).
5. **The M4-0 guardrail** — `tests/input/characterization_spec.lua` (+ `tests/helpers/input_session.lua`,
   the `tests/mock.lua` extensions). This is the net you must keep green. It contains **one `pending`
   forward test** marked `DEFERRED (0.1.0-m4)`: *`isrepeat` reaches the keypressed path*. Threading
   `isrepeat` is what flips it — convert that `pending(...)` to a live `it(...)` green assertion.
6. **`agents/rules.md`** + **`agents/development.md`** — the repo's compiled, authoritative ruleset
   (hard limits: 64-char lines, 14-line bodies, 4 params, 4 nesting; "no C accent"; conventional
   commits; KISS; **report-don't-fix** discovered debt; tests-first). Auto-loaded via the repo-root
   `CLAUDE.md`.
7. **The live code you will touch** *(design docs use short paths; these are the REAL paths)*:
   - `src/controller/controller.lua` *(the design spec calls it `src/controller.lua`)* — `set_handlers`
     (`:73`), the `get_user_input()` overlay accessor (`:19`), the `setup_callback_handlers` harvest
     path (`:543+`), and the **keypressed harvest wrapper `:554`** (`function(k)` today — this is where
     you thread `isrepeat`/`scancode`). Remove the overlay gate here; wire `ProjectInputController` into
     the `love.keypressed`/`love.textinput` slots for the project-running states.
   - **New: `src/controller/projectInputController.lua`** *(design spec calls it
     `src/projectInputController.lua`; place it under `src/controller/` to match the siblings)* — model
     it on `src/controller/consoleController.lua` / `editorController.lua`.

## Do — in this order

1. **Create `ProjectInputController`** (`src/controller/projectInputController.lua`) — a first-class
   sibling to `ConsoleController`/`EditorController` for the project-running context, with
   `keypressed` / `textinput` / `keyreleased`. **Sink delegation only** (the three-tier dispatch is
   **M5** — do not build it here). Include the **D-9 native-coexistence wrapper**: a project with native
   `love.keypressed`/`textinput` and **no** `compy.*` input surfaces gets `compy.input.on_key_pressed`
   auto-provisioned as a lifecycle-split wrapper (singleton visible → text-editing sink; hidden →
   project's native handler). Debug mode logs the chosen branch.
2. **Remove the overlay gate** in `controller.lua` and wire `ProjectInputController` into `set_handlers`
   / the `love.keypressed` slot for `'running'` / `'project_open'` states; restore
   `ConsoleController:keypressed` on project stop. Routing becomes symmetric — the singleton is always
   the sink, project key events are no longer silently dropped while it is active.
3. **Thread `isrepeat` (+ `scancode`)** through the harvest wrapper (`controller.lua:554`, today
   `function(k)`) so the project input path receives them. This is the regression-undo. **Then convert
   the M4-0 forward test** (`pending` → live `it(...)`, drop the `DEFERRED (0.1.0-m4)` marker) and
   confirm it is now **green**.
4. **Scope discipline.** M4 delivers **sink delegation + gate removal + isrepeat availability** only.
   Do **NOT** implement: the M5 three-tier dispatch, the `handlers[isrepeat][combo]` fresh-only
   structural keying, `combo_string` scratch-buffer, or the noop-`__index`/`or`-chain (all **M5**). You
   thread `isrepeat` so it is *available*; M5 *uses* it. Do not reach into M6/M7 either.
5. **Run the M4-0 net + full suite** — `busted tests` (or `just ut_all`). The whole net stays green; the
   former-pending forward test is now green; the full suite (**701+** plus M4-0's adds) passes.
6. **Manually verify all four app modes** (the spec's runtime acceptance — the net alone does not cover
   live integration): **REPL**, **editor**, **project + overlay**, **project, no overlay**. Record
   exactly what you exercised in each. If you cannot run a mode, say so — do not claim it.
7. **Docs.** M4 changes an internal contract (routing, controller set) — update the relevant
   `doc/development/` docs (`internals/user input`, etc.) where behaviour/shape changed. Keep milestone
   ref-ids out of doc prose (`development.md`). State "none needed" only if truly none.
8. **Commit locally** — Conventional Commits (`feat:` for the controller/gate work; the test conversion
   rides with it or as a `test:` commit). Existing committer config; the `pre-commit` hook runs the
   suite (must be green).

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M4.md`**:

- **Commit(s)** — hash + subject.
- **Files changed** — exact paths.
- **Verification** — real `busted` counts (incl. the now-green forward test); the **four-mode manual
  verification** record (what each mode exercised, and any not run); confirmation the M4-0 net stayed
  green.
- **`{M,C,V}` disposition** — what you did with the scaffolding handle (reworked-with-sweep / left +
  logged as debt) and why.
- **Surfaced gaps** — anything the spec didn't anticipate, any path/contract mismatch, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves. *(If M4 proves it cannot be characterized/integrated black-box — design churn,
real judgment calls — **stop** and say so: per E9 §3 it then escalates to a managed subtopic.)*

## Boundaries

- Stay within M4's file list: `src/controller/projectInputController.lua` (new),
  `src/controller/controller.lua`, the **one** M4-0 forward-test conversion in
  `tests/input/characterization_spec.lua`, plus any `doc/development/` docs warranted and new tests for
  M4's own behaviour. If you must touch more, **stop** and record it under "Surfaced gaps".
- **No M5/M6/M7 work** — no three-tier dispatch, no `handlers[isrepeat][combo]`, no `combo_string`
  rework. Thread `isrepeat`, don't *dispatch* on it.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — the specs are frozen input you read.
