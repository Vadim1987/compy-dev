# Implement — M2-02: restore the full `{ M, C, V }` input overlay (corrective take)

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 06); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: Claude Sonnet, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent working in the **compy** LÖVE2D codebase (this repo, root = your
cwd). This is a **corrective take**: the first M2 attempt (commit `2245aa5`) extracted the singleton
correctly but **narrowed a live interface** and let a behaviour regression slip past CI. Read the
corrective spec, write the failing tests **first**, then fix — commit, record the outcome, and
**wait for the human to approve**. Do **not** blindly end the session.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M2-02.md`** — the **corrective spec**.
   **Authoritative** for this take: C-1 (restore the `{ M, C, V }` overlay handle), C-2 (restore
   empty-on-fresh-prompt), C-3 (tests first, covering the previously-uncovered overlay path), C-4
   (correct the outcome record). It also lists what is **explicitly out of scope** — do not touch
   those.
2. **`doc/development/wip/77-new-input-api/design/spec/M2.md`** — the original frozen milestone slice,
   for context. M2-02 must **not** regress anything M2 already passed (singleton identity,
   `show`/`hide`/`force` method semantics, `oneshot` retained, `compy.input` surface).
3. **`doc/development/wip/77-new-input-api/implementation/reviews/M2-01.md`** — the take-1 review.
   F-1 (the `.V` blocker) and F-2 (stale text) are what you are fixing; it has the exact consumer
   (`controller.lua:401`) and the example reachability table (`turtle`, `tixy`).
4. **`/agents/rules.md`** — the repo's compiled, authoritative coding ruleset (hard limits: 64-char
   lines, 14-line bodies, 4 params, 4 nesting; formatting; "no C accent"; conventional commits).
   Auto-loaded by the repo-root `CLAUDE.md`; follow it.
5. **`/agents/development.md`** — the working rules. In particular for this take: **"start with
   breaking tests, then fix by implementation"**, **KISS**, and **"do not fix/overoptimize beyond
   the current request and its blockers — discovered non-blocking tech debt must be reported, not
   fixed on the spot."** That last rule is exactly what take 1 violated; honour it here.
6. **The live code:**
   - `src/controller/userInputController.lua` — `show()` (~L200) currently sets
     `love.state.user_input = { C = self }` (L212); `apply_config` (~L184) only sets text when
     `cfg.text ~= nil`. The singleton owns `self.model` (constructor) and `self.view` (via
     `init_view`, wired in `main.lua`).
   - `src/controller/controller.lua:401` — the running-project draw overlay: `ui.V:draw()`. This is
     the consumer take 1's `{ C }` shape broke.
   - `src/main.lua` (~L360) — where the startup singleton is built (`ui_c`, `init_view(ui_v)`,
     `love.state.user_input_controller = ui_c`).

## Do — tests first, then fix (in this order)

1. **Write the corrective tests FIRST and watch them fail (C-3).** Before changing any production
   code:
   - An **overlay-shape test** that drives the real path the suite missed: assert
     `love.state.user_input` carries a usable, drawable view after `show()` — ideally exercising the
     **startup singleton** (`main.lua` wiring) and the `love.draw` overlay (`ui.V:draw()` / the
     `set_love_update` draw wrapper), **not** an ad-hoc `make_ctrl` mock. This test must **fail**
     against the current `{ C = self }` shape.
   - An **empty-on-reprompt test**: submit a oneshot session, `show()` again with no `text`, assert
     the model opens **empty**. This must **fail** against the current persistent-model behaviour.
   - Run `busted tests` and confirm both are **red**. Record the red result; you will cite the
     red→green transition in the outcome.
2. **Fix C-1 — restore the full triade.** In `show()`, set
   `love.state.user_input = { M = self.model, C = self, V = self.view }`. Do **not** rewrite the
   overlay to reach the view through the controller (that is a deliberate M4-era change, out of scope
   here) — restore the handle the overlay already expects.
3. **Fix C-2 — restore empty-on-fresh-prompt.** On a **fresh activation** (a `show()` transitioning
   `love.state.user_input` from inactive to active) with **no `text`/`init`**, the model must open
   **empty**. Put the clear at the **fresh-activation (or submit) boundary** — **not** in `hide()`
   and **not** in the already-active `force` path: M2's `hide()`-preserves and `force`-preserves
   semantics must stay exactly as they are.
4. **Re-run the suite** — `busted tests` (or `just ut_all`). The two new tests must now be **green**,
   the 11 existing method-semantics tests unchanged, and nothing else regressed.
5. **Confirm at runtime (C-2 is runtime-sensitive).** Drive `turtle` (or `tixy`) through submit →
   re-prompt: it must (a) no longer fault on the input frame (C-1) and (b) re-open **empty** (C-2).
   Report what you actually observed.
6. **Honour the scope fence.** Do **not** touch the items M2-02 lists as out of scope: F-4
   (`compy.input` per-env build — accepted as-is), F-5 (`force` re-applies only text) — both recorded
   in the feature's interim ledger
   `doc/development/wip/77-new-input-api/implementation/technical_debt.md` — and G-1/G-2 (dispatched to
   `M8-01.md` / `M6-01.md`). If you spot anything else, **report it as a surfaced gap**, do not fix it
   (per `development.md`).
7. **Docs.** If take 1's edit to `doc/development/internals/user_input.md` described the `{ C }`
   shape, correct it to `{ M, C, V }`. Keep milestone ref-ids out of the prose (per `development.md`).
8. **Commit locally.** Existing committer config (do not change git identity); conventional commits —
   this take is a `fix` (the test commit may be `test:` first, then the `fix:`, which also documents
   the red→green order). The `pre-commit` hook runs the suite; if not installed, run `busted tests`
   first.

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M2-02.md`**:

- **Commits** — hash(es) + subject line(s).
- **Files changed** — exact paths.
- **Verification** — the **red-before / green-after** test results (cite both), the full-suite counts,
  and the **runtime** observation for `turtle`/`tixy` (no fault + empty re-prompt). Report what
  actually ran — do not fabricate.
- **Corrected shape record (C-4)** — state the real `love.state.user_input = { M, C, V }` shape and
  its consumers (`controller.lua:401` reads `.V`; the `.C` reads listed in the take-1 ledger). Do
  **not** repeat the inaccurate "no consumer reads `.V`" claim from the take-1 G-3.
- **Surfaced gaps** — anything new, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- Stay within M2-02's file list: `src/controller/userInputController.lua`, the two tests under
  `tests/input/`, the outcome ledger, and the internals doc if it needs correcting. If you must touch
  more to make the fix work, **stop** and record it under "Surfaced gaps".
- **Never edit `doc/development/wip/77-new-input-api/design/`** — the specs (including `M2-02.md`) are
  frozen input you read, not edit.
