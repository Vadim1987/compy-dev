# Implement — M2-02: real-submit reprompt test (close the C-2 acceptance gap, test-only)

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 07); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: Claude Sonnet, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). This is
a **test-only** slice. The M2-01 fix (empty-on-fresh-prompt) already ships and is correct; you are
adding the regression net the review withheld the C-2 tick on — a test that drives the **real oneshot
submit** path, not a proxy. **Do not change any production code.** Add the test, prove it has teeth,
record the outcome, and **wait for the human to approve** (do not blindly exit).

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M2-02-submit-path-test.md`** — the
   **authoritative** spec: C2T-1 (the real-submit test) and C2T-2 (the fidelity / mutate→red check),
   plus the test-only guiding constraint and the out-of-scope fence.
2. **`doc/development/wip/77-new-input-api/design/spec/M2-01-restore-mvc.md`** §C-2 — the behaviour
   under test (fresh activation with no text opens empty; `hide()` and `force` preserve content).
3. **`doc/development/wip/77-new-input-api/implementation/reviews/M2-01-restore-mvc.md`** — the open
   C-2 gap and its runtime addendum (runtime is already confirmed via turtle; you close the unit-test
   half).
4. **`/agents/rules.md`** + **`/agents/development.md`** — repo conventions (auto-loaded via the
   repo-root `CLAUDE.md`). Note **KISS** and **"do not fix/overoptimize beyond the current request;
   report discovered tech debt, don't fix it."**
5. **The live code (read, do not edit):**
   - `src/controller/userInputController.lua` — `open_fresh` (`:202`, the clear at `:203-205`),
     `show()` (`:218`), `hide()` (`:232`), `is_empty()` (`:61`).
   - `src/model/input/userInputModel.lua` — `handle(eval)` (`:803`): on a successful oneshot submit it
     pushes the `userinput` event (`:813-819`) but **never** clears `entered` — that is exactly why a
     reprompt would re-open pre-filled without the M2-01 fix.
   - `tests/input/overlay_spec.lua` — the existing proxy test + the `make_ctrl` / `mock.mock_love`
     harness to reuse.

## Do — test-only (in this order)

1. **Add the real-submit reprompt test (C2T-1).** In `tests/input/overlay_spec.lua` (or a new
   `tests/input/reprompt_spec.lua`): build a **oneshot** controller (as `overlay_spec.lua` does),
   `show()`, give it **distinctive** text (`set_text("REMEMBER_ME")`), call **`model:handle(true)`**
   (mock `love.event.push` / `love.harmony` as needed so the submit runs), then `hide()`, then
   `show()` with **no `text`**, and assert `c:is_empty()` is true (and, if cheap, that the submitted
   value is gone). The test **must** drive `handle(true)` — not a `set_text → hide → show` proxy.
2. **Run the suite** — `busted tests` (or `just ut_all`). C2T-1 must be **green**; the existing 698
   tests unchanged.
3. **Prove it has teeth (C2T-2, fidelity check).** Temporarily neutralise the clear in `open_fresh`
   (comment out `if cfg.text == nil then self.model:clear_input() end` at
   `userInputController.lua:203-205`), re-run, and confirm **C2T-1 turns red**. Then **restore** the
   code exactly. **Do not commit the mutation.** Record the mutate→red→restore in the outcome. If
   C2T-1 stays green with the clear removed, the test is not driving the real path — fix the test.
4. **Honour the scope fence.** No production-code change in the committed diff. If C2T-1 reveals the
   fix is actually wrong (reprompt is *not* empty after a real submit), **stop** and record it as a
   surfaced gap — that is a new corrective take, not something to patch here.
5. **Docs.** None expected (test-only). If you touch a doc, keep milestone ref-ids out of the prose.
6. **Commit locally.** Conventional commits — a single `test:` commit. The `pre-commit` hook runs the
   suite; if not installed, run `busted tests` first.

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M2-02-submit-path-test.md`**:

- **Commit** — hash + subject.
- **Files changed** — exact paths (tests only).
- **Verification** — C2T-1 green result + full-suite counts; the **C2T-2 mutate→red→restore** record
  (the failing assertion location when the clear is removed). Report what actually ran — do not
  fabricate; the mutation must not appear in the commit.
- **C-2 closure** — strike the **C-2 acceptance gap** row in
  `doc/development/wip/77-new-input-api/implementation/technical_debt.md` (unit-test half now closed)
  and note it here.
- **Surfaced gaps** — anything new, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- **Test files only** in the committed diff: `tests/input/…`, the outcome ledger, and the one struck
  row in the interim debt ledger. **No production-code change.** If you must edit production code to
  make the test pass, **stop** and record it under "Surfaced gaps".
- **Never edit `doc/development/wip/77-new-input-api/design/`** — the specs are frozen input you read.
