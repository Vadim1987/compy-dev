# Implement — M5c: the dispatch chain (consolidated slice — the behaviour change)

_Generated by LLM(Claude Opus 4.8): 2026-07-06 (brainlab session35, Gate 3 — authored with the amended M5c slice, human-directed); status revised by LLM(Claude Opus 4.8): 2026-07-06 (brainlab session36 — demoted from binding commission to advisory outline, human-directed)._
_Approved by human?: N/A — advisory, not a gated commission._

> **STATUS: advisory recommended-decomposition — NOT a binding commission.** Under the corrected
> topology the **opus-sweeper (PM)** owns carving the M5c slice into small independently-reviewable
> chunks and writing each chunk commission to disk. This file was authored (before that correction)
> as a single 14-step black-box prompt; it is retained as a **recommended execution outline** the
> sweeper may take as a starting point **and is free to reconsider, re-chunk, or supersede**. The
> **authority is the slice `design/spec/M5c-dispatch-chain.md`** (Scope items 1–10 = the architect's
> ordered decomposition); this file never overrides it. Do not treat the 14 steps as a fixed contract.

_Target executor: **Claude Sonnet**, inside the **M0** dev image (lua, luarocks, busted, love2d, just), under the governance of [`M5c-M8-sweep-mandate.md`](M5c-M8-sweep-mandate.md). Orchestration + gates + final chunk commissions are held by the opus-sweeper PM (`agents/sweep.md`)._

> ⚠ **This is the widest slice of feature #77 and the first real behaviour change.** It **rewrites
> the M4-landed routing code** (`controller.lua`, `projectInputController.lua`) to the ratified
> four-tier chain — it does not build on a blank slate, and it does not preserve M4's shape. M4 was
> built to a *superseded* spec (the day-one derivation mutation E29 reversed); parts of it are
> wrong and must flip. When the landed code and this slice disagree, **the slice wins** — but if the
> disagreement looks like a genuine spec gap or contradiction, **stop + gate round** (mandate
> guardrail 1), never an in-slice ruling. Risk ≈ 2× M4 (PERT); order your work in independently
> revertible commits.

## You are

A one-shot implementation agent in the **compy** LÖVE2D codebase (this repo, root = your cwd).
This is **M5c — the dispatch chain**: the complete four-tier keyboard/text routing (framework
handlers → project combo handlers → generic callback → sink), submit/cancel, widget outputs, the
route-connection lifecycle, the mutable boundary, the pinned QUALITY corrective, and the
turtle/maze migration — all on the project route, per the ratified model. Absorbs the old
M5a/M5b/M6. Implement test-first, commit locally, record the outcome, then **present a summary and
wait for the human to approve** — do **not** blindly exit; he may contest and demand fixes.

## Read first (the corpus that binds you — all ratified/approved)

1. **`doc/development/wip/77-new-input-api/design/spec/M5c-dispatch-chain.md`** — the
   **authoritative** slice: Scope (10 items), the pinned SCOPE-remarks table, **Acceptance
   criteria AC-1…AC-37**, Test strategy, Files, Risk. Your red suite transcribes the ACs; your
   outcome ledger cites them by id. *(The old `spec/M5.md`, `spec/M5-01-split.md`, `spec/M6.md`
   are superseded — do not implement against them.)*
2. **`doc/development/wip/77-new-input-api/design/notes/ratified-model.md`** — the one-page
   canonical model (**on any divergence it wins**). The **glossary is binding — mint no
   architectural nouns outside it** (route / chain / tier / sink / widget / widget outputs /
   participant / gateway / slot). R1–R14 + the five rulings are the anchors the ACs cite.
3. **`design/spec.md` §1/§2/§4/§5/§7/§8/§9/§10** — the cross-cutting contract: `keys_pressed` +
   combo serialisation (§1); the four-tier chain + per-channel signatures (§2); widget outputs
   (§4); submit/cancel semantic chains (§5); mutable boundary (§7); route-connection lifecycle +
   native pure-wrap (§8); legacy removal mapping — **read for M5c's turtle/maze migration only,
   the global removals are M8** (§9); edge cases (§10).
4. **`design/design.md` §3/§5** — the submit-policy-is-route-policy framing (§3) and the
   turtle/maze sequencing constraint (§5 — why they migrate *here*, not M8).
5. **`implementation/prompts/M5c-M8-sweep-mandate.md`** — your governance. Non-negotiables:
   **no in-slice design rulings** (gap/contradiction/surprise = stop + gate round); **citation-or-
   stop** outcome bullets; **surprise-first** ledger; **REVIEW comments are design inputs**
   (guardrail 4); **no new public surface without a named consumer** (guardrail 5); **nested-
   checkout discipline** (guardrail 7 — maze); **human reversal is always in-bounds**.
6. **`notes/input-contracts.md` (doc A)** — current-behaviour contracts (still valid); the suite
   cites it by `§N`.
7. **`agents/rules.md`** + **`agents/development.md`** — the repo's compiled ruleset (hard limits,
   "no C accent", KISS, tests-first, report-don't-fix, Conventional Commits, no push). Auto-loaded
   via the repo-root `CLAUDE.md`.
8. **The live code you rewrite** (M4-landed, to a superseded spec — expect to delete/reshape):
   `src/controller/controller.lua`, `src/controller/projectInputController.lua`,
   `src/controller/userInputController.lua`, `src/model/input/userInputModel.lua` (oneshot
   removal, `is_at_limit` extension, evaluate-path decoupling), `src/view/input/userInputView.lua`
   (M6-01 oneshot snapshot), `src/util/key.lua` (matcher/normalisation, if it lands there), the
   `compy` namespace setup, `src/examples/turtle/`, `src/examples/maze/`, `tests/input/*`. Read
   `implementation/outcomes/M4.md` to understand what M4 built and why it is being reshaped.

## Do — in this order

1. **Red suite first (test-first, mandate + §9.2).** Transcribe `M5c-dispatch-chain.md`'s
   **AC-1…AC-37** into acceptance tests under `tests/input/`; convert the contract suite's
   **pending** m5/m5a/m5b/m6-family rows to live assertions. **Also re-derive the M4-landed
   *green* Bucket-B rows (AC-36):** the row `a native handler coexists with the sink` currently
   green-asserts *suppress-while-shown* — **flip it** to AC-31 (the native receives events while
   the widget is shown, falls through to the sink); re-verify `project keys reach the project
   sink`, `stop names the console as restored route`, `the keypressed path carries the triple`
   against AC-1/AC-8/AC-29. Run — everything red **for the right reasons** before you implement.
2. **The chain (AC-1…AC-11, spec §2).** Build the four tiers on all three channels in the project
   route: `framework_handlers.<event>[combo]` → `handlers.<event>[combo]` (per-event sub-tables,
   R14 — **flat table forbidden**; normalising assignment, overloadable matcher §1) → per-event
   generic callback (default **noop + debug log**) → the sink (terminal, **internal** hidden-check,
   **no external gating wrapper**). Truthy return at any tier = consumed, stop; falsey = fall
   through. Uniform signatures per channel; `isrepeat` threaded on keypressed. `keyreleased` runs
   the same shape (AC-9, ruling 4). **No replace-semantics anywhere (R13); the sink's return has
   no chain meaning (R12).**
3. **Submit/cancel (AC-17…AC-26, spec §5).** Tier-1 `keypressed['return']`/`['escape']` engage
   **only while the widget is shown**; the `before_/after_submit` + `before_/after_cancel`
   call-order chains; validator gate; delivery via **`on_text_entered(text)`** (widget output, R1
   — *not* the per-char callback); unconditional deactivate-on-submit (project-route policy);
   Escape genuinely dismisses; `hide()` fires no cancel chain. **Delete `oneshot`; dissolve
   `push('userinput')`** (notify job → `on_text_entered`; deferral job → the deactivate step —
   sync-vs-deferred is mechanism). Continuous-session idiom works via re-`show()` from
   `after_submit` (AC-24).
4. **Widget outputs (AC-12…AC-16, spec §4):** `on_text_entered`, `on_limit_reached(direction,
   scope)` (D-5 both scopes — extend `is_at_limit` to horizontal + line), `validator`,
   `highlighter` — each a config key AND an assignable field (D-b), **never** a chain return value.
5. **Route connection (AC-27…AC-30, spec §8):** keyboard/text slots occupied only while
   `'running'` (ruling 3); on drop to `'project_open'` they restore to the console route — **remove
   the M4 ruling-1 forwarding**. **Pointer slots untouched** (do not unify the pointer disconnect
   in — forbidden). Stop = full teardown invariant incl. silent widget hide. `inspect` = console
   route over the project env, project route disconnected (AC-30, R11).
6. **Legacy natives = pure wrap (AC-31/32, spec §8, R7):** capture a project's native
   `love.keypressed`/`textinput`/`keyreleased` at load as the **initial tier-3 callback values** —
   plain participants that see events **even while the widget is shown**; falsey falls to the sink;
   assigning the field replaces the wrapped native. **Delete `native_split`/the lifecycle-split
   wrapper.** **Migrate turtle + maze in this slice** (design §5 — never deferred to M8). ⚠
   **maze is a copied-in checkout** (`src/examples/maze/`, own nested `.git`, untracked):
   migrate in place but deliver as **uncommitted working-tree changes** — **never commit inside it,
   never touch its `.git`** (guardrail 7); list the changed files in the ledger. turtle is
   in-repo, commits normally.
7. **Mutable boundary (AC-33, spec §7):** exactly the `handlers` tables + `on_*`/`before_*`/
   `after_*` fields are assignable; **everything else on `compy.input` errors loudly on
   assignment** (C2 — never a silent swallow).
8. **QUALITY corrective (AC-34, Scope item 8) — each pinned remark gets an explicit disposition**
   (`fixed` / `dissolved-by-rework` / `note-only`, with the remark id) in the ledger: C1/C15/P3
   warn-on-silent-drop, C2/C5/C16/P11 naming + per-event de-dup (table-driven setup), C3/C14
   return-propagation, C9/C13 dispatch legibility, C19/C20 comments, C23 `active_keyboard_route`
   test-scoped-unless-consumed, T2/T3/T4 cite doc-A/spec + live assertions, U3 keyreleased doc.
9. **Suite retro-reconciliation (AC-37).** Every in-tree `-- REVIEW:` comment in
   `tests/input/input_contracts_spec.lua` is a **design input**: reconcile each against a cited AC
   (remove it with a ledger line) or **escalate as a stop** if it names a genuine contradiction.
   The `-- STALE (E29 …)` marker on the native-coexistence row records the required flip — honour
   it. **None silently deleted, none left dangling.**
10. **`>> REVIEW` markers** (project tree) that these rows resolve are removed, each with a ledger
    line citing the resolving contract point; markers this slice does not resolve stay.
11. **Repeat semantics — park, do not invent** (spec §2 DEFERRED 0.1.0-m5): whether combo tiers
    fire on key-repeat is **not ruled**; ship the **pre-authorized default** (existing combos keep
    current behaviour; `on_key_pressed` sees repeats via `isrepeat`) and leave an in-code
    `DEFERRED (0.1.0-m5)` marker. This is *not* a new ruling — do not escalate it, do not design a
    new mechanism for it.
12. **Run the full suite** (`busted tests` or `just ut_all`) — all former m5/m5a/m5b/m6 pendings
    green, the re-derived Bucket-B rows green, m7 rows still pending, everything else green.
13. **Manual check (record exactly what you exercised; if you cannot run a mode, say so — do not
    claim it):** the 4-mode check (REPL / editor / project+widget / project no-widget) **plus** the
    ruling-3 case (a non-blocking script — typing after `main.lua` returns reaches the REPL)
    **plus** migrated turtle and maze played by hand.
14. **Commit locally** — Conventional Commits, independently revertible commits, pre-commit hook
    green, **in-repo files only**; verify `git -C src/examples/maze status` shows changes but
    `git -C src/examples/maze log` shows **no new commit** and its `.git` is untouched.

## Record the outcome (required, before you wait)

Fill `doc/development/wip/77-new-input-api/implementation/outcomes/M5c.md`, opening with the
mandatory **"what will surprise the architect"** section (guardrail 3 — deviations,
reconciliations, new names, new public surface, the M4-code reshape), then: commit refs · files
changed (**maze files listed separately as the uncommitted patch set**) · verification (real
busted counts; the manual-check record) · **per-AC checklist AC-1…AC-37** (each `met` with the
test name, or the stop that blocked it) · the **per-pinned-remark disposition table** (Scope item
8) · the **suite `-- REVIEW:` reconciliation ledger** (AC-37) · the **`>> REVIEW`-marker removal
ledger** · surfaced gaps or "none". Every non-obvious decision bullet cites its source
(`M5c-dispatch-chain.md §…` / `spec.md §…` / `ratified-model.md` / ruling/remark id) — uncitable =
it was a judgment call = you should have stopped.

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves. Mid-slice stops (decide **with** the human, never paper over): scope
ambiguity, a spec gap, an irreversible choice, the suite won't converge, or a `-- REVIEW:` comment
that names a real contradiction with the ratified model.

## Boundaries

- Stay within the slice's Files list; on overreach, **stop** and record it under gaps.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — frozen input you read.
- **No M7 work** (`configure`/`clear`/`get_cursor`/`set_cursor`/`set_text` — those stay pending);
  **no M8 work** (legacy-global removal, other example migrations).
- **Never commit inside `src/examples/{maze,balloons,keyboard}/` or touch their `.git`.**
- Do not "unify" the pointer disconnect into ruling 3's keyboard/text scope (forbidden, spec §8).
- Do not re-green the M4 suppress-while-shown assertion — flip it (AC-31/AC-36).
