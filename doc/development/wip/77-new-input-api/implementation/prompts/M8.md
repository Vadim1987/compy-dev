# Implement — M8: legacy text-input removal + example migration (terminal milestone)

_Generated by LLM(Claude Fable 5): 2026-07-06 (brainlab session34, Gate 3 — pre-authored with the M8-02 re-cut spec, human-ruled); Approved by human?: **YES — 2026-07-07** (Gate 3 CLOSED, brainlab session40 / E33);_
_Black-box implementation prompt. Self-contained. All paths relative to the repo root (= your cwd)._
_Target executor: **Claude Sonnet**, inside the **M0** dev image, under the governance of [`M5c-M8-sweep-mandate.md`](M5c-M8-sweep-mandate.md)._

> ⚠⚠ **DO NOT COMMISSION before M5c AND M7 have landed and passed their human gates — and
> not before a REVALIDATION pass.** This prompt and its slice (`spec/M8-02-recut.md`) were
> pre-authored before M5c/M7 existed in code (human-accepted staleness risk, Gate 3). At
> commissioning: diff both against the M5c/M7 outcome ledgers (surface names, file paths,
> the widget API as actually landed) and reconcile at a gate round first. The Contract
> binds; the mechanics may need repointing.

## You are

A one-shot implementation agent in the **compy** LÖVE2D codebase (this repo, root = your cwd).
This is **M8 — the terminal milestone** of feature #77: remove the five legacy text-input
globals (and the polling idiom) and migrate the examples that used them to `compy.input.*`.
No backward compatibility — D-1 is discarded; SR1: "only text fields break". Implement,
test (test-first), commit, record the outcome, then **present a summary and wait for the
human to approve** — do **not** blindly exit.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M8-02-recut.md`** — the **authoritative**
   milestone slice: removals, the migration recipe (replacement mapping), migration scope,
   the folded-in M8-01 dead-write removal, **Delivery mechanics for balloons**, and the
   **Acceptance criteria (AC-1…AC-10)** your tests transcribe. *(The old `spec/M8.md` is
   superseded as a target — do not implement against it.)*
2. **`doc/development/wip/77-new-input-api/design/spec.md` §8/§9/§10** — teardown invariant,
   the removal mapping, edge cases.
3. **`doc/development/wip/77-new-input-api/implementation/prompts/M5c-M8-sweep-mandate.md`** —
   the governing guardrails. **Guardrail 7 is load-bearing here:** `src/examples/balloons/`
   (like `keyboard/` and `maze/`) is a copied-in checkout with its own nested `.git`,
   untracked by this repo — **never commit inside it, never touch its `.git`**; its
   migration ships as uncommitted working-tree changes, listed file-by-file in the outcome
   ledger; the human carries the patch upstream. Also in force: no in-slice design rulings;
   citation-or-stop; surprise-first ledger.
4. **`agents/rules.md`** + **`agents/development.md`** — the repo's compiled ruleset.
   Auto-loaded via the repo-root `CLAUDE.md`.
5. **The live code you will touch:** `src/controller/consoleController.lua` (the five legacy
   entry points + the dead `compy_namespace.text_input` nil write — grep-confirm no reader
   before removing), `src/examples/tixy/` (in-repo, commits normally),
   `src/examples/balloons/` (uncommitted — see above), `src/examples/{repl,guess,valid}/`
   (migrate or record excluded), `tests/input/`.

## Do — in this order

1. **Red suite first.** Transcribe `M8-02-recut.md`'s AC-1…AC-10 into acceptance tests
   (legacy globals gone = ordinary `nil` call; no polling surface remains; migrated examples
   exercised). Red for the right reasons before you implement.
2. **Remove** the five globals + the reftable/polling idiom + the M8-01 dead write;
   `love.state.user_input` driven solely by widget activation.
3. **Migrate tixy** (priority, in-repo): `input_code`-style entry → `show{}` with
   highlighter/validator, `write_to_input` → `set_text`, polling → `on_text_entered` /
   `after_submit`.
4. **Migrate balloons** (priority, foreign checkout): the hint loop becomes the
   continuous-session idiom — `show` once, re-prompt via the submit hook, `configure{prompt=…}`
   for hint updates; **leave every balloons change uncommitted**.
5. **Convert or exclude** `repl`, `guess`, `valid` — each migrated or explicitly recorded as
   excluded (owner's release call) in the outcome ledger; no third state.
6. **Scope discipline.** turtle/maze are already migrated (M5c) — do not touch them; no
   dispatch or widget-surface rework; pure-native examples untouched. A spec/code
   contradiction = **stop + gate round**, never an in-slice ruling.
7. **Run the full suite**; then **manual check:** play tixy and balloons by hand
   (composition, submit, re-prompt loop, Escape dismiss), spot-check one pure-native
   example, confirm REPL/editor unaffected. Record exactly what you exercised.
8. **Docs.** The feature is complete here — update `doc/development/` input docs to the
   final surface; archive/annotate stale wip notes (roadmap Additional Scope); no milestone
   ids in doc prose.
9. **Commit locally** — Conventional Commits, independently revertible, **in-repo files
   only**; verify `git -C src/examples/balloons status` shows changes but `git log` there
   shows no new commit (same check for keyboard/maze: untouched).

## Record the outcome (required, before you wait)

Fill `doc/development/wip/77-new-input-api/implementation/outcomes/M8.md`, opening with
**"what will surprise the architect"**, then: commit refs · files changed (**balloons files
listed separately as the uncommitted patch set**) · verification (busted counts + the manual
record) · per-AC checklist (AC-1…AC-10) · the convert-or-exclude record per example · the
M8-01 closure · surfaced gaps or "none". Citation-or-stop applies to every non-obvious bullet.

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the
human explicitly approves.

## Boundaries

- Stay within the slice's Files list; on overreach, **stop** and record it under gaps.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — frozen input you read.
- **Never commit inside `src/examples/{balloons,keyboard,maze}/` or touch their `.git`.**
- turtle/maze: hands off (landed in M5c).
