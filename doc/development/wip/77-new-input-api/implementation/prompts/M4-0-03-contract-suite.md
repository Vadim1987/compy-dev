# Implement — M4-0-03: re-author the input test suite from the contract record

_Generated by LLM(Claude Opus 4.8): 2026-06-29 (session 27); Approved by human?: NOT YET;_
_Restructure execution prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_**Target executor: Claude Opus.** This is judgment-heavy authoring (re-homing + fixture design +_
_outcome-vs-mechanism calls), **not** a transcription — it warrants the stronger model despite the_
_usual "tests run on a cheaper model" convention._
_Runs inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot execution agent in the **compy** LÖVE2D codebase (this repo, root = your cwd). The framework's
input layer grew without an up-front design doc, so the test suite grew the same way — incrementally,
each slice mirroring the prior one's idiom. The result tests **this sprint's mechanism**
(`love.state.user_input`, controller identity, slot internals) rather than the framework's **behavioural
contracts**, so the upcoming M4/M5 routing rewrite will churn it and it guards little that survives.

A clean **contract record now exists** (validated → cold-revalidated → rulings landed). Your job is to
**re-author the input suite top-down *from* that record** into four clearly-separated buckets, so the
tests describe framework behaviour — what must be **preserved** and what must be **implemented** — and
outlive the implementation. This is a **restructure**, not an add-on: you migrate the existing specs'
content into the new shape, you do not accrete more rows onto the old files.

This is a **test-only slice — you change NO production code** (nothing under `src/`). Forward-contract
tests describe behaviour that does **not** exist yet; carry them as `pending` so the suite stays green
and the pre-commit hook passes (M4/M5 will green them).

Do the job, prove the kept tests have teeth, record the outcome, then **present a summary and wait for
the human to approve** — do **not** blindly exit; the human may contest and demand fixes.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M4-0-03-contract-suite.md`** — the **authoritative**
   spec for this slice: the four buckets (with PRESERVE fully enumerated P1–P11, IMPLEMENT by milestone,
   MECHANISM-GUARD, CHARACTERIZE-PROVISIONAL, the M6/M7 stub), the authoring discipline, Acceptance,
   Files. Binding. *(Spec is `Approved by human?: NOT YET` — it earns approval at this execution; if you
   find it wrong, stop and record it, don't patch the design.)*
2. **`doc/development/wip/77-new-input-api/notes/input-contracts.md`** — the **contract record**, this
   suite's source of truth. **The §2 glossary is binding vocabulary:** `route` / `sink` / `widget`. Each
   contract §3/§4 → a PRESERVE or CHARACTERIZE-PROVISIONAL test; each §5 forward contract → a `pending`
   IMPLEMENT test. Cite section ids (e.g. `§3.1`) in test comments so each test traces to its contract.
3. **`doc/development/wip/77-new-input-api/design/spec/M4.md`** + **`M5.md`** / **`M5-01-split.md`** —
   *context* for the forward (IMPLEMENT) behaviour you express as `pending`. You do **not** implement any
   of it.
4. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (64-char lines, body/param/nesting
   limits, conventional commits, KISS, **report-don't-fix discovered debt**). Auto-loaded via repo-root
   `CLAUDE.md`.
5. **The live test files you re-home (read all; edit only test files):**
   - **`tests/input/input_routing_spec.lua`** — ~80% contract-level already (drives the real
     `love.handlers` gate). **Keep its production-slot setup**; lift the ~175 ln of MVC/gfx/font standup
     into the fixture; **replace the `record_calls(obj, name)` method-name spy** with observable-outcome
     or public-callback assertions.
   - **`tests/input/singleton_spec.lua`** — re-home: the singleton-identity / no-realloc rows →
     MECHANISM-GUARD (labelled); the show/hide/force **behaviour** → PRESERVE P10 driven through the
     public surface; **drop** the `love.state.user_input` / `.C` internals assertions (that behaviour is
     covered by PRESERVE rows at the boundary).
   - **`tests/input/overlay_spec.lua`** — re-home: behavioural → PRESERVE rows; mechanism (drawable
     handle) → MECHANISM-GUARD or drop.
   - **`tests/helpers/input_session.lua`** — the keypress-level driver. **Thicken it** (+ a small
     fixture) to carry the standup so each test reads as a contract statement; consume it, no inline copy.
   - `src/controller/controller.lua` — `setup_callback_handlers` (`:543`), `get_user_input()` (`:19`),
     `handlers.{keypressed,textinput,keyreleased,mousepressed,...}`, the keypressed harvest wrapper `:554`
     (`function(k)` today). **Read — do not edit.**
   - `src/controller/consoleController.lua` (`get_compy_input` `:347` = the `compy.input` surface;
     `CC.input` `:44` = console-line `UserInputController`; `CC.editor` `:68`), `editorController.lua`,
     `userInputController.lua` — the real consumers/surfaces you drive and assert against. **Read.**
   - `tests/mock.lua` — the `textinput` emitter + `keystroke` `isrepeat`/`scancode` opts already exist.
     **Keep them. Use them; do not duplicate.**
   - `tests/helpers/editor_session.lua` — used **unchanged** for the editor block-nav row only.

## Do — in this order

### 1. Build the fixture first
Lift the MVC/gfx/font standup out of the specs into `tests/helpers/input_session.lua` (+ a small fixture
helper if cleaner) so a test can stand up the real gate + a route in ~1 line. The boilerplate volume in
today's `input_routing_spec` is a too-thin-fixture symptom — fix that root, don't copy it around.

### 2. Bucket A — PRESERVE (green now), fully enumerated
Author P1–P11 (spec table). Drive through the public entry / installed `love.handlers.*`; **assert
observable outcomes** (state/text changed) or **public-callback receipt**, never a method-name spy, never
`love.state.user_input`/`.C` as behaviour. EXCLUSIVE = "**exactly one route** received" (never two, never
dropped); BOTH = "widget **and** route both received" (order **not** asserted). Carry each P-row's
supersession tag in a comment where it has one (P8/m4, P9/m8, P10/m6).

### 3. Bucket B — IMPLEMENT (carried `pending`)
For each forward contract write `pending('<behaviour>', function() … end)` whose body documents the
target assertion **on the public API** (`compy.input.on_key_pressed = fn`, `ProjectInputController`
receipt, etc.), with a greppable marker `-- DEFERRED (0.1.0-mN): <behaviour> — greened by <m4|m5a>.` at
the right milestone (m4: I1–I4; m5a: I5–I6; m5b/optional: I7–I8). Per **D-α** assert the uniform
`(k, keys_pressed, isrepeat)` triple reaches the **sink**, not only the callback. Implement **none** of
this in `src/`.

### 4. Bucket C — MECHANISM-GUARD (green now, labelled)
Singleton identity + no-realloc (NFR-1). **Label the describe block honestly** as mechanism/NFR guards,
not behaviour contracts (a comment stating so).

### 5. Bucket D — CHARACTERIZE-PROVISIONAL (factual only)
inspect (§3.4) and wheel (§3.7). Assert **only the verifiable current behaviour**; tag the block
`provisional — expected to change, no stakeholder mandate`. Any "how it should become" is a **comment**,
never an assertion. A deliberate future change here must read as expected, not regression.

### 6. M6/M7 stub
One `pending` block naming the not-yet-authored forward contracts (submit/cancel chains,
`on_limit_reached`, `configure`/`set_text`/cursor, R6 force-vs-`configure`), pointing at
`notes/input-contracts.md` §5 scope note. Do **not** flesh it out (scope fence: m4/m5).

### 7. Vocabulary + naming
Test/`describe` names use `route` / `sink` / `widget` — **no** "base sink", no overlay-gate vocabulary,
**no milestone/sprint ids in any test description** (C3; name by behaviour). Layout per the spec (soft:
split per orthogonal behaviour if at all, not per bucket; a grouping subdirectory under `tests/input/` is
**permitted** where it aids comprehensibility — that space already holds ~10 mixed-altitude specs).

### 8. Prove teeth, then verify
- **Teeth:** for at least **one EXCLUSIVE row and one BOTH row**, **temporarily** perturb the relevant
  production routing *in your working tree only* (do not commit), confirm the test **reds**, restore
  **exactly**. Record each perturb→red→restore.
- **Run** `busted tests` (or `just ut_all`): full suite green; Bucket B + the M6/M7 stub show as
  **pending**, not failed.
- **Commit locally** — Conventional Commits (`test:` / `chore(test):`). The pre-commit hook runs the
  suite, so it must be green (pendings keep it so). No perturbation in any commit.

## Record the outcome (required, before you wait)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M4-0-03-contract-suite.md`**:
- **Commit(s)** — hash + subject. **Files changed** — exact paths (all under `tests/`), incl. any rename.
- **Bucket-by-bucket disposition** — which old-spec rows re-homed where; what was dropped (and why it's
  covered elsewhere); the fixture extraction (what moved out of the specs).
- **Mechanism→outcome rewrites** — where you replaced `record_calls`/internals assertions, and with what.
- **Verification** — real `busted` counts (green + pendings); the perturb→red→restore records.
- **Surfaced gaps** — any contract the public surface can't express today (note it as a `pending` with
  the reason), anything the spec didn't anticipate, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- **Test files only** in the committed diff (the re-authored spec(s), `input_session.lua` + fixture, the
  outcome ledger). **No `src/` change.** If you must touch `src/` to make a PRESERVE test pass, **stop**
  and record it — it means the behaviour isn't observable black-box, which is itself a finding.
- Do **not** implement Bucket-B behaviour; do **not** change `tests/helpers/editor_session.lua` or the
  `tests/mock.lua` emitters (use them).
- **Never edit `doc/development/wip/77-new-input-api/design/`** or `notes/input-contracts.md` — they are
  the input you read. If the contract record can't be expressed as a test, record the gap; don't edit it.
