# Tooling — M0: dev/test Docker image (just, lua, luarocks, busted, love2d)

_Generated by LLM(Claude Opus 4.8): 2026-06-16 (session 05); Approved by human?: NOT YET;_
_Black-box prompt. Self-contained. All paths relative to the repo root (= your cwd)._
_M0 is a **tooling prerequisite**, not a roadmap milestone — it has no `design/spec` slice. It builds
the environment the code milestones (M1+) run in._

## You are

A one-shot agent working in the **compy** LÖVE2D repo (root = your cwd). Produce a **Dockerfile**
(plus any minimal supporting files) that gives a working **development + unit-test environment**, then
commit it, record the outcome, and **wait for the human to approve** (do not blindly exit — they may
contest and ask for fixes).

## Goal

The **agentic dev image** for this implementation workflow — the environment the black-box
implementation agent (M1+) runs *inside*. With the repo mounted/copied in, it must let:

- `busted tests` run the unit suite green,
- `love src` launch the app (LÖVE2D present), and
- **Claude Code (the agent itself) run on board** — so the implementation agent operates from within
  the image.

The image is an **add-on to the current base environment** — extend the base this container already
uses (`FROM` it or a matching base), installing only what's missing. **Do not** rebuild a foreign
stack from scratch; layer the toolchain on top.

> **This is NOT a dockerization of the canonical project.** The upstream project does not need a
> container or Claude on board — those are redundant for it. This image exists only to run *this
> agentic implementation workflow*. So it lives **under the topic's `implementation/` area**, never at
> the repo root, and it must not alter the canonical project's build/CI.

## Read first (authoritative sources — do not guess versions)

1. **`DEVELOPMENT.md` → "Installing"** — the canonical setup recipe. It specifies the runtime is
   **Lua 5.1 / LuaJIT** (what LÖVE embeds) and the luarocks dependencies installed `--local`
   `--lua-version 5.1`: **`busted`, `luautf8`, `luafilesystem`**. Treat this list as authoritative;
   re-read it in case it has grown.
2. **`.github/workflows/package.yml`** — CI. Use it to pin the **LÖVE2D version** (and any other tool
   versions) to whatever CI already uses, so the image matches CI. If CI doesn't pin LÖVE, check
   `src/conf.lua` (`t.version`) and `DEVELOPMENT.md`'s LÖVE link, and pick the matching stable release.
3. **`justfile`** — the task runner (`just`). Tests: `just ut_all` → `busted tests`. App: `just dev`
   uses `nodemon` + `love` (watch mode is a dev convenience; the image doesn't need `nodemon` to run
   `busted tests` or `love src`).

## Before installing — check what's already there (the vendoring question)

The luarocks deps are installed **`--local`** per `DEVELOPMENT.md` (i.e. into the user rocktree
`~/.luarocks`, **not** vendored in the project root like `node_modules`). Confirm the actual state of
**this** environment before adding anything:

- `luarocks list` (and `luarocks --local list`) — is `busted`/`luautf8`/`luafilesystem` already present?
- Is there a project-root rocktree (`lua_modules/`, `.luarocks/`, `*.rockspec`)? *(At authoring time
  there was none — but verify; don't reinstall what's already available.)*
- Are `lua`/`luajit`, `luarocks`, `love`, `just` already on `PATH`?

Install only the **missing** pieces. Record what was already present vs. what you added.

## Build the Dockerfile

- **Lua/LuaJIT 5.1** + **luarocks**, then `luarocks install busted luautf8 luafilesystem`
  (match the `--lua-version 5.1` flavour from `DEVELOPMENT.md`; a system/global install in-image is
  fine — `--local` matters on a shared host, less so in a single-purpose image, but keep `busted` on
  `PATH`).
- **love2d** at the version CI uses (headless app-run may need a virtual display, e.g. `xvfb`; note
  this — but the **unit suite does not need a real LÖVE**: tests use `mock_love` (`tests/mock.lua`),
  so `busted tests` needs only Lua 5.1 + the three rocks, not love2d/X).
- **just**.
- **Claude Code (the agent)** — install the CLI so the implementation agent runs on board. Node is
  already in the stack (the project uses npm/nodemon), so the npm route is natural:
  `npm install -g @anthropic-ai/claude-code` — but confirm the current official install method and
  pin sensibly. (No API key is baked into the image; that's injected at run time.)
- **Location:** put the Dockerfile and any supporting files **under the topic's implementation area**,
  e.g. `doc/development/wip/77-new-input-api/implementation/docker/Dockerfile`. **Not** at the repo
  root, and **not** wired into the canonical project's CI. Keep it small and layer-cached sensibly.

## Verify

- Build the image and **run `busted tests` inside it** — capture the real pass/fail output.
- Confirm `love --version` (and, if feasible headless, that `love src` starts) works.
- If you cannot build/run here, say so plainly in the outcome — do **not** claim a green build you
  didn't produce.

## Commit & record

- **Commit locally** using the repo's existing committer config (don't change identity); commit
  conventions are in `agents/rules.md` (repo-root `CLAUDE.md` auto-loads it) — this is tooling, so
  **`chore:`** (e.g. `chore: add agentic dev image`).
- Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M0.md`**: the Dockerfile path,
  what was already present vs. installed, pinned versions (LÖVE, Lua, busted), the build/test result,
  and any caveats (e.g. headless display for `love src`).

## Then — wait for approval

Present a short summary and **stop for human review**; revise on contest; done only when approved.

## Boundaries

- **All M0 artifacts live under `doc/development/wip/77-new-input-api/implementation/`** (e.g.
  `implementation/docker/`). Do **not** place a Dockerfile at the repo root, and do **not** modify the
  canonical project's build or `.github/workflows/` (read CI for versions only).
- Tooling only — **do not** touch `src/` or `tests/` source.
- **Never edit `doc/development/wip/77-new-input-api/design/`.**
