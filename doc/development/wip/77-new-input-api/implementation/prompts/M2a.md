# Implement — M2a: M1 follow-up hygiene

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 07); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: Claude Sonnet, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent working in the **compy** LÖVE2D codebase (this repo, root = your
cwd). Read the spec, implement it, test it, commit it, record the outcome — then **present a summary
and wait for the human to approve**. Do **not** blindly end the session.

This is a **zero-behaviour-change** cleanup: dead test boilerplate removed, plus one duplicated
definition collapsed to a single source of truth. The suite must be **byte-identical** before and
after (685 successes). It is **independent of M2** — these files do not overlap with M2's.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M2a.md`** — the milestone spec slice.
   **Authoritative** for the two cleanups and the Acceptance criteria.
2. **`doc/development/wip/77-new-input-api/implementation/reviews/M1-01.md`** — the M1 review that
   surfaced both items (Waste #1 the modifier duplication, Waste #3 the dead profiler stub). It
   includes the verification that the stub is unnecessary (685/685 without it) and explains why the
   *other* two boilerplate pieces must stay.
3. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (style, "no C accent" /
   data-driven preference, commit conventions). The repo-root `CLAUDE.md` auto-loads it; follow it.
4. **The live code you will touch:**
   - `tests/input/keys_pressed_spec.lua` — carries the `package.preload['controller.profiler']`
     stub block (+ its comment).
   - `src/controller/controller.lua` — `combo_string` (~L143) driven by a local `COMBO_MODS` literal
     (~L136) that re-declares the l/r modifier pairs.
   - `src/util/key.lua` — already owns the `{l*, r*}` pairs as `ctrl_k` / `alt_k` / `shift_k` (~L5),
     module-local, with **no `gui` pair** today.

## Do

1. **Drop the dead profiler stub (M2a §1).** Remove the
   `package.preload['controller.profiler']` block **and its comment** from
   `tests/input/keys_pressed_spec.lua`. The M1 review verified it is unnecessary
   (`lib.profile` has no load-time LÖVE dependency; the profiler runtime path is gated behind
   `if love.PROFILE`, set `false` in the test). **Leave the other two boilerplate pieces in place** —
   they are load-bearing (`view.view` calls `gfx.newFont()` at load; `love.handlers = {}` is consumed
   by `setup_callback_handlers`).
2. **Single source of truth for the l/r fold (M2a §2).**
   - In `src/util/key.lua`: **add the `gui` pair** (`lgui` / `rgui` / `gui`) and **export** the
     modifier groupings as `(left, right, generic)` triples, **in precedence order**
     ctrl → alt → shift → gui.
   - In `src/controller/controller.lua`: have `combo_string` consume the **exported triples** and
     **remove the local `COMBO_MODS` literal**. Fold precedence and output must be **unchanged** —
     `ctrl`, `alt`, `shift`, `gui`, then the triggering key.
3. **Scope discipline.** Zero behaviour change. Do **not** alter `combo_string`'s output, the fold
   semantics, or anything outside the three listed files. This slice does **not** touch M2's
   singleton work — leave `main.lua` / `consoleController.lua` / `userInputController.lua` alone.
4. **Run the suite** — `busted tests` (or `just ut_all`) **before and after**. Every existing M1
   `combo_string` test (precedence, l/r folding, bare key, multi-modifier ordering) must still pass
   **unchanged**, and the total must still report **685**.
5. **Commit locally.** Existing committer config (do not change git identity); follow `agents/rules.md`
   commit conventions — this is a `refactor` (the test cleanup may fold into the same commit or a
   `test:` commit). The `pre-commit` hook runs the suite; if not installed, run `busted tests` first.

## Record the outcome (required, before you wait for approval)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M2a.md`** (clone `outcomes/M1.md`):

- **Commits** — hash(es) + subject line(s).
- **Files changed** — exact paths (expect exactly the three above).
- **Verification** — `busted tests` real result before & after; confirm the 685 count held and the
  `combo_string` tests passed unchanged.
- **Surfaced gaps** — anything unexpected, or "none".

## Then — wait for approval

Present a short summary and **stop for human review**. Revise on contest; done only when the human
explicitly approves.

## Boundaries

- Stay within M2a's three files (+ the outcome ledger). If you must touch more, **stop** and record
  it under "Surfaced gaps" rather than expanding scope.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — it is a frozen input.
