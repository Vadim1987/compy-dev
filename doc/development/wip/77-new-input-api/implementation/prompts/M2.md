# Implement — M2: `UserInputController` singleton extraction

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 07); Approved by human?: NOT YET;_
_Black-box implementation prompt. Self-contained. All paths are relative to the repo root (= your cwd)._
_Target executor: Claude Sonnet, inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot implementation agent working in the **compy** LÖVE2D codebase (this repo, root = your
cwd). Read the spec, implement it, test it, commit it, record the outcome — then **present a summary
and wait for the human to approve**. Do **not** blindly end the session: the human may contest your
work and ask for fixes; the job is done only when they approve.

This milestone is **structural** (it moves where a widget is constructed and adds a lifecycle
surface), not purely additive like M1. Read the real code carefully **before** changing it.

## Read first

1. **`doc/development/wip/77-new-input-api/design/spec/M2.md`** — the milestone spec slice.
   **Authoritative** for what M2 must deliver: the singleton-lifecycle contract, the
   `show`/`hide` semantics (incl. `show()`-while-active no-op and `show({force=true})`
   reconfigure), and the Acceptance criteria.
2. **`doc/development/wip/77-new-input-api/design/spec.md` §2 and §7** — the full cross-cutting
   contract for `show`/`hide` (§2) and the show-while-active policy (§7); context, the slice is the
   binding subset.
3. **`doc/development/wip/77-new-input-api/design/roadmap.md`** → milestone **M2** — description,
   output, and **Risk** ("preserve `love.state.user_input` set/clear; run all tests before & after").
4. **`agents/rules.md`** — the repo's compiled, authoritative ruleset (code hard-limits + style,
   design principles, commit conventions, doc index). The repo-root `CLAUDE.md` auto-loads it; follow
   it. It supersedes any convention restatement — don't look for rules elsewhere first.
5. **Test harness** (not covered by `rules.md`) — `doc/development/tests.md`: the `mock` /
   `mock.keystroke` helpers, `EditorSession`, and the existing input specs under `tests/input/`.

## The live code — design file list vs. reality (read this carefully)

The spec's "Files" list is **idealised**; the real tree diverges (the same way M1's design said
`src/controller.lua` but the real path was `src/controller/controller.lua`). Map it before you start:

- `src/main.lua` — **real**; the startup file. This is where the singleton is created once.
- design `src/consoleController.lua` → **real `src/controller/consoleController.lua`**. This is where
  per-call construction lives today, in the `input()` closure (~L562–586): it builds
  `UserInputModel` → `UserInputController(ui_model, input_ref, true)` → `UserInputView`, then sets
  `love.state.user_input = { M = ui_model, C = ui_con, V = view }`, guarded by an
  `if love.state.user_input then return end` ("there can be only one"). Teardown sets
  `love.state.user_input = nil` in `ConsoleController:stop_project_run` (~L864). The
  `write_to_input` / `user_input` / `input_code` / `input_text` helpers in the same region read the
  same `ui_model` / `ui_con` upvalues — they must keep working against the singleton.
- design `src/userInputController.lua` → **real `src/controller/userInputController.lua`**. Add the
  `show()` / `hide()` state-change methods here.
- design `src/compy_namespace.lua` (or equivalent) — **there is no such file.** The compy namespace
  is assembled by `get_compy_namespace(terminal)` in `src/controller/consoleController.lua` (~L331),
  returned and assigned to `prepared.compy` / `project_env.compy` (~L461, ~L628). Create the
  `compy.input` table **there** (once, at namespace setup) and mount `show`/`hide` on it. "or
  equivalent" in the spec authorises exactly this: put it where the namespace is actually built.

Read each whole file (or the relevant region) before editing — especially the `input()` closure and
its sibling helpers in `consoleController.lua`, so the singleton rewire preserves every existing
entry point.

## Do

1. **Implement exactly the M2 slice's Contract + Acceptance — nothing more.**
   - **Singleton, once.** `UserInputController` is instantiated **once at startup (lazily)** — no
     per-`input()`-call construction, no per-session allocation. Wire `src/main.lua` to create it and
     `consoleController.lua` to use it.
   - **`show(config)`** — activates the singleton; sets `love.state.user_input` to the instance.
     Wire the `show`/`hide` surface only; do **not** implement the full M7 config field-set
     (`configure`/`clear`/cursor/text live-edit are M7). `show()` **while already active → no-op by
     default** (D-2): nothing happens, active session untouched, no error, no cancel chain.
     `show({force = true})` reconfigures in place (replace content iff `text` given, else preserve),
     still no cancel chain.
   - **`hide()`** — deactivates **without** firing the cancel chain; content preserved;
     `love.state.user_input` set to `nil`.
   - **`compy.input` table** created once at namespace setup; `show` / `hide` mounted on it.
   - **`oneshot` stays.** Do **not** remove the `oneshot` flag — it keeps driving submit through
     M2–M5 (removed only in M6).
2. **Behaviour preservation is the headline risk.** Existing examples must still work (legacy globals
   untouched until M8) and `love.state.user_input` must be set on show / cleared on hide exactly as
   the current code does. Run the full suite **before** you start and **after** you finish; nothing
   that passed may regress.
3. **Scope discipline.** Touch only M2's surface. Do **not**: implement M4/M5 dispatch or
   `ProjectInputController`; remove the overlay gate; implement M7 config/clear/cursor; remove
   `oneshot`; migrate legacy globals (M8). **Do not touch `controller/controller.lua` or
   `util/key.lua`** — the `combo_string` / modifier-fold cleanup is **M2a's** job, a separate slice;
   leave those files alone.
4. **Tests.** Add/extend busted tests under `tests/input/` covering Acceptance: singleton created
   once (not re-created per call); `show()` on an active singleton is a no-op; `show({force=true})`
   reconfigures in place; neither fires a cancel chain; `love.state.user_input` points to the
   instance after `show()` and is `nil` after `hide()`. Follow the patterns in the neighbouring specs
   and the `mock` harness.
5. **Run the suite** — `busted tests` (or `just ut_all`). All existing tests must still pass and your
   new tests must pass. Fix until green.
6. **Docs.** M2 introduces a consumer-facing surface (`compy.input.show`/`hide`). Update
   `doc/development/` where warranted (e.g. `internals/user_input.md`, which M1 already touched, or
   `keyboard.md`). If you judge none needed, say "none needed" in the outcome and why.
7. **Commit locally.** Use the repo's **existing committer config** (do not change git identity);
   follow the commit conventions in `agents/rules.md` — M2 is a `feat` (a `refactor` + `feat` split
   is acceptable if it reads cleaner). The `pre-commit` hook runs the suite; if it isn't installed,
   run `busted tests` yourself first.

## Record the outcome (required, before you wait for approval)

Fill **`doc/development/wip/77-new-input-api/implementation/outcomes/M2.md`** (clone the shape of
`outcomes/M1.md`):

- **Commits** — the commit hash(es) and subject line(s) you made.
- **Files changed** — exact paths.
- **Verification** — the command run (`busted tests`) and the **real result** (before/after pass
  counts; the new test names). Do not fabricate — report what actually ran.
- **Surfaced gaps** — anything the spec didn't anticipate, any path/contract mismatch beyond the ones
  already mapped above, or "none".

## Then — wait for approval

Present a short summary of what you did and **stop for human review**. If the human contests
anything, fix it (amend or add commits) and update the outcome ledger. Consider the job complete
only once the human explicitly approves.

## Boundaries & escalation

- Stay within M2's file list: `src/main.lua`, `src/controller/consoleController.lua`,
  `src/controller/userInputController.lua`, the namespace assembly in `consoleController.lua`, and
  new/extended tests in `tests/input/` (+ docs if warranted). If you find you **must** touch more to
  make M2 work, **stop** and record it under "Surfaced gaps" rather than expanding scope.
- **If the real wiring genuinely fights the spec** — i.e. preserving `love.state.user_input`
  semantics while hoisting construction to a startup singleton turns out to demand real design
  judgement, not mechanical rewiring — **do not force it**. Stop, write what you found and the
  options under "Surfaced gaps", and flag it as a **candidate escalation** (M2 may need to be
  promoted to a managed subtopic). The black box implements a frozen spec; it does not redesign.
- **Never edit `doc/development/wip/77-new-input-api/design/`** — it is a frozen input.
