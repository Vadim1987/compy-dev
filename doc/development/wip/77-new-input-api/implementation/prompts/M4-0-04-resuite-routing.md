# Implement — M4-0-04: re-derive the drifted routing rows from the corrected contract record

_Generated by LLM(Claude Opus 4.8): 2026-06-29 (session 27); Approved by human?: NOT YET._
_**GATE: do NOT launch until the prompt12 correction is owner-blessed and
prompt13-reviewed.** This slice consumes the corrected record/spec as its source._
_**Target executor: Claude Opus.** Judgment-heavy re-derivation, not transcription._
_Runs inside the **M0** dev image (lua, luarocks, busted, love2d, just available)._

## You are

A one-shot execution agent in the **compy** LÖVE2D codebase (root = your cwd). The input
contract record and the bucket spec were just **corrected** to remove a routing drift:
keyboard routing was keyed on **widget presence** and pointer routing was tabulated as
inter-route **BOTH** — both were today's implementation mistaken for the invariant. The
existing suite `tests/input/input_routing_spec.lua` still encodes the **old (drifted)**
framing in specific rows. **Re-derive only those rows from the corrected source, keeping
all the sound scaffolding.** This is a focused revision of the M4-0-03 suite, not a
rebuild.

This is a **test-only slice — you change NO production code** (nothing under `src/`).

## Read first

1. **`doc/development/wip/77-new-input-api/notes/input-contracts.md`** (CORRECTED) —
   source of truth. §2 invariant, §3/§4 routing, the per-row provenance.
2. **`doc/development/wip/77-new-input-api/design/spec/M4-0-03-contract-suite.md`**
   (CORRECTED) — bucket definitions + the corrected meaning of EXCLUSIVE / BOTH.
3. `doc/development/wip/77-new-input-api/notes/input-contracts-correction.md` +
   `doc/development/wip/77-new-input-api/implementation/reviews/input-contracts-correction.md`
   — what changed and why.
4. **`doc/development/wip/77-new-input-api/implementation/prompts/M4-0-03-contract-suite.md`**
   — the **authoring discipline** (four buckets, teeth, vocabulary, pending-forward,
   observable-outcome-not-spy). You follow it unchanged.
5. `agents/rules.md` — the authoritative ruleset.

## The rows to re-derive (changed by the correction)

- **P1/P2** keyboard/text "reaches only the widget when one is up" → re-found on the
  **active route by mode/context**, or demote to CHARACTERIZE-PROVISIONAL per the
  corrected bucket.
- **P3** keyreleased "does not reach the route under a widget" → re-derive per the
  corrected framing (a release reaches the active route; provisional if the consumer is
  undecided).
- **P4** mouse "reaches BOTH route and widget" → *"pointer reaches the active route;
  intra-route forwarding to a widget is the route's concern."* Re-do its teeth
  accordingly.
- **CP2** wheel "route not widget" → active-route framing.

**EXCLUSIVE teeth are now** "exactly one route received, for every event type." The only
"both" is **intra-route** (a route + a surface it activated) — if asserted at all it
belongs in a route-internal test, never in the dispatch contract.

## Discipline (unchanged from M4-0-03)

Test-only; no `src/` change; keep the fixture / four buckets / teeth / pending-forward;
**observable-outcome or public-callback** assertions, no method-name spy, no
`love.state.user_input` as behaviour; **no milestone ids in test descriptions**;
vocabulary `route` / `sink` / `widget`. **Prove teeth** (perturb → red → restore) for at
least one corrected EXCLUSIVE row and the corrected **pointer** row. Run `busted tests`
(or `just ut_all`) green (pendings stay pending). Commit locally (`test:`). Record the
outcome to `implementation/outcomes/M4-0-04-resuite-routing.md` (rows re-derived,
mechanism→outcome rewrites, real busted counts, perturb→red→restore records, surfaced
gaps). Then **stop for human approval**; review via the standard `agents/review.md`
launcher.

## Boundaries

- **Test files + the outcome ledger only** in the committed diff. **No `src/` change.**
  If a corrected contract can't be expressed black-box, **stop** and record the gap.
- **Never edit `design/`** or `notes/input-contracts.md` — now corrected and blessed,
  you **consume** them. Do not re-touch rows the correction left unchanged.
