# Review — M4-0-03: contract-shaped input suite (re-authored from the record)

_Reviewer: Claude Opus 4.8 (independent) — 2026-06-29. Commit reviewed: `41a2c46`_
_`test: re-author input suite from the contract record`._

## Verdict

**Approve.**

The suite is genuinely re-authored top-down from the contract record into the four
obligation buckets, drives behaviour through the public entry / `love.handlers.*`,
and asserts observable outcomes (not the old method-name spy). I re-ran the suite and
**independently re-verified the teeth** (perturb → red → restore) on one EXCLUSIVE and
one BOTH row. No `src/` change. Spec compliance is full.

**Approval-scope note.** This is a **test-only** slice; the verdict establishes that the
*existing* behaviour is now guarded at the contract level and that the forward contracts
are correctly parked `pending`. It does **not** establish any M4/M5 behaviour — the
seven IMPLEMENT rows are documentation-shaped placeholders whose bodies reference an
as-yet-unwritten API (`ProjectInputController.on_event`, `active_keyboard_route`,
`provision_legacy`, `compy.input.on_key_pressed/handlers`); those names will be reconciled
when M4/M5 green them. Runtime was **not** exercised (the suite runs on mock_love, by
design); the verdict rests on the suite + diff, which is the correct bar for this slice.

## Spec compliance (per acceptance item)

| Item | Status | Evidence |
|---|---|---|
| Suite re-authored into the four buckets, canonical vocabulary, citing the record | ✔ | `input_routing_spec.lua` — bucket banners at `:46/:422/:458/:488`; §-cites throughout |
| **Bucket A fully present + green**, public entry, observable outcomes, supersession tags | ✔ | P1–P11 all present; P8/m4 `:286`, P9/m8 `:303`, P10/m6 `:337`. P5 carried `pending` (justified gap, below) |
| **Bucket B** `pending` with `DEFERRED (0.1.0-mN)` at the right milestone | ✔ | I1–I4 m4 (`:497/:510/:519/:534`), I5 m5a (`:549`), I6 m5 (`:560`), I7 m5b (`:571`); I8 noted optional/already-green |
| D-α: uniform `(k, keys_pressed, isrepeat)` triple, **sink included** | ⚠ | Stated in I4/I6 comments; bodies probe the widget / the callback-that-defaults-to-the-sink, not the sink directly. Acceptable as `pending` documentation; M5 must assert the sink explicitly when greening |
| **Bucket C** present + labelled as mechanism/NFR | ✔ | `describe('mechanism / NFR guards — not behaviour')` `:464`; MG1/MG2 |
| **Bucket D** factual-only, tagged provisional, assumptions as comments | ✔ | `describe('provisional — expected to change, no mandate')` `:429`; CP1 inspect `:436`, CP2 wheel `:452` |
| **M6/M7 stub** — one `pending` pointer | ✔ | `:592` names submit/cancel chains, `on_limit_reached`, `configure/set_text/cursor`, force-vs-configure |
| Teeth: ≥1 EXCLUSIVE + ≥1 BOTH perturb→red→restore | ✔ | **Re-verified independently** (below) |
| Standup in the fixture; no milestone id in any description; no `base sink`/overlay-gate vocab in names | ✔ | `input_fixture.lua` carries the standup; grep of `it/describe/pending` strings finds no milestone/sprint id; no forbidden vocab in names |
| No production `src/` changed | ✔ | commit touches only `tests/**` + the outcome doc |

### Teeth — re-verified by the reviewer (working tree only, never committed)

| Row | Perturbation (`controller.lua`) | Result |
|---|---|---|
| P2 EXCLUSIVE (`:88` "only the widget when one is up") | drop `user_input.C:textinput(t)` (`:663`) | **red** (widget stays empty) |
| P4 BOTH (`:221` "widget and the route both") | drop `user_input.C:mousepressed(…)` (`:692`) | **red** (widget gets no selection) |

`git diff --stat src/` clean after each restore. The outcome's recorded teeth match what I
observed. **Counts re-run independently: 716 successes / 0 failures / 0 errors / 9 pending**
— exactly as claimed (9 = P5 touch + 7 Bucket B + 1 M6/M7 stub).

## Scope fence

✔ Clean. The diff touches only `tests/helpers/input_fixture.lua` (new),
`tests/helpers/input_session.lua` (thickened), `tests/input/input_routing_spec.lua`
(re-authored), the two removed specs, and the outcome ledger. `tests/helpers/editor_session.lua`
and `tests/mock.lua` are **not** in the commit (consumed unchanged, as required). No reach into
downstream milestones; the forward behaviour is parked `pending`, not implemented.

## Re-homing — faithful, one minor coverage drop

- `singleton_spec` show/hide/force/no-op/warn → **P10** through `compy.input.show/hide`,
  asserted on `get_text`/`is_empty` + the `Log.warn` contract. ✔
- `singleton_spec` identity / no-realloc → **MG1/MG2** (Bucket C, labelled). ✔
- `singleton_spec` `love.state.user_input`/`.C` internals-as-behaviour → **dropped**; identity
  survives only in Bucket C. ✔
- `overlay_spec` reprompt-empty / real-submit (C2T-1) → **P10** fresh + oneshot rows. ✔;
  drawable-handle (`user_input.V`) mechanism row → **dropped** (not an input-routing contract). ✔
- **Minor (logged, accepted):** `singleton_spec`'s *"force does not warn"* (C2 boundary —
  the sanctioned override must stay silent) is **not** re-homed. It is the inverse of the kept
  non-force warn row and low-risk; logged in the interim ledger as **accepted**.

## Residual coverage / observations (non-blocking)

- **Mechanism crutch in a Bucket-A row.** P8 (`:293`) asserts
  `Controller._defaults.keypressed == love.keypressed` alongside the observable backspace. The
  identity compare is mildly mechanism-flavoured; the observable backspace (`:296`) is the real
  contract assertion and carries the teeth, so this is cosmetic, not a violation of the "outcome,
  not mechanism" discipline.
- **CP2 wheel (`:453`)** asserts the *absence* of a `wheelmoved` gate entry — a characterization of
  current mechanism. This is correct for Bucket D ("factual today, expected to change") and is
  honestly labelled, so it is in-scope rather than a stray mechanism assertion.
- **P4 widget-half instrument.** The production singleton disables selection (its pointer handler is
  a no-op, §3.6), so the test uses a selection-enabled widget instance (`F.show_selectable_widget`)
  to witness delivery. Disclosed in the fixture comment and the outcome; a reader will not mistake it
  for production config. Sound.

## Rules check

- **Conventional commit** `test: …`, committer identity unchanged, `Co-Authored-By` present. ✔
- **Function bodies / params / nesting** within limits in the new helpers and fixture. ✔
- **Line length (≤64): ⚠ minor.** ~90 lines exceed 64 display columns in the re-authored spec
  (mostly heavy contract-citation **comments** at 65–70; ~12 are code/description lines at 65–69).
  Pervasive but cosmetic, and the density is comparable to existing input specs
  (`keys_pressed_spec` ~16%). Not a blocker; worth a light tidy if the file is touched again.
- **Vocabulary.** One `-- … overlay gate (§5.1)` appears in an *I1 forward-contract comment*
  explaining the mechanism being removed — permitted (the prohibition is on durable vocab in test
  *names*, and the record itself uses the term). ✔

## Tech-debt + docs

- **Logged to `implementation/technical_debt.md`** (dispatch map + detail blocks):
  1. **P5 touch BOTH not black-box expressible** — *anticipated*; greens when a touch consumer lands
     (the implementer surfaced it but had not logged it).
  2. **"force does not warn" (C2) coverage dropped** — *accepted*; inverse of a kept contract.
- The pre-existing **`tests.md` not updated for new emitters — open** row still applies: this slice
  added a new fixture + emitters (`mousereleased`, `touchpressed`) and `tests.md` does not yet
  describe them. No **new** stale doc reference was introduced (persistent `doc/development/tests.md`
  does not enumerate the deleted spec files; remaining `singleton_spec`/`overlay_spec` mentions are
  in historical M2-era wip artifacts, correctly left as-is).
- **Doc-update verdict:** acceptable for a test-only slice — no observable behaviour / interface /
  internal contract changed; the only doc gap (`tests.md` emitters) was already on the ledger.

## Acceptance checklist (spec)

- [x] Re-authored into four buckets, canonical vocabulary, cites `input-contracts.md`.
- [x] Bucket A fully present + green via public entry, observable outcomes; supersession tags present.
- [x] Bucket B `pending` with correct `DEFERRED (0.1.0-mN)` markers; suite green; pre-commit passes.
- [x] Bucket C present + labelled mechanism/NFR.
- [x] Bucket D factual-only, tagged provisional; assumptions are comments.
- [x] M6/M7 stub present as a single `pending`.
- [x] Teeth shown (one EXCLUSIVE + one BOTH) — re-verified by the reviewer.
- [x] Standup in the fixture; no milestone id in descriptions; no `base sink`/overlay-gate names.
- [x] No `src/` changed.
- [~] D-α "sink included" — documented in `pending` comments; the explicit sink assertion lands with
  M4/M5 (flagged for the implementor, not a defect of this slice).

## Recommendation to the orchestration plane

**Approve** and resume the TDD run order: M4 greens I1–I4, M5a greens I5–I6. When greening the
isrepeat/callback rows, make D-α explicit — assert the `(k, keys_pressed, isrepeat)` triple reaches
the **sink**, not only the widget/callback. Pick up the two logged debt rows at their named points.
