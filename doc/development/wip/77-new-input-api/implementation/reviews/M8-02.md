# M8-02 review — balloons migration (verify-don't-trust)

**Verdict: APPROVE.**

Independent reviewer, ran the checks myself. The balloons example is migrated
off the legacy `user_input()`/`input_text()` poll-a-reftable idiom onto the
`compy.input.*` continuous-session API cleanly; the headline correctness risk
(configure-during-`on_text_entered` survival) is real-proven both by a genuine
contract row and by tracing the production code; and the human-redirected
detached-repo delivery is exactly the shape the review note requires.

## Suite (verify-don't-trust)

- `busted tests` → **809 / 0 / 0 / 4** (my run). Matches expected 808 (M8-01
  baseline) + 1 new contract row. The four pending are unchanged and are the
  pre-existing routing-gap cells @101/@153/@161/@222 — not this chunk.

## Per-trap findings

**Trap 1 — continuous-session idiom (AC-4): PASS.** `terminal.lua` activates
once (`compy.input.show{ on_text_entered = deliver }`) and re-arms via a
**field-write** `compy.input.after_submit = function() compy.input.show({}) end`
— the corrected M8-01 recipe, not a dropped `show{after_submit=…}` key. The
per-frame poll is gone: `main.lua`'s `hooks.update` no longer calls
`ui_read_input`, and `terminal_read`/`ui_read_input` are deleted (ui.lua alias
block removed). Grep of the whole balloons tree for all six legacy globals →
**zero** hits.

**Trap 2 — configure-during-session survival (headline): PASS, proven not
assumed.** Traced in production code (`src/controller/userInputController.lua`):
- `configure()` → `apply_config` writes `self.model.custom_label = cfg.prompt`
  (line 216) on the persistent singleton model.
- `submit()` (line 380) calls `deliver()` — which fires `on_text_entered`
  (line 369) — **before** `self:hide()` (line 387). So the `configure{prompt}`
  set inside `on_text_entered` runs while the session is still active (AC-25
  observable order).
- `hide()` (line 302) only nils `love.state.user_input`; never touches
  `custom_label`.
- The bare re-arm `show({})` runs `open_fresh` → `apply_config(self, {})`; the
  `if cfg.prompt ~= nil` guard (line 215) is **false** for a bare show, so
  `custom_label` is preserved. `open_fresh`'s `clear_input()` clears text only.
- Net: the configured-while-active prompt survives the hide→reshow intact. No
  design gap, no surface change needed. The ledger's static-trace claim holds
  exactly.

**Trap 3 — `terminal_write` → `configure{prompt}` (not `input_text`/re-show):
PASS.** `terminal_write(msg)` body is now `compy.input.configure({prompt=msg})`
— a live reconfigure, no widget deactivation. Smoke-load produced **zero**
`show()`-while-active warn lines.

**Trap 4 — detached-repo delivery (AC-9 superseded by human redirect): PASS,
correct new shape.**
- THIS repo: `516ba2b` touches only `outcomes/M8-02.md` + 23-line addition to
  `tests/input/input_contracts_spec.lua`. **No balloons file.** PM follow-up
  `1685ca1` is docs-only (ledger addendum + review-note + track). Confirmed.
- `src/examples/balloons` (nested checkout): `56347d0` is on top,
  `## main...origin/main [ahead 1]` = **UNPUSHED**. `git show 56347d0 --stat`
  is exactly the 3-file migration: `main.lua` (the `game_init` hunk only:
  +`terminal_set_handler(input_handler)`, −`ui_read_input(input_handler)`),
  `terminal.lua`, `ui.lua`. The pre-existing `-- test`/`print("ok")` cruft is
  **NOT** in the commit — it remains an unstaged working-tree mod
  (`git diff main.lua` = the L108+ cruft block, and `git status -sb` shows
  ` M main.lua`). This is precisely the human-authorized shape (a clean
  unpushed migration commit, cruft excluded). Net still no-push.
- This redirect is a legitimate human reversal of a frozen slice — correctly
  **not** flagged as an AC-9 breach.

**Trap 5 — AC-8 edge cases: PASS (as scoped).** balloons sets no validator and
never issues a mid-session `show{force}`, so it doesn't newly exercise
stop/show-while-active/validator-lock beyond the existing `#m5c`/`#m7`/`#m8`
fixture rows. The ledger points at that coverage honestly; the migration
introduces no re-show-while-active that would trip the re-entry guard
(confirmed by the clean smoke-load).

**Trap 6 — natives untouched, suite green: PASS.** No in-repo example or native
edited; `/repo` `git status` shows only the one test file among tracked changes.
M8-01 files do not reappear.

**Trap 7 — rules limits: PASS.** New `terminal.lua` closures are tidy
(`deliver` 2 lines, `terminal_set_handler` 1 line, `after_submit` closure 1
line) — all within line ≤64 / body ≤14 / params ≤4 / nesting ≤4. The unused
`flushed` param on `terminal_write` and the line-49 dead second arg are
pre-existing; correctly reported-don't-fixed in the ledger.

**Trap 8 — scope fence: PASS.** All six legacy globals **still present** in
`src/controller/consoleController.lua` (project_env `user_input`/`input_code`/
`input_text`/`write_to_input`/`validated_input`/`astv_input` + the
`compy_namespace.text_input` dead write) — removal is M8-03. No
`src/controller/*`, `src/model/*`, or `evaluator.lua` change (git confirms).

## The new contract row (test-quality gate)

`tests/input/input_contracts_spec.lua` (in `#input`, ~L2091): activates a real
project (`F.activate_project`), sets `after_submit` to bare re-show, shows with
`prompt='first'` and an `on_text_entered` that `configure{prompt='live'}`,
then `F.session.type('a')` + `F.session.press('return')`, and asserts
`F.singleton.model:get_label() == 'live'`. This drives real production
show/configure/submit/reshow machinery and asserts on real model state — **not**
an empty/mock-only test. The red→green story is credible: the assertion
discriminates `'live'` (survived) from `'first'` (show-time prompt) — a
tautological or stale-model test would read `'first'`. Genuine.

## Headless smoke-load (honest ceiling)

`xvfb-run -a love src play src/examples/balloons` → **traceback-free**: log is
only expected ALSA "no audio device" noise, `Project play opened`, and the
pre-existing cruft's `ok/ok/ku` prints (love runs the on-disk working tree,
cruft included — harmless). **No** `show()`-while-active warn. As the ledger
states correctly, no keystroke injection is possible in-container, so real
compose/submit/re-prompt remains a **human hand-play gate** — balloons joins
tixy/repl/guess/valid on that open list. No overclaim.

## Notes (non-blocking)

- The ledger **body**'s "Verification of the untouched-`.git`" block (HEAD
  `9e7a1e1`, "up to date with origin/main") describes the implementor's
  *original* uncommitted delivery and is now stale — but the **PM ADDENDUM**
  explicitly supersedes it with the `56347d0` unpushed-commit shape. Coherent;
  the addendum is loud and surprise-first. Not a defect.
- The running example still carries the `-- test`/`print` cruft on disk (it's
  an unstaged working-tree mod, deliberately excluded from the commit) — this
  is the human's upstream state, correctly left untouched per report-don't-fix.

## Bottom line

Delivery is correct: clean unpushed migration commit `56347d0` in the detached
balloons repo (cruft excluded), THIS-repo commit is ledger + one real contract
row only. The configure-during-`on_text_entered` survival is verified in
production code, not assumed. Suite 809/0/0/4. **APPROVE.**
