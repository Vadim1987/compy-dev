# Review — M4-0-01: M4 front-tests (corrective char-net → routing front-tests)

_Independent review (Claude Opus 4.8): 2026-06-28. Reviewer did **not** write this code._
_Spec: [`../../design/spec/M4-0-01-front-tests.md`](../../design/spec/M4-0-01-front-tests.md)
(supersedes `M4-0-characterization-net.md`)._
_Outcome: [`../outcomes/M4-0-01-front-tests.md`](../outcomes/M4-0-01-front-tests.md)._
_Commits reviewed: `38b8710` (rewrite+rename), `34af48d` (readability follow-up).
Result in `tests/input/input_routing_spec.lua` (312 lines)._

## Verdict

**approve.**

**Approval scope.** This establishes that the char-net was correctly **reduced** to a routing-level
front-test suite: the wrong-target coverage is gone, Group-1 smokes drive the real `love.handlers.*`
gate and assert real controllers received (teeth reviewer-reproduced), Group-2 is carried `pending`
with greppable markers, and no `src/` was touched. It does **not** sign off any M4 behaviour — the
four `pending` rows are the M4 gate; greening them is M4's gate (and M4's prompt/`process.md §9.2`
still need the test-first reconciliation the spec Notes call out, tracked as follow-ups).

## Spec compliance (Acceptance, item by item)

| # | Acceptance item | Verdict | Evidence |
|---|---|---|---|
| 1 | Char-net reduced; example-BC rows, alias trio, reimpl debounce removed; file renamed | ✔ | `characterization_spec.lua` deleted (`38b8710`); `grep tixy\|balloons\|maze tests/input` → none; `grep held_keys tests/` → none; renamed to `input_routing_spec.lua` |
| 2 | Group 1 (+ block-nav) green now; driven through `love.handlers.*`; asserts a **real** controller (no lambda) | ✔ | `input_routing_spec.lua:198-249` drive `session.*`/`mock.textinput` and assert against real `CC.input`/`CC`/`CC.editor`/overlay `c` (built as `main.lua` wires); block-nav `:264-281` |
| 3 | Group 1 has teeth — perturb→red for no-double-delivery + one receive test | ✔ | **Reviewer-reproduced** both (see below); outcome's two records confirmed |
| 4 | Group 2 carried `pending` with `DEFERRED (0.1.0-m4)` markers; suite green; hook passes | ✔ | `:289-310` four `pending` + markers; `busted tests` → 708/0/0/**4 pending** |
| 5 | `input_session.lua` consumed (no inlined copy); `mock.textinput`/`keystroke` opts retained & exercised | ⚠ | Consumed at `:113`,`:130` (dead-copy finding closed). `mock.textinput` exercised (`:204`); `keystroke` via block-nav (`:273`). **`isrepeat`/`scancode` opts not yet exercised** (tracked, anticipated) and helper **not trimmed** — accepted deviation, see Rules/Residual |
| 6 | No `src/` change; no milestone/sprint id in any test description | ✔ | Both commits touch only `tests/` + outcome md; descriptions name behaviour only (`M4`/`D-9` appear only in code comments as the `0.1.0-m4` release marker, per spec) |

### Per Group-1 contract row

- **console, no overlay** → ✔ `keypressed` (`:208-212`, records `CC.keypressed`) **and** `textinput`
  (`:198-206`, observable effect on `CC.input:get_text()`) reach the console.
- **editor mode** → ✔ `textinput` (`:214-219`) and `keypressed` (`:221-226`) reach `CC.editor`.
- **overlay active** → ✔ singleton receives (`:228-233`) **and** native not also fired (`:237-249`).
- **block-nav at-limit** → ✔ kept via `editor_session.lua` (`:264-281`); comment reframed to
  "exercises block navigation INDIRECTLY through the at-limit condition" (`:252-257`).

## Tests re-run (verify, don't trust)

- `busted tests` → **708 successes / 0 failures / 0 errors / 4 pending** — matches the ledger exactly.
  The 4 pendings are the Group-2 rows (`@ 293/299/305/310`).
- **Teeth independently reproduced** (perturb `src/controller/controller.lua` in working tree only,
  observe red, restore — `git status` clean after each):

  | Test | Perturbation | Observed |
  |---|---|---|
  | `active overlay does not also fire native` (`:237`) | overlay `keypressed` branch also calls `love.keypressed` | **red** — `native` = 1, expected 0 |
  | `console text reaches the console input` (`:198`) | drop the no-overlay `textinput` fall-through | **red** — input stayed `''`, expected `Z` |

  Confirms the smokes route through the **real gate** (not a shortcut), and the C-5 perturbation is
  absent from both commits.

## Scope fence

✔ Clean. `38b8710` = outcome md + `characterization_spec.lua` (del) + `input_routing_spec.lua` (new);
`34af48d` = `input_routing_spec.lua` only (comments/rename `make_overlay`→`show_input_overlay`, no
behaviour change). No `src/` reach, no downstream-milestone work, no unrequested tidying. (The
intervening `9715b78 entrypoints updated` touches only `entrypoints.md`, not this slice.)

## Rules check

- **Hard limits:** all lines ≤ 64 (`awk length>64` → none). No inline comments (own-line only). The
  kept block-nav `it` body sits at the 14-line ceiling (comments excluded) — acceptable, it is the
  unchanged regression row, not new code.
- **Commits:** both `test:` Conventional subjects; tests-first slice has no `fix:`. Committer identity
  unchanged (`Hleb Rubanau <g.rubanau@gmail.com>`); `Co-Authored-By: Claude Opus 4.8` present.
- **No C accent:** n/a (idiomatic Lua/busted).

## Residual coverage

- **Helper not trimmed (⚠ minor, accepted).** Spec/prompt said "consume **and trim** `input_session`
  to what the suite needs"; `release`/`repeat_press` have no live consumer. The implementer kept the
  cohesive driver (outcome judgement call: the M4 gate-rewrite converts the `isrepeat`/restoration
  pendings, which `repeat_press`/`release` exist to drive; trim-then-re-add is churn). Reasonable — the
  driver is now genuinely consumed, so the DRY/dead-copy intent of the requirement is met; the
  unfulfilled "trim" is cosmetic. Not a corrective trigger.
- **`isrepeat`/`scancode` opts not exercised yet** — by design (the path drops `isrepeat` today; the
  `isrepeat` `pending` covers it). Tracked **anticipated** in the interim ledger.

## Tech-debt + docs

**Interim ledger updated** (`implementation/technical_debt.md`) to keep tracking honest:

- `M4-0 input_session.lua driver unused` → **closed** (verified consumed; dispatch row + detail block
  struck, with the not-trimmed residual recorded as accepted).
- `M4-0 keyboard-debounce reimplemented in-test` → **closed** (verified deleted; row + detail struck).
- `M4-0 overlay-mechanism collapse + maze Lua path` → mechanism-collapse half now **moot** (example
  rows deleted); maze Lua-command path stays **anticipated → M8**. Detail updated.

**Still open (correctly):**

- `M4-0 mock.keystroke isrepeat/scancode opts unexercised` — anticipated, M4 converts the `isrepeat`
  pending.
- **`M4-0 tests.md not updated for new emitters` — remains OPEN.** `doc/development/tests.md` still
  does not document `mock.textinput` / the `keystroke` opts, now actively exercised by this suite.
  Out of this slice's **test-only file scope** (the spec's Files list excludes docs), so not a
  corrective trigger here — but it is now overdue and should land at M4. Left open in the ledger.

**Doc-update verdict:** acceptable. This slice changes no observable runtime behaviour or interface
shape, so `internals/` needs no change; the only doc gap (`tests.md` emitters) is pre-existing and
already tracked.

## Notes for the orchestration plane

- **Record-accuracy nit (not blocking):** the outcome ledger predates and does **not** mention the
  follow-up readability commit `34af48d` (it cites only the rewrite). The reviewed file reflects that
  commit; the omission is cosmetic. Worth a one-line ledger addendum if the plane wants the record
  complete.
- The spec's own Notes (M4 prompt stale; `process.md §9.2` "black-box" line) are explicit follow-ups,
  not regressions in this take.

## Acceptance checklist

- [x] Char-net reduced to front-tests; example-BC rows / alias trio / reimpl debounce removed; renamed.
- [x] Group 1 (+ block-nav) green against current code; driven through `love.handlers.*`; real
  controllers asserted.
- [x] Group 1 has teeth — perturb→red reproduced by reviewer for no-double-delivery + a receive test.
- [x] Group 2 carried `pending` with `DEFERRED (0.1.0-m4)` markers; suite green (708/0/0/4); hook passes.
- [x] `input_session.lua` consumed (dead-copy finding closed); `mock.textinput`/`keystroke` exercised.
- [ ] `input_session.lua` **trimmed** — not done; accepted as a justified deviation (see Residual).
- [x] No `src/` change; no milestone/sprint id in any test description.
