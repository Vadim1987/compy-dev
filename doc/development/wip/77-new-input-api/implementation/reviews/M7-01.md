# Review — M7-01: cursor/text surface + set_text keep_cursor fix

_Reviewer: independent Opus (`agents/review.md`). Milestone `M7-01`, first
chunk of the M7 carve. Reviewed the committed diff `91e6e99`
(parent `e0664d1`) + the outcome ledger against `design/spec/M7-02-recut.md`
AC-6/7/8/9/10 and `agents/rules.md`. lua-lsp used for diagnostics + the
`set_text` caller enumeration; grep as the completeness backstop._

## Verdict: **approve**

Every AC is met by a real, discriminating test driving the real
`compy.input` surface. The one behaviour-changing edit (the model
`set_text` keep_cursor gate) is byte-equivalent on the falsy path that
every existing caller takes, so it regresses nobody. Scope fence held.
One minor, pre-existing rules note (below) and one cosmetic
inconsistency — neither blocks.

## Verify-don't-trust (re-confirmed myself)

- **Suite:** `busted tests` → **794 successes / 0 failures / 0 errors / 5
  pending** (0.85s). Matches the ledger's claimed `779 → 794`.
- **The 5 pending are unchanged.** The four routing-gap rows (@101/@153/
  @161/@222) and the m7-family anchor
  (`configure/set_text/cursor, force-vs-configure`) all remain pending.
  The anchor line shifted @1681 → @1828 because the new `#m7` block was
  inserted above it — **it is not retired** (M7-02 retires it at AC-12).
  Confirmed by the pending listing text.
- **Diff scope.** `--stat` = `consoleController.lua` (+28),
  `userInputController.lua` (+21/−2), `userInputModel.lua` (+17/−2), the two
  test files, and the outcome ledger. No `design/`, no `technical_debt.md`,
  no routing/dispatch file, no legacy-global touch.
- **lua-lsp `diagnostics`** on both edited src files → clean (no diagnostics).

## Trap-by-trap

1. **Model fix caller-safety — PASS (hardest trap).** Old tail:
   `if not keep_cursor then init_visible end; jump_end()` (jump_end
   *unconditional*). New tail: `if keep_cursor then _clamp_cursor_pos;
   return end; init_visible; jump_end`. For **falsy** keep_cursor the two are
   observably identical (`init_visible` then `jump_end`), so only the
   truthy path — which no prior caller used — changes. Enumerated all
   `:set_text(` sites (grep, the authoritative backstop here since the
   qualified `UserInputModel:set_text` LSP query returns no refs):
   `consoleController.lua:786` (`overlay.C:set_text(content)`),
   `userInputController.lua:219/282` (`cfg.text`),
   `userInputModel.lua:436/448` (history), `editorController.lua:336/602`,
   plus tests — **all pass a single arg** (keep_cursor nil/falsy). The only
   two-arg site is the new widened controller passthrough
   (`userInputController.lua:56`). Behaviour-safe. The ledger's caller
   table is accurate (minor line drift: show force path is `:282`, ledger
   says `:264` — cosmetic).
2. **AC-10 by placement — PASS.** The three callables live in the `methods`
   table (`consoleController.lua` ~L435-459), **not** in `INPUT_CALLBACKS`
   (L357-369, untouched — the new keys are absent). Assignment therefore
   rides the existing `__newindex` raise; the `assigning … raises` row
   asserts all three error. No assignable surface added.
3. **AC-6 hidden → nil, not warn — PASS.** `get_cursor` returns `nil` on
   `not love.state.user_input` with no `Log.warn`; distinct from
   set_cursor/set_text which warn. Correctly single-out per spec §6 (only
   the two mutators carry the no-op+warn clause).
4. **AC-7 clamp lands at boundary — PASS.** Confirmed `move_cursor`
   (`userInputModel.lua:535-538`) falls back to the *previous* column when
   `x` is out of range — it does **not** clamp to the line end. The
   implementor therefore did not route through raw `move_cursor`;
   `set_cursor_pos` computes `min(col, llen+1)` / `min(line, n)` itself and
   hands `move_cursor` an in-range pair. The discriminating test seats the
   cursor at col 2, then `set_cursor(1,999)` and asserts **col 6** (line
   end) — distinguishable from the fallback (col 2). This proves
   clamp-to-boundary, not fallback. AC-7 finding in the ledger is real and
   correctly resolved to the spec wording.
5. **AC-8 view without re-show — PASS.** Controller `set_text` calls
   `model:set_text` then `self:update_view()` (the existing no-reshow path,
   not `show()`). Test asserts the `love.state.user_input` overlay handle is
   **identity-unchanged** after the call (no re-publish) AND that
   `view.render` fired — a real behavioural assertion. keep_cursor preserve
   + shrink-clamp are each covered (cursor 3 survives `set_text('world',
   true)`; cursor 5 → col 3 after `set_text('xy', true)`).
6. **AC-9 real Log.warn — PASS.** Both hidden branches call `Log.warn`; the
   two hidden rows swap in a counter and assert exactly one warning each,
   plus a no-op assertion (`get_cursor()` still nil / `is_empty()`). Real
   warn-channel assertion, not a comment.
7. **Scope fence — PASS.** No `configure`/`clear`; F-5 not struck;
   internals boundary doc not written; @1681 anchor intact; no
   INPUT_CALLBACKS addition; no legacy global or poll idiom touched; no
   routing edit. Files match the expected surface. The DEFERRED comment
   block above `get_compy_input` was correctly left as-is (comment, not the
   boundary doc — M7-02's concern).
8. **Rules limits — one minor pre-existing note (below).** New methods are
   within budget: `set_cursor_pos` (5-line body), `_clamp_cursor_pos`
   (5-line body), the three `methods` closures (≤6 lines each, nesting ≤2).
   Every added source line is ≤64 chars (`awk` over the diff = clean).

## Test quality

The contract rows drive `F.compy_input()` = `CC:get_project_env().compy.input`
— the real project-facing surface, exercising real controller/model code.
The two monkeypatches (`Log.warn`, `view.render`) are at genuine boundaries
(logging channel; a render spy paired with a real handle-identity
assertion). No empty/mock-only tests. Model-level rows isolate the single
behaviour-changing edit. Red→green evidence: the model row failed red
(`Expected 3, Passed 4`) before the gate; the contract rows' red was
reconstructed by removing the three `methods` entries (11 errors / 1 pass,
the AC-10 row rightly staying green on the pre-existing boundary) — an
honest note that the natural red-first order was broken by a mid-run
interruption.

## Findings (non-blocking)

- **`UserInputModel:set_text` body exceeds the 14-line limit — pre-existing,
  marginally worsened.** The body is now 19 lines; it was already 17 before
  this chunk (the type-dispatch head alone is 12). The mandated fix was a
  surgical tail gate, and bringing the function under budget means an
  extraction that would expand this additive slice — so leaving it is
  defensible, but it is worth recording that the one src function the chunk
  edits was already over the hard limit and is now +2. A tighter
  `if keep_cursor … else … end` (no early return) would have saved one line.
  Not a corrective; noted for the M7-02 pass or a future refactor.
- **Cosmetic: singleton resolution is inconsistent across the three new
  `methods` closures.** `get_cursor` and `set_text` bind
  `local ui = love.state.user_input_controller` then call on `ui`;
  `set_cursor` calls `love.state.user_input_controller:set_cursor_pos(...)`
  inline. Purely stylistic — flag only for local consistency, no behaviour
  impact.

## Report-don't-fix confirmed

The `set_text` multiline-**string** branch (`n_added > 1` never reassigns
`self.entered`) is pre-existing and was **noted, not fixed** — confirmed in
both the diff (untouched) and the ledger's tech-debt section. The
implementor also correctly flagged that the qualified
`UserInputModel:set_text` LSP refs query returns empty on this codebase, so
grep is authoritative for that caller sweep — a useful note for future
sweeps.

## Bottom line

Approve. 794/0/0/5, 5 pending unchanged (anchor @1828 intact). All five ACs
proven by discriminating tests against the real surface; the model fix is
caller-safe; scope fence held. The only substantive note is the
pre-existing (now +2) `set_text` body-length overrun — a candidate for the
M7-02 slice, not a blocker here.
