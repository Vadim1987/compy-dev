# Review — M4-0-02: keyboard/text routing matrix (contract gap-fill)

_Reviewer: Claude Opus 4.8 — 2026-06-29 (independent; did not write the code)._
_Slice: `prompts/M4-0-02-contract-routing-suite.md` · outcome:
`outcomes/M4-0-02-contract-routing-suite.md` · commit reviewed: `30cd9ca`._

## Verdict

**Approve.**

**Approval-scope note.** This is a **test-only** slice (no `src/` change). The
evidence establishes: (a) the full suite is green at the claimed counts; (b) each
required Group-1 cell has **teeth** — I independently perturbed the production
routing and watched the named tests red, then restored. It does **not** establish
anything at runtime beyond the busted harness (none is owed here — the slice adds
regression guards, it changes no behaviour). The contract-note revision visible in
the working tree is **out of this slice's scope** (see Scope fence) and is not part
of this verdict.

## Spec compliance (per charge item)

| Charge | Result | Evidence |
|---|---|---|
| **G1 textinput EXCLUSIVE** (overlay-only when active; base sink otherwise; never both) | ✔ | `active overlay receives textinput` (spec:253) + `active overlay blocks native textinput` (:260). Base-sink case already covered by `console text reaches the console input` (:198, via `mock.textinput`) — correctly reused, no gap. Gate: `controller.lua:660-667` (if/else exclusive). |
| **G1 keyreleased EXCLUSIVE** (base when no overlay; overlay-only when active; never both) | ✔ | `console keyreleased reaches the console` (:276), `active overlay receives keyreleased` (:282), `active overlay blocks native keyreleased` (:289). Gate: `controller.lua:669-682`. |
| **Inspect — characterization `pending`, NOT a guard** | ✔ | `describe('inspect ownership')` + `pending('inspect: console owns input — provisional')` (:321-330). Block comment states current behaviour (`get_user_input()`→nil under `inspect`, `controller.lua:20`) + the verbatim PROVISIONAL §3.4 marker. No green assertion — a deliberate inspect change cannot red it. |
| **Mouse/touch BOTH (optional)** | ✔ (mousepressed included) | `mousepressed reaches widget and base sink` (:305) asserts both `c.mousepressed` (count 1) and `love.mousepressed` (count 1), order not asserted (R3). Gate is non-exclusive: `controller.lua:689-698` (widget call, then unconditional base). `input_session.lua` gained a `mousepressed` emitter (:20,:28). Touch not added — acceptable, optional. |
| **Naming & hygiene (C3)** — no milestone/sprint id in any test/`describe` string | ✔ | All new `it`/`describe` strings name behaviour. (One pre-existing **comment** at `input_session.lua:26` mentions "M4" — not a test string, and untouched by this commit.) |

All five new `it()`s sit inside the existing `must-not-degrade` group and are written
at **outcome level** (assert which real consumer received), so they survive the M4/M5
gate rewrite. Spine of the spec preserved (production-slot setup, `record_calls`,
`show_input_overlay`, editor block-nav row, 4 carried `pending`s).

## Re-run + teeth (verified independently)

- **Suite:** `busted tests` → **714 successes / 0 failures / 0 errors / 5 pending**
  (1 new inspect + 4 carried). Matches the ledger exactly.
- **Teeth — I reproduced the perturb→red→restore myself** (ephemeral edits to
  `controller.lua`, restored via `git checkout`; working tree confirmed clean after):

  | Perturbation | Test reddened |
  |---|---|
  | `handlers.textinput`: drop widget call + always fire `love.textinput` | `active overlay receives textinput` **and** `active overlay blocks native textinput` |
  | `handlers.mousepressed`: drop `user_input.C:mousepressed(...)` | `mousepressed reaches widget and base sink` |
  | `handlers.keyreleased`: drop both widget + base call | `console keyreleased reaches the console` **and** `active overlay receives keyreleased` |

  5 of the 6 ledger cells reproduced directly; the 6th (`active overlay blocks
  native keyreleased`) is structurally identical to the verified textinput
  block-native cell. **The ledger's teeth claims hold.** Confirmed `user_input.C ==`
  the overlay controller `c` (`userInputController.lua:222-226`), so the
  `record_calls(c, …)` spies intercept the real gateway delivery, not a proxy.

## Scope fence

- Commit `30cd9ca` (`git show --stat`) touches **only**
  `tests/input/input_routing_spec.lua` and `tests/helpers/input_session.lua`. No
  `src/` change. ✔ Clean.
- The `input_session.lua` diff also re-aligned the existing `repeat_press` line to
  accommodate the new `mousepressed` key — cosmetic whitespace, harmless.
- **Note (not a finding against this slice):** `notes/input-contracts.md` shows as
  **modified** in the working tree, but that change is **not** in commit `30cd9ca` —
  it is the orchestrator's separate s26 contract-note revision (uncommitted). The
  slice correctly treated the contract note as read-only input; no scope breach.

## Rules check

- Hard limits: function bodies / params / nesting all fine in the new code.
- **Line length (minor):** `input_routing_spec.lua:320` is **65 chars**, exceeding
  the 64-char hard limit (a comment line). `:252` is 66 **bytes** but 64 **display
  columns** (the `→` is multibyte) — within limit. Low severity; comment-only; the
  pre-commit hook does not gate on it. Recorded here, not logged as ledger debt
  (fix-in-place nit, not deferred design debt).
- Commit hygiene: subject `test: complete keyboard/text EXCLUSIVE matrix in routing
  suite` — Conventional Commits, `test:` prefix correct for a test-only slice.
  Committer identity unchanged (`Hleb Rubanau`). ✔

## Tech-debt + docs

- **Tech debt:** nothing new surfaced by the slice; ledger needs no new row. The
  one rules nit above is a trivial fix-in-place, not tracked debt.
- **Docs:** test-only, no observable-behaviour or interface change. The relevant
  doc (`notes/input-contracts.md`) already records §3.2/§3.3/§3.5 and is read-only
  input to this slice. No `internals/` update owed.
- **Contract-note feedback:** ledger says "none"; I **concur** — every §3 row
  exercised here proved straightforwardly black-box observable through the real
  gateway slots (confirmed by the teeth perturbations). No finding for the
  orchestrator on the contract record.

## Acceptance checklist

- [x] textinput EXCLUSIVE matrix complete, green, teeth-verified.
- [x] keyreleased EXCLUSIVE matrix complete, green, teeth-verified.
- [x] inspect carried as characterization `pending` (not a guard).
- [x] mouse BOTH optional guard included with teeth (touch deferred — allowed).
- [x] No milestone id in any test/`describe` string.
- [x] Full suite green; pendings show as pending; commit is test-only.
- [ ] _(nit)_ `input_routing_spec.lua:320` comment trims to ≤64 chars.

**Recommendation:** approve. The single nit (one over-length comment) is optional
cleanup and does not block the slice.
