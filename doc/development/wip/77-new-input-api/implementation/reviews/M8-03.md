# M8-03 review — legacy text-input global removal (THE TERMINAL CHUNK)

**Commit:** `b4d96ec` `refactor(input)!: remove legacy text-input globals + poll machinery (M8-03)`
**Reviewer:** independent Opus reviewer (did not write the code). Verify-don't-trust: suite re-run,
grep + lua-lsp zero-ref sweeps, all five examples smoke-loaded, docs + balloons `.git` inspected.

## VERDICT: APPROVE — the #77 new-input-API sweep is COMPLETE

Suite green with all six legacy globals gone from the project environment, the poll-a-reftable
machinery at zero references (grep + lua-lsp), AC-1/AC-2/AC-7/AC-10 (+ AC-6/AC-8/AC-9) all holding,
and the five migrated examples smoke-loading traceback-free against the post-removal tree. No defect
survived verification. **This closes #77.**

## Busted counts (observed by me)

- **Final:** `808 successes / 0 failures / 0 errors / 4 pending` (1.40s).
- The **4 pending are unchanged** — the pre-existing routing-gap cells
  `input_contracts_spec.lua @101/@153/@161/@222`, not this chunk's.
- Net `-1` from the 809 baseline: the deleted reftable-fill row; the `#legacy` block was a 1-for-1
  swap (old warn-refusal test → new nil-call test). Matches the ledger exactly.

## Per-trap findings

1. **AC-1 — all SIX globals gone as ordinary nil fields — PASS.** The diff removes
   `project_env.user_input/input_code/input_text/write_to_input/validated_input` **and** the
   `if love.debug then project_env.astv_input = … end` block. No shim, no deprecation stub. The
   converted `#legacy` test drives the real `F.cc:get_project_env()` (after `F.activate_project()`)
   and asserts each of the six is `nil` — a real production path, not an empty/mock test. Green.
2. **Machinery fully gone, zero dangling refs — PASS.** `input`, `input_ref`, `create_input_handle`
   all deleted. lua-lsp `references`: `input_ref` → "No references found", `create_input_handle` →
   "No references found". Grep tree-wide for `input_ref|create_input_handle` → zero. (The `input`
   name is too generic for a useful LSP query; grep for the machinery names + the removed helper's
   call sites confirms none remain. `local input = self.input` at ~L1113/1127 is the console's own
   command-line widget field read — unrelated, correctly left.)
3. **AC-2 — dead write gone, no reader/writer — PASS.** `compy_namespace.text_input = input_text`
   removed. Grep `.text_input` tree-wide (excluding `on_text_input`) → **zero**. No reftable poll
   surface remains project-facing.
4. **`astv_input` pinned ruling — PASS.** Removed WITH the machinery (rode the same `input()` /
   reftable plumbing), flagged surprise-first in the ledger as the sixth global. NOT re-plumbed onto
   `compy.input` (no unrequested new surface), NOT left dangling. Removed-and-flagged = correct.
5. **AC-7 — `love.state.user_input` overlay handle untouched — PASS.** `controller.lua`
   (`get_user_input()` + mouse/touch handlers L929-1026), `types.lua`, `userInputController.lua`,
   `projectInputController.lua` all show **zero diff** (commit stat: only consoleController.lua +
   test + two docs + ledger). The removed `input()` never set `love.state.user_input` directly — it
   only called `uic:show({…})`, the same activation path `compy.input.show` uses; with it gone, no
   alternate legacy driver exists. Suite rows asserting `love.state.user_input` transitions stay green.
6. **Legacy tests correctly resolved — PASS.** `#legacy` block converted to the AC-1 nil-call
   assertion; the `'a legacy solicitation still fills the reftable on submit'` row deleted (subject
   gone). Its surviving submit-deactivation half is still covered by the adjacent AC-26 row
   `'submit and cancel complete with no hooks set'` (present L1478, green). Grep the spec → no live
   `env.user_input()`/`env.input_text()` caller remains (only the nil-call assertions + comments).
7. **AC-10 / AC-8 — five examples run with globals gone — PASS (to the honest ceiling).**
   Smoke-loaded tixy/repl/guess/valid/balloons headless (`xvfb-run -a love src play …`): each ran to
   the timeout kill with **zero error/traceback output**. Negative control (nonexistent project) DID
   surface its error to stdout, confirming the harness reveals load failures — so the clean runs are
   meaningful, not silent no-ops. Corroborated decisively by the grep sweep: **zero live callers of
   the six globals anywhere in `src/examples/`** (only comments in guess/valid naming the
   `ValidatedTextEval` evaluator that stays), so no code path exists to nil-crash. Honest ceiling
   (no keystroke injection → real compose/submit is a human hand-play gate) is stated in the ledger,
   not overclaimed.
8. **AC-6 natives / AC-9 nested `.git` — PASS.** No example or pure-native file in the diff
   (`git show --stat`: none). `cd src/examples/balloons && git log --oneline -1` → `56347d0` (the
   M8-02 migration commit), untouched, no new M8-03 commit.
9. **Doc sync proportionate + honest — PASS.** `internals/user_input.md` updated: `compy.input.*`
   stated as the sole project-facing surface as of 0.1-m8, the six globals explicitly retired
   (ordinary nil, no shim), the `r:is_empty()` poll paragraph replaced with the
   `on_text_entered → hide → after_submit` description. `internals/console.md` also updated (Overlay
   section rewritten, `compy.text_input` table row corrected to `compy.input`). Per-example-doc drift
   (7 files) is explicitly FLAGGED as a follow-up in the ledger, not silently skipped — acceptable
   for the bounded terminal chunk.
10. **`src/vadexamples/` left alone — PASS.** No vadexamples file in the diff. It still calls the
    globals (would nil-crash if run) — correctly noted, not migrated/deleted (untracked scratch).
11. **Report-don't-fix respected — PASS.** The controller-side dead `result`/reftable delivery path
    (`userInputController.lua:223-224`, `:364-366`), now unreachable with `input()` gone, was LOGGED
    (ledger + I added it to `technical_debt.md`), NOT removed — correct, it is `src/controller/*`
    outside the consoleController-only Files scope.
12. **Rules limits — PASS.** The removal only deletes; the new nil-call test closure is tidy
    (six `assert.is_nil` lines, well within line/body/param/nesting limits). lua-lsp `diagnostics`
    clean on both edited files per the ledger.

## Zero-references confirmation

- lua-lsp `references`: `input_ref` → none; `create_input_handle` → none.
- grep tree-wide: `input_ref|create_input_handle` → zero; the six global names → zero live callers
  outside `src/vadexamples/` (all other hits are the overlay handle `love.state.user_input`, the
  overlay `@field` annotation in `types.lua:144`, comments, buffer string literals, `user_input = nil`
  overlay-state fields in fixtures, and the new nil-call assertions).
- `.text_input` (excl. `on_text_input`) → zero.

## Five-example smoke-load results

tixy / repl / guess / valid / balloons — all loaded and ran headless to the timeout kill,
**traceback-free** (only ALSA no-device noise), with the six globals gone. Pure-native spot check
(pong, per the ledger) traceback-free. Honest gap: no keystroke injection in-container → actual
compose/submit/re-prompt remains the human's final hand-play gate (consistent with every prior
example-migration ledger in this sweep).

## Scope-fence confirmation

Code change is `consoleController.lua` ONLY. `evaluator.lua`, `controller.lua`, `types.lua`,
`userInputController.lua`, `projectInputController.lua`, `src/model/*`, `src/examples/*`, and
`src/vadexamples/` all untouched (verified via commit stat + `git diff`). Balloons detached `.git`
untouched.

## Tech debt (recorded in `technical_debt.md` this review)

1. Controller-side dead `result`/reftable delivery path — unreachable after the global removal;
   report-don't-fix, future controller-pass cleanup.
2. Per-example internals-doc drift (7 files) — flagged follow-up doc pass.
3. `src/vadexamples/` untracked scratch still calls the removed globals — note-don't-touch.

## Note for the PM (not a M8-03 finding)

A pre-existing, unrelated working-tree diff to
`doc/development/wip/77-new-input-api/implementation/docker/compose.yml` was present at session start
and is correctly NOT in commit `b4d96ec` (same file flagged in the M8-01 ledger). Left for you.

---

**APPROVE. The #77 new-input-API sweep is COMPLETE** — the legacy text-input globals and the
poll-a-reftable machinery are fully retired, `compy.input.*` is the sole project-facing input surface,
the suite is green (808/0/0/4), and all migrated examples run against the post-removal tree.
