# M1 — `keys_pressed` table — outcome ledger

_Skeleton seeded by LLM(Claude Opus 4.8): 2026-06-16 (session 05). Filled by the implementation agent._

**Status:** ⏳ awaiting implementation (prompt: [`../prompts/M1.md`](../prompts/M1.md))
**Spec implemented:** [`../../design/spec/M1.md`](../../design/spec/M1.md)

---

## Commits

| Hash | Subject |
|---|---|
| _(agent to fill)_ | _(Conventional Commits — e.g. `feat(input): add Controller.keys_pressed + combo_string`)_ |

## Files changed

- _(agent to fill — e.g. `src/controller/controller.lua`, `tests/input/…_spec.lua`)_

## Verification

- **Command:** `busted tests` (or `just ut_all`)
- **Result:** _(agent to fill — real pass/fail counts; the names of the new specs)_
- **Behaviour-change check:** M1 is purely additive — confirm no behavioural change observable in
  any app mode (existing suite still green).

## Surfaced gaps

- _(agent to fill, or "none" — e.g. the `src/controller.lua` → `src/controller/controller.lua`
  path mismatch in the design docs)_
