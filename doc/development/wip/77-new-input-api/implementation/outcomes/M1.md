# M1 — `keys_pressed` table — outcome ledger

_Skeleton seeded by LLM(Claude Opus 4.8): 2026-06-16 (session 05). Filled by the implementation agent._

**Status:** ✅ implemented — awaiting human approval
**Spec implemented:** [`../../design/spec/M1.md`](../../design/spec/M1.md)

---

## Commits

| Hash | Subject |
|---|---|
| `2a15602` | `feat: M1 — keys_pressed table and combo_string helper` |

## Files changed

- `src/controller/controller.lua` — added `COMBO_MODS` table and
  `combo_string` local function (exposed as `Controller.combo_string`);
  added `Controller.keys_pressed = { }` field;
  added `Controller.keys_pressed[k] = true` at the top of
  `handlers.keypressed` and `Controller.keys_pressed[k] = nil` at
  the top of `handlers.keyreleased` in `setup_callback_handlers`.
- `tests/input/keys_pressed_spec.lua` — new file, 11 tests.
- `doc/development/internals/user_input.md` — added "Key state:
  `Controller.keys_pressed` and `combo_string`" subsection under
  "Keyboard Handling".

## Verification

- **Command:** `busted tests`
- **Result:** 685 successes / 0 failures / 0 errors / 0 pending
  (baseline was 674; 11 new tests added).
- **New test names:**
  - `keys_pressed table — adds key on keypressed`
  - `keys_pressed table — removes key on keyreleased`
  - `keys_pressed table — tracks multiple held keys`
  - `keys_pressed table — does not fold lr variants in table`
  - `combo_string — bare key escape`
  - `combo_string — bare key s`
  - `combo_string — ctrl+s from lctrl held`
  - `combo_string — ctrl+s from rctrl held`
  - `combo_string — alt+shift+f4 ordering`
  - `combo_string — ctrl before alt precedence`
  - `combo_string — all modifiers: ctrl alt shift gui`
- **Behaviour-change check:** purely additive — all 674 pre-existing
  tests continue to pass.

## Surfaced gaps

- **Path mismatch (known, already noted in prompt):** the design docs
  reference `src/controller.lua`; the real path is
  `src/controller/controller.lua`. No action needed — noted in
  `../prompts/M1.md`.
- **`gfx` global in controller.lua:** `gfx` (used in `set_love_update`
  / `set_love_draw`) is an implicit global not set in controller.lua
  or any of its requires. It is a free variable that must exist at
  call time (set by the app's load sequence). This is pre-existing
  tech debt; M1 does not touch those code paths.
- **`table.protect` is a no-op on the passed table:** calling
  `table.protect(love.handlers)` at the end of `setup_callback_handlers`
  returns a proxy but does not mutate the original table. Pre-existing;
  not a gap introduced by M1.
