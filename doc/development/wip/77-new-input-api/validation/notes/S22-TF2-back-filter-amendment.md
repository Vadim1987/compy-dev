# S22 — back-filter amendment

`S22-TF2-back-filter.md` used too narrow a criterion: whether a later
ruling would change TF2's behavioural reading. The owner correctly
identified the broader criterion: whether it would change the eventual
PR candidate and create a second review discontinuity.

The revised recommendation is to dispose every visible pre-review item
before TF2: execute it in #77, retain it with a stable explanation, or
explicitly exclude it from #77. Then regenerate the navigation batch
and perform one full review of that settled candidate.

The full independent consultation and item-by-item recommendations are
in `../outcomes/S22-sol-pre-TF2-ruling-order.md`.

Corrections to the earlier note:

- literal jargon and bad-reference annotations remain in the split
  tests, so their cleanup changes what TF2 reads;
- RVW-087 is the current owed held-key identity question; the inventory
  cross-wires its disposition with a widget-state outcome;
- RVW-092 is the still-live suite-level reference-cleanup instruction.
