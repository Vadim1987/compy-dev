# estimates — version 02 (E16 recalc: M4-0 added)
_Generated by LLM(Claude Opus 4.8): 2026-06-23 (E16, session 21); Approved by human?: NOT YET;_

> **Latest version → references up to canonical.** Per [`doc-format.md`](/var/lib/brainlab/current/rules/doc-format.md)
> §6/§6.1, the single latest version file points up to the canonical doc (it *is* canonical at this
> moment); only non-latest versions are frozen full snapshots ([`version01.md`](version01.md) holds the
> frozen genesis snapshot).
>
> **What changed vs. version01.** The milestone set changed: **`M4-0`** (feature-global characterization
> net + harness extension) was sized and added after the E9 architect call answered its infra-feasibility
> (modest harness extension — keypress-level driver + two `mock` emitters, the three production slots
> already exist — plus the characterization suite). The M5/M6/M7 **test-first** acceptance steps are a
> *re-sequencing* of test work already in the existing Test-coverage bucket → **no volume delta**.
>
> **Total at this baseline:** ≈ 74 h (without LLM) / ≈ 44 h (with LLM) — **+≈ 8 h / +≈ 5 h** vs. the
> frozen design-phase baseline (≈ 66 h / ≈ 39 h), attributable to `M4-0`.
>
> **Current content:** see [`../estimates.md`](../estimates.md).
>
> When `estimates.md` is next recalculated (E11), **freeze this file's content to the then-current
> canonical snapshot** before minting `version03.md` (demote-by-freezing, §6.1), and append the new
> total as a line to the roadmap's total-estimated log. The frozen design-phase total is never touched.
