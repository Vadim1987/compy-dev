# M5c — the dispatch chain (consolidated slice, E29 re-cut)

_Generated by LLM(Claude Fable 5): 2026-07-06 (brainlab session33, E29 Stage 3); amended by LLM(Claude Opus 4.8): 2026-07-06 (brainlab session35 — Gate-3 cold-validation retro-reconciliation, human-directed); amended by LLM(Claude Opus 4.8): 2026-07-06 (brainlab session36 — Scope-10 generic-callback terminology + AC-36 four-case interception across both install paths & all channels + AC-38 signature/proxy & full-travel coverage + implementor/design SoD split; route-restoration lexicon and assign-replaces-capture escalated to a cold architectural session); amended by LLM(Claude Opus 4.8): 2026-07-07 (brainlab session37, E30 — the two escalated contradictions **resolved**: (a) route-restoration = active-route/mode, the `stop names the console…` row retargeted to AC-29 teardown, `active_keyboard_route()` accessor assertion dropped (C23); (b) assign-replaces = **precedence, not replace** — explicit `on_*` > captured native > noop, spec §8 R7 hardened; no design.md/spec.md contract reopen — spec §8 R7 was a realignment to the ratified model); amended by LLM(Claude Opus 4.8): 2026-07-07 (brainlab session39, E32 — the four E31 suite defects **resolved by owner ruling in-plane**: F1 legacy-solicitation retirement lifecycle → AC-39; F2 `on_text_input`/`on_text_entered` split + three-channel combo dispatch → AC-40/AC-41; F4 mechanism-vocabulary row superseded by AC-36 keyreleased → Scope-10(c); F3 → AC-42 (highlighter/validator functionally applied, not just settable — owner-ruled in-slice); + AC-43 retirement meta-rule (no silent test retirement — owner-ruled); spec.md §5 L234 push-slice collision **resolved + reworded** (producer-m5c / consumer-m8, owner-accepted, awaiting his commit)); Approved by human?: **YES — 2026-07-07** (Gate 3 **CLOSED**, brainlab session40 / E33 cold spec-vs-design revalidation **clean** on all four dimensions; this slice is now **frozen** — post-freeze gaps route to adjacent `-NN` slices per process §9.1, never a retroactive rewrite);_
Derived from: [`../spec.md`](../spec.md) (the re-derived contract, Gate-2 approved — §§1–2, §4–§5, §8; **authority**),
[`../design.md`](../design.md) (§0 ratified model, §3, §5),
[`../../reviews/m4-architect-pushback.md`](../../reviews/m4-architect-pushback.md) (QUALITY/SCOPE remark inventory).
Supersedes (as implementation targets): [`M5.md`](M5.md), [`M5-01-split.md`](M5-01-split.md) (M5a/M5b), [`M6.md`](M6.md) — those files stay frozen as history; **do not implement against them**.

> **Why one slice (hypothesis verified, session33).** Old M5a+M5b+M6 are one semantic — the
> chain: tiers 2–3 (M5a/M5b) and the tier-1 submit/cancel entries (M6) are the same four-tier
> shape on the same dispatch path in the same files. The M4 gate showed what slicing
> mid-semantic costs: 8 of the pushback remarks were scope-parallax (judging a skeleton
> against the end-state chain). One slice = one coherent review surface = the whole chain
> behaving per the ratified model. The ~18 QUALITY remarks fold in rather than riding a
> separate `M4-01` corrective because most target code this slice deletes or rewrites
> (`native_split` disappears under R7 pure wrap; the `forward_*` wrappers reshape when the
> sink's hidden-check internalizes; suppressed return values *become* the chain's
> truthy-propagation contract). Polishing code about to be replaced would be effort burn.

## Summary — the slice in plain language

After this slice, a project's keyboard and text input flows through one predictable pipeline.
When a key is pressed (or text arrives, or a key is released), the framework first checks its
own reserved keys (Enter/Escape — only while the input widget is on screen), then the
project's registered shortcuts (`compy.input.handlers.keypressed['ctrl+s'] = fn`), then the
project's catch-all callback (`on_key_pressed` / `on_text_input` / `on_key_released`), and
finally the input widget itself — which does ordinary text editing when visible and nothing
when hidden. Any stage can declare the event handled (return truthy) and nothing after it
runs for that event — but nothing ever unplugs a later stage. Enter submits (validate →
deliver the text → close the widget); Escape now genuinely dismisses the widget; the old
`oneshot` flag and the `push('userinput')` machinery are gone. A project's plain
`love.keypressed`-style handlers keep working as ordinary pipeline participants; turtle and
maze — the two examples whose behaviour visibly changes — are migrated in this same slice.

## Scope — what this slice delivers

The complete four-tier dispatch chain of `spec.md` §2, on all three keyboard/text channels,
in the project route — plus everything the ratified model changed about M4's landed shape:

1. **The chain** (`spec.md` §2): `framework_handlers.<event>[combo]` →
   `compy.input.handlers.<event>[combo]` (per-event sub-tables, R14; normalising assignment,
   overloadable matcher — §1) → per-event generic callback (`on_key_pressed` /
   `on_text_input` / `on_key_released`, default noop + debug log) → the sink (terminal,
   internal hidden-check, no external gating wrapper). Truthy = consumed, stop. Uniform
   signatures per channel (`spec.md` §2 table), `isrepeat` threaded on keypressed.
2. **`keyreleased` unified** (ruling 4) — same chain shape, key already removed from
   `keys_pressed` when consumers run.
3. **Submit/cancel** (`spec.md` §5): tier-1 `keypressed['return']` / `['escape']` engage only
   while the widget is shown; `before_/after_submit`, `before_/after_cancel` call-order
   chains; validator gate; delivery via `on_text_entered(text)` widget output; unconditional
   deactivate step (project-route policy — spec §5 scope note); Escape genuinely dismisses;
   `hide()` fires no cancel chain. **`oneshot` deleted** — both jobs moved out of the widget;
   `push('userinput')` dissolved (spec §5 mechanism note).
4. **Widget outputs live** (`spec.md` §4): `on_text_entered`, `on_limit_reached(direction,
   scope)` (D-5 both scopes — extends `is_at_limit` to horizontal + line scope), `validator`,
   `highlighter`; config keys AND assignable fields (D-b); never chain return values (R12).
   Functional application (highlighter transforms, validator gates) is asserted, not just
   settability — AC-42.
5. **Route connection per ruling 3** (`spec.md` §8): keyboard/text slots occupied only while
   `'running'`; on drop to `'project_open'` they restore to the console route — **the M4
   ruling-1 forwarding is removed**. Pointer slots untouched (Stage-1 census: `sapper` stays
   clickable). Stop = full teardown invariant (spec §8) incl. silent widget hide.
6. **Legacy natives = pure wrap** (R7, `spec.md` §8): captured at load as initial tier-3
   callback values; `native_split`/lifecycle-split wrapper deleted. **turtle + maze migrate
   in this slice** (design.md §5 sequencing constraint — never deferred to M8, else an
   interim window ships misbehaving examples). SR1 break mandate covers them.
   **Delivery mechanics (Gate-3 ruling, 2026-07-06):** `src/examples/maze/` is a **copied-in
   checkout** — it has its own nested `.git` and is not tracked by this repo. Migrate it in
   place, but deliver the migration as **uncommitted working-tree changes**: never commit
   inside `src/examples/maze/`, never touch its `.git`; the outcome ledger lists the changed
   files and the human carries the patch upstream. turtle is tracked in-repo and commits
   normally.
7. **Mutable/immutable boundary** (R3, `spec.md` §7): exactly the `handlers` tables +
   `on_*`/`before_*`/`after_*` fields; everything else errors loudly on assignment.
8. **QUALITY corrective** (in-slice; per-remark disposition required in the outcome ledger —
   `fixed` or `dissolved-by-rework`, each with the remark id): C1/C15/P3 warn-on-silent-drop
   (C2 rule); C2/C5/C16/P11 naming + per-event duplication (table-driven set-up); C3/C14
   return-value propagation (now the chain contract); C9/C13 dispatch legibility; C19
   `set_love_*` table+iterator TODO; C20 `love.handlers` section comment; C23
   `active_keyboard_route` accessor → test-scoped unless a real consumer lands (no unconsumed
   public surface); T2 comments cite doc A/spec, not session lore; T3/T4 forward test notes →
   live assertions (downstream propagation without swallowing; same `keys_pressed` table
   end-to-end); U3 doc hygiene (keyreleased routing now real, document it as landed).
9. **`>> REVIEW` markers** in the project tree that these rows resolve are removed by this
   slice, each with a ledger line citing the resolving contract point. Markers this slice
   does not resolve stay in place.
10. **Suite retro-reconciliation (E29 — the landed suite predates the re-derivation).** The
    contract suite was last shaped by the **M4 milestone against the superseded spec** and was
    never re-reviewed against the ratified model. This slice **re-derives the M4-landed green
    Bucket-B rows to the target assertions stated here** (this item + AC-36/AC-38): the implementor
    conforms the suite to those assertions and does **not** itself judge a row against the ACs —
    a row that cannot be reconciled is an **escalation stop** (AC-37), never a quiet test edit. It
    does not merely convert the pending rows:
    - **The generic-callback interception rows** (landed as `a native handler coexists with the
      sink`, `project keys reach the project sink`, `the keypressed path carries the triple`).
      *Generic callback* = the tier-3 participant of `spec.md` §chain
      (`on_key_pressed`/`on_text_input`/`on_key_released`), contrasted with the *specific*
      combo-handlers of tiers 1–2. A project installs one by either of **two paths — both populate
      the same tier-3 slot** (spec §8 R7): a **legacy `love.*` native** (the project-declared
      `love.keypressed` etc. **in its own env**, captured at load as the callback's initial value —
      *not* a live LOVE hook) **or** a direct **`compy.input.on_*` assignment**. The landed
      coexistence row asserts the reversed day-one mutation (widget shown ⇒ callback suppressed, the
      `native_split` wrapper). Target assertions → **AC-36** (four interception cases, both install
      paths, all three channels) + **AC-38** (signature/proxy schema + full tier-1→4 travel). Rows
      renamed to the generic-callback vocabulary.
    - The in-tree `-- REVIEW:` comments in the suite are design inputs (mandate guardrail 4):
      reconcile each against a cited AC; a genuine contradiction is a stop, never a quiet test edit.

    **Resolved (E30, session37 — cold architectural session;
    [`../../notes/talk/m5c-suite-reconciliation-open-contradictions.md`](../../notes/talk/m5c-suite-reconciliation-open-contradictions.md) §Resolution):**
    - **(a) Route-restoration lexicon.** The binding concept is the **active route/mode**, not slot
      restoration — and the frozen design **already states it correctly** (AC-27–30 are route-level);
      no design.md/spec.md contract reopen. The landed `stop names the console as restored route` row
      is **retargeted to AC-29 teardown**: stop's distinctive contract is the full teardown
      (project participants unwired, widget silently hidden), **not** "keyboard route == console" —
      that end-state is shared by project-exit and inspect and is not stop-specific (answers the L718
      REVIEW). **Drop** the `active_keyboard_route()` accessor assertion (C23 — no unconsumed public
      surface). Restore cases in route terms: project-exit → `'project_open'`, typing reaches the
      REPL (AC-27); stop → teardown to framework defaults (AC-29); inspect → console route bound over
      the project env (AC-30).
    - **(b) Assign-replaces-capture.** Resolved to **precedence, not replace** (AC-31/AC-36 above):
      explicit `on_*` > captured native > noop; the native seeds tier-3 only when no `on_*` is set
      and never overrides one. `spec.md` §8 R7 + §11 #8 **hardened** (E30) — a realignment to the
      ratified model R7 / design §5 (which never carried a replace relationship), removing the
      reverse-override the old "assignment replaces the wrapped native" phrasing permitted.
    Both landed rows' `-- ESCALATED`/`-- STALE` markers are converted to point at the resolving AC.

    **Resolved (E32, session39 — owner ruling in-plane;
    [`../../notes/talk/e31-m5c-suite-sequencing-contradictions.md`](../../notes/talk/e31-m5c-suite-sequencing-contradictions.md)
    §Resolution):**
    - **(c) Mechanism-vocabulary behavioural row (F4).** The green row `a release under a widget
      reaches the occupant` (L589, `-- REVIEW:` L588 "the raw slot stands in for the occupant … the
      correct test checks downstream propagation without swallowing") is **superseded by AC-36 on the
      keyreleased channel**: AC-36 case (a) already asserts the tier-3 participant fires regardless of
      widget-shown state, and cases (b)/(c)/(d) assert downstream propagation-without-swallowing — the
      L588 REVIEW's "correct test." The row is **deleted the moment AC-36's keyreleased rows land
      green** (not before — no coverage gap). No new AC; a Scope-10 disposition.

**Riding adjacents:** [`M6-01-oneshot-snapshot.md`](M6-01-oneshot-snapshot.md) (view `oneshot`
snapshot removal — rides the `oneshot` deletion) and [`M6-02-before-exit.md`](M6-02-before-exit.md)
(`compy.before_exit` stop hook, human-approved session 23 — rides the stop-path work, item 5).

**Out of scope:** the M7 cursor/config surface (`configure`/`clear`/`get_cursor`/`set_cursor`/
`set_text`); M8 legacy-globals removal and the remaining example migrations; console/editor
migration (follow-on; Gate-2 closing ruling: no opportunistic unification in-slice); pointer
chain adoption (R10 — later, no model cost); the ergonomic **form** of the
`show`/`configure`/`set_text` relationship (owner-deferred open question — the surface ships per
spec §3/§6, but which authoring shape is most ergonomic is revisited on the landed foundation:
[`../notes/deferred-input-ergonomics.md`](../notes/deferred-input-ergonomics.md)). *(The
route-restoration lexicon and assign-replaces-capture contradictions, previously escalated here,
are **resolved** — Scope item 10 (a)/(b), E30.)*

## Pinned SCOPE remarks (from the M4 pushback — assert, don't re-discover)

| Id | What this slice must now satisfy |
|---|---|
| U1 | The end-to-end chain is visible and testable: combo → handler → callback → sink, truthy stops propagation |
| P8 | Project-installed keypressed handlers + truthy-return chain exist and fire |
| P10 | textinput runs the same unified chain shape (combo tier per D-d, callback = `on_text_input`) |
| C4 / P2 | Project-space wiring goes through `compy.input.*`, not cross-layer `ui.C` access |
| C12 | Debug Ctrl+Shift if-blocks → table-driven combos now that the combo tier exists |
| U3 | keyreleased unified routing is landed and documented (no TBD left) |
| U2a | *Note only:* console still checks `love.state.user_input` — console migration is the named follow-on, not this slice |

## Acceptance criteria (verbatim, self-contained — the review surface)

*Added at Gate 3 (2026-07-06) on the architect's request: every criterion states the full
behaviour in place — ruling ids in parentheses are anchors, never required reading. The red
acceptance suite transcribes these; the outcome ledger cites them by `AC` id.*

**The chain**

- **AC-1** Every keyboard/text event in the project route traverses, in order: framework
  handlers → project combo handlers → per-event generic callback → the widget's editor (the
  sink). No other order; no other participants.
- **AC-2** A truthy return at any stage consumes the event: nothing after it runs — the sink
  included — for that event. A falsey return falls through to the next stage.
- **AC-3** A stage with no participant for the event falls through silently.
- **AC-4** Consuming never removes (R13): after any handler consumes an event, all stages
  remain configured, and the next event traverses the full chain again.
- **AC-5** Assigning a generic callback (e.g. `compy.input.on_key_pressed = fn`) replaces
  only that callback; the sink still runs for events the new callback does not consume.
- **AC-6** Combo handlers live in three per-event tables — `compy.input.handlers.keypressed`,
  `.keyreleased`, `.textinput` (R14). There is no flat combined table.
- **AC-7** Combo keys normalise on assignment: `handlers.keypressed['Ctrl+S'] = fn` is stored
  and matched as `'ctrl+s'` (modifier-first, l/r folded, `+`-joined).
- **AC-8** Signatures are uniform at every stage including the sink: keypressed participants
  receive `(k, keys_pressed, isrepeat)`; textinput `(text, keys_pressed)`; keyreleased
  `(k, keys_pressed)`. `keys_pressed` is a read-only proxy (writes raise).
- **AC-9** `keyreleased` runs the same chain shape (ruling 4); by the time its consumers run,
  the key is already absent from `keys_pressed`.
- **AC-10** The default generic callbacks are noop + debug log — they never edit text and
  never consume.
- **AC-11** An event with no participant anywhere (widget hidden, no handlers, default
  callbacks) falls through every stage to the hidden sink's **internal** no-op: a debug-log
  line is the only trace, and nothing mutates — no model, view, history, cursor, or
  selection change.

**Widget and widget outputs**

- **AC-12** Sink visible: ordinary text editing — insert, backspace, cursor moves, selection,
  clipboard, and Shift+Enter newline when `multiline`.
- **AC-13** Sink hidden: a delivered keystroke mutates nothing (as AC-11).
- **AC-14** The sink's return value carries no chain meaning (R12); boundary and submit
  conditions surface **only** through the widget outputs below, never through return values.
- **AC-15** `on_limit_reached(direction, scope)` fires when the cursor attempts to cross a
  boundary: `direction` ∈ up/down/left/right, `scope` ∈ input/line; single-line inputs
  collapse the two scopes. It is observational — its return value is ignored.
- **AC-16** Every widget output (`on_text_entered`, `on_limit_reached`, `validator`,
  `highlighter`) is settable both as a `show()`/`configure()` config key and as an assignable
  `compy.input` field — one underlying slot, two ergonomics.

**Submit and cancel**

- **AC-17** Enter with the widget shown runs: `before_submit(keys_pressed)` → validator →
  on accept: `on_text_entered(text)` with the full assembled text → the widget deactivates →
  `after_submit(text)`.
- **AC-18** Validator reject: the error is displayed and input locks until acknowledged
  (Enter/Space/arrows); `on_text_entered` and `after_submit` do **not** fire; the session
  stays active for correction or Escape.
- **AC-19** Escape with the widget shown runs: `before_cancel(keys_pressed)` → content
  cleared + widget hidden → `after_cancel()`. Escape genuinely dismisses (the old
  clears-but-does-not-dismiss behaviour is gone).
- **AC-20** Enter/Escape with the widget hidden are ordinary keys — no framework entry
  engages; they fall down the chain like any other key.
- **AC-21** Enter/Escape with the widget shown cannot be shadowed by project handlers: the
  framework entries run first, unconditionally.
- **AC-22** Shift+Return is not intercepted by the framework — it reaches the sink and
  inserts a newline when `multiline`.
- **AC-23** `hide()` fires no cancel chain. `show()` while a session is active is a no-op +
  warning; `{force=true}` reconfigures in place (content replaced iff `text` given), still
  with no cancel chain.
- **AC-24** Continuous-session idiom: `after_submit = function() compy.input.show{…} end`
  re-activates within the same submit sequence, before the frame draws (no visible gap);
  fresh activation without `text` starts empty; callbacks and `handlers.*` persist across
  deactivation (only project stop resets them).
- **AC-25** The `oneshot` flag is gone from the codebase. The `push('userinput')` custom
  event no longer exists as contract; the observable order is fixed: `on_text_entered` sees
  the session still active, `after_submit` sees it deactivated (unless the hook itself
  re-activated it).
- **AC-26** All four hooks (`before_/after_submit`, `before_/after_cancel`) default to
  noop + debug log.

**Route connection and teardown**

- **AC-27** The project route owns the keyboard/text slots only while the run state is
  `'running'` (ruling 3). When a non-blocking `main.lua` returns (no `update`/`draw`), the
  state drops to `'project_open'` and typing reaches the REPL again.
- **AC-28** Pointer slots are **not** part of that disconnect: a pen-and-paper example
  (`sapper`) stays clickable in `'project_open'`. Do not unify pointer disconnection in.
- **AC-29** Project stop is a full teardown: all slots restore to framework defaults; the
  `handlers.*` tables and every mutable field reset; a shown widget is silently hidden with
  no cancel chain; nothing installed by the project survives it.
- **AC-30** `inspect` is the console route bound over the project environment; the project
  route is disconnected and the project's widget goes unhonoured. No special rules.

**Legacy natives**

- **AC-31** A project's native `love.keypressed`/`love.textinput`/`love.keyreleased`
  auto-provision as the tier-3 **default participant** (R7 pure wrap) — plain participants that
  see events **even while the widget is shown**; a falsey return falls through to the sink. The
  native seeds tier-3 **only when the project set no `on_*`**: an explicit `compy.input.on_*`
  **takes precedence** (else the captured native, else the default noop), and `love.*` — read
  **once at load** — is never re-consulted, so reassigning it after load neither overrides an
  installed `on_*` nor re-enters the slot (spec §8 R7). There is **no "replace the wrapped
  native"** relationship: native and `on_*` are mutually exclusive by precedence.
- **AC-32** turtle and maze are migrated in this slice and hand-playable afterwards; maze is
  delivered as uncommitted working-tree changes in its nested checkout (scope item 6).

**Mutable boundary**

- **AC-33** Exactly the `handlers` tables and the `on_*`/`before_*`/`after_*` fields are
  project-assignable; assigning anything else on `compy.input` raises a loud error (never a
  silent swallow).

**Hygiene**

- **AC-34** Every pinned QUALITY remark and SCOPE row has an explicit disposition (`fixed` /
  `dissolved-by-rework` / `note-only`, with remark id) in the outcome ledger; every resolved
  `>> REVIEW` marker is removed with a ledger line citing the resolving contract point.
- **AC-35** The contract suite's m5/m5a/m5b/m6-family `pending` rows are live green; m7 rows
  stay pending; the full suite is green. (The AC-39 deprecated legacy-solicitation rows end this
  slice `pending()`-or-deleted, never red at the boundary, so they do not violate this.)
- **AC-36** The M4-landed generic-callback interception rows are **re-derived to these target
  assertions (Scope item 10), not preserved as-is.** A tier-3 generic callback is installed by
  **one of two mutually-exclusive paths** (spec §8 R7): a direct `compy.input.on_*` assignment,
  **or** — only when the project set no `on_*` — a legacy `love.*` native captured at load as the
  fallback default participant. The two are **not co-present**: an explicit `on_*` **takes
  precedence** and the native never seeds the slot (E30 ruling; AC-31). For whichever populates
  the slot, on **each** channel (`keypressed`/`textinput`/`keyreleased`, ruling 4): (a) it is
  invoked **regardless of widget-shown state** (AC-31); (b) a **truthy return intercepts** — the
  sink does not receive that event (R13: truthy consumes, sink skipped); (c) a **present callback
  returning falsey** falls through — the event reaches the sink; (d) **no project-set callback**
  (default noop, falsey) ≡ non-intercepting — the event reaches the sink. The prior
  suppress-while-shown assertion is gone; no green row anywhere still encodes replace/suppress
  semantics (R13). **Precedence (E30-resolved, was escalated 10(b)):** when a project defines both
  a native and an `on_*` for a channel, the `on_*` wins and the native never seeds the slot —
  assert this **precedence**, not a "replace the native" relationship. *(Optional, not required:
  the framework MAY `Log.warn` (or error) when both a native and an `on_*` are defined for the
  same channel — pursue **only** if trivially cheap (~3 lines), **no test**; the case is marginal.
  Not a slice requirement.)*
- **AC-38** *(signature/proxy schema + full-travel coverage — Scope item 10.)* **Schema:** every
  receiver on a channel — framework handler (tier 1), combo handler (tier 2), generic callback
  (tier 3), sink (tier 4) — receives the read-only `keys_pressed` **proxy** (`__newindex` raises,
  §1) plus the channel payload: `keypressed (k, proxy, isrepeat)`, `textinput (text, proxy)`,
  `keyreleased (k, proxy)`. `isrepeat` is asserted **passed through** to tier 3 only; whether combo
  tiers fire on repeat is **DEFERRED** (spec §2, 0.1.0-m5) and NOT asserted. **Full travel:** the
  project route is covered end-to-end — a tier-1 framework handler consumes before lower tiers; an
  unconsumed event descends tier-1→4 and reaches the sink; a truthy return at any tier stops
  descent for that event (consuming ≠ removing, R13). Route-**restoration** coverage is escalated
  (Scope item 10 (a)), not in this AC.
- **AC-37** Every in-tree `-- REVIEW:` comment in the contract suite is reconciled — resolved
  against a cited AC/contract point (comment removed, ledger line) or escalated as a stop. None
  is silently deleted; none left dangling.

**Suite sequencing & semantics (E32 — owner rulings, brainlab session39)**

- **AC-39** *(F1 — legacy-solicitation retirement lifecycle.)* The green rows exercising the
  deleted `oneshot`/`push('userinput')` machinery — `a submit fills the handle and closes` and
  `a oneshot submit deactivates the widget` — follow an explicit route in this slice: **(1)** they
  are tagged `#deprecated` in the suite (design-plane step 1, landed at E32); **(2)** they go **red**
  when AC-25 deletes the machinery; **(3)** they are demoted to `pending()` once red — **busted has
  no xfail/expected-fail primitive; `pending()` is its skipped state**; **(4)** they are **deleted**
  once the same behaviour is green through the new chain (submit → validator → `on_text_entered(text)`
  → deactivate, AC-17/AC-25). Because they end the slice `pending()`-or-deleted, they never stand red
  at the boundary — AC-35 holds without loosening. `a refused solicitation warns` rides `input_text`
  (a legacy global surviving to M8, spec §9) and **stays** through this slice. The
  submit→`on_text_entered`→deactivate behaviour MUST have a live red acceptance row in this slice
  (test-first) — the new chain is where the retired behaviour is re-proven. **AC-39 is the instance
  of the AC-43 no-silent-retirement meta-rule for the legacy-solicitation rows.**
- **AC-40** *(F2 — `on_text_input` vs `on_text_entered`, distinct and both tested.)* These are two
  different contract points, each with its own red acceptance row: `on_text_input` is the **tier-3
  textinput generic callback** — fires **per character** as text arrives (signature `(text,
  keys_pressed)`, AC-8); `on_text_entered` is the **widget submit output** — fires **once** at Enter
  with the full assembled text (AC-17, R1). The landed pending row `on_key_pressed and
  on_text_entered exist` (L806), which drafts `on_text_entered` as a per-character capture, is
  **re-drafted** to this split; its stale per-char-vs-block `OPEN` question is settled by R1. A naive
  greening that keeps the per-character body is forbidden (it re-encodes the R1 trap).
- **AC-41** *(F2 — combo dispatch across all three channels.)* The pending row `combo handlers
  dispatch on the combo` (L842), drafted with the **R14-forbidden flat** `handlers = {['ctrl+s'] =
  fn}` table, is **rewritten/expanded to three rows** — one per per-event sub-table
  (`handlers.keypressed['ctrl+s']`, `handlers.textinput[…]`, `handlers.keyreleased['ctrl+s']`) — each
  asserting dispatch on the normalised combo for that channel (AC-6/AC-7). No flat combined table.

- **AC-42** *(F3 — highlighter and validator are functionally applied, not merely settable.)*
  Beyond settability (AC-16), each text-processing widget output is proven to **run against the live
  text**, each a distinct red acceptance row: **(a) a custom `highlighter` transforms the composed
  text** — as the user types, the widget's queried highlight reflects `highlighter(text)` (e.g. an
  upper-casing highlighter yields upper-cased highlight output via `get_highlight()`/the viewdata,
  `userInputModel:highlight()` — design.md §3 `input_code` flagship), asserting the function is
  invoked on the live text and its output surfaces, not merely stored; **(b) a custom `validator`
  validates the submitted text** — on Enter it is invoked with the assembled text and its verdict
  gates delivery: accept → `on_text_entered` (AC-17), reject → error/lock with no
  `on_text_entered`/`after_submit` (AC-18). *Generalised form: a settable text-processing widget
  output is asserted to actually execute against the live/submitted text, never only accepted as a
  field.*
- **AC-43** *(retirement meta-rule — no test is retired silently.)* A behavioural test is **never
  removed silently**. When the behaviour a row asserts is deleted or replaced, the row follows the
  retirement lifecycle: **`#deprecated` (marked now, design plane)** → **red** (when the change
  lands) → **`pending()`/skipped** (busted has no xfail primitive) → **deleted only once an
  equivalent new test is green.** Deleting a row without a green replacement covering the surviving
  behaviour is forbidden; a row leaves the suite only by (a) replacement-proven-green or (b) explicit
  owner disposition recorded in the outcome ledger. This governs the whole M4→M8 suite evolution, not
  just this slice's rows (AC-39 is its first instance).

## Test strategy

Tier-2 **test-first** (`agents/process.md` §9.2): acceptance tests transcribed from the
**Acceptance criteria above** (backed by `spec.md`
§§2/4/5/7/8) run red before implementation; the 8 carried `pending` rows of the contract suite
in the m5/m5a/m5b/m6 families convert to live green (m7 rows stay pending). **The M4-landed green
Bucket-B rows are re-derived to the assertions this slice states (AC-36), not preserved — the
generic-callback interception rows assert the four cases across both install paths and all three
channels (AC-36), with signature/proxy + full tier-1→4 travel coverage (AC-38); the suite's
`-- REVIEW:` comments are reconciled (AC-37). The `stop names the console…` row is **retargeted
to AC-29 teardown** and the assign-replaces-capture claim is **resolved to precedence** (Scope
item 10 (a)/(b), E30) — both derived here.** C3 rule: test
names state behaviour, no milestone ids. Manual verification: 4-mode check (REPL / editor /
project+widget / project no-widget) **plus** the ruling-3 case (non-blocking script — typing
after `main.lua` returns reaches the REPL) and migrated turtle/maze played by hand.

## Files (expected surface — overreach = stop, record)

`src/controller/controller.lua`, `src/controller/projectInputController.lua`,
`src/controller/userInputController.lua`, `src/model/input/userInputModel.lua` (oneshot
removal, `is_at_limit` extension, evaluate-path decoupling), `src/view/input/userInputView.lua`
(M6-01), `src/util/key.lua` (if matcher/normalisation lands there), `compy` namespace setup,
`src/examples/turtle/`, `src/examples/maze/` (migration), `tests/input/*` (+ helpers as needed).

## Risk

The widest slice of the re-cut plan (≈ twice M4's PERT) — accepted deliberately for review
coherence; mitigations: test-first red suite, ordered internal steps each independently
committed, the amended sweep mandate's mid-slice stop rules, and the hard human gate at the
boundary. Chief semantic traps: reintroducing replace-semantics anywhere (R13), letting the
sink's return leak chain meaning (R12), flattening the per-event handler tables (R14), and
"unifying" the pointer disconnect into ruling 3's keyboard/text scope (forbidden, spec §8), and
**re-greening the M4 suppress-while-shown assertion** instead of flipping it (AC-31/AC-36).
