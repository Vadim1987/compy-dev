# M8-01 — remove the dead `text_input` nil write (supplementary slice)

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 06); Approved by human?: NOT YET;_
Derived from: M2 take-1 surfaced gap **G-1** ([`../../implementation/outcomes/M2.md`](../../implementation/outcomes/M2.md),
[`../../implementation/reviews/M2-01.md`](../../implementation/reviews/M2-01.md)). Supplements
[`M8.md`](M8.md) (legacy text-input removal); does **not** alter the frozen M8 slice.

> A one-line dead-code removal surfaced during M2 that belongs to **M8's legacy-removal sweep**.
> Captured as its own supplementary slice (rather than back-editing the frozen `M8.md`) so the
> discovered work is not lost when M8 is picked up. Pure removal, zero behaviour change.

## Scope

`src/controller/consoleController.lua:634` assigns the bare identifier `input_text` to
`compy_namespace.text_input`. No local or global named `input_text` is in scope there, so the field is
silently set to **`nil`**, and no code ever reads `compy.text_input` / `compy_namespace.text_input`
(grep finds no consumer). It is a dead write — likely a mistyped alias for `project_env.input_text`.

| | |
|---|---|
| **Realises** | — (housekeeping; rides M8's legacy removal) |
| **Depends on** | nothing hard; do it within the M8 sweep so the legacy `input_text` function and this dead alias go together |
| **Risk** | none — removing a write whose value is already `nil` and whose field has no reader |

## Contract

- Remove the `compy_namespace.text_input = input_text` line (`consoleController.lua:634`).
- Confirm by grep that nothing reads `compy.text_input` / `compy_namespace.text_input` before removal.
- If, during the M8 sweep, a *real* `text_input` alias turns out to be wanted, that is a deliberate
  M8 decision — make it explicitly with a defined value, do not resurrect the nil write.

## Files

- `src/controller/consoleController.lua` — delete the dead assignment.

## Acceptance

- The dead line is gone; full suite still green; no behaviour change in any app mode.
- No remaining writer or reader of `compy_namespace.text_input` in the tree.
