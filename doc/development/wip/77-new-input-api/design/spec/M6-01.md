# M6-01 — drop the vestigial view `oneshot` snapshot (supplementary slice)

_Generated by LLM(Claude Opus 4.8): 2026-06-17 (session 06); Approved by human?: NOT YET;_
Derived from: M2 take-1 surfaced gap **G-2** ([`../../implementation/outcomes/M2.md`](../../implementation/outcomes/M2.md),
[`../../implementation/reviews/M2-01.md`](../../implementation/reviews/M2-01.md)). Supplements
[`M6.md`](M6.md) (`oneshot` removal); does **not** alter the frozen M6 slice.

> A vestigial field surfaced during M2 that becomes pure dead weight once M6 removes `oneshot`.
> Captured as its own supplementary slice (rather than back-editing the frozen `M6.md`) so the
> cleanup rides along with M6's `oneshot` work. Refactor only, zero behaviour change.

## Scope

`src/view/input/userInputView.lua` snapshots `oneshot = ctrl.model.oneshot` into `self.oneshot` (L19)
and declares `@field oneshot boolean` (L28), but **never reads `self.oneshot`** — the only oneshot
access in the view is the live `self.controller:is_oneshot()` at `draw()` (L289), which reads
`model.oneshot` directly. The snapshot is already dead today; M6 removes `model.oneshot` /
`UserInputController:is_oneshot()`, at which point the stale `@field` annotation also dangles.

| | |
|---|---|
| **Realises** | — (housekeeping; rides M6's `oneshot` removal) |
| **Depends on** | best done **with** M6 (so the view's live `is_oneshot()` read is reworked in the same pass) |
| **Risk** | none — the field has no reader; dropping it cannot change behaviour |

## Contract

- Remove the `self.oneshot = ctrl.model.oneshot` snapshot from the `UserInputView` constructor (L19)
  and the `@field oneshot boolean` annotation (L28).
- Do this as part of the M6 `oneshot` removal so the constructor and the live `draw()` read are
  reconciled together — not as a standalone edit that leaves M6 to trip over it later.
- Verify by grep that `self.oneshot` / `view.oneshot` has no reader before removal.

## Files

- `src/view/input/userInputView.lua` — drop the snapshot field and its annotation.

## Acceptance

- The vestigial field and annotation are gone; full suite still green; no behaviour change.
- View oneshot state is sourced solely from the controller's live accessor (until M6 retires that
  too).
