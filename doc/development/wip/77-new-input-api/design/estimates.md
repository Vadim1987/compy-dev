# Feature #77 — Estimates

_Generated by LLM(Claude Opus 4.8): 2026-06-16 (inline in roadmap); extracted to first-class node (E10, session 11) 2026-06-18; rev: M4-0 sized + total recalc, version02 baseline (E16, session 21) 2026-06-23; rev: M5 split (M5a/M5b) + M6-02 before_exit, version03 baseline (E11, session 23) 2026-06-24; rev: E29 Stage-3 re-cut — M5c consolidation, version04 baseline (E11 op, session33, Claude Fable 5) 2026-07-06; Approved by human?: NOT YET;_
Derived from: [`roadmap.md`](roadmap.md) (milestone set), [`spec/`](spec/) (per-milestone detail)

> **First-class derived sizing node.** Sibling to `spec/` and the roadmap — not a subsection
> buried inside the roadmap (`agents/process.md` §7, `agents/sdlc.md`). Holds the **per-milestone
> PERT sizing** on the two bases this lifecycle uses (without-LLM, with-LLM), the round deltas, and
> the confidence notes. The frozen design-time `spec/MN.md` slices are referenced, not the reverse.
>
> **One source of truth for the current total.** This doc owns the *current* derived total; the
> roadmap carries **only** (a) the **frozen design-phase total** (a historical artifact, never
> recalculated) and (b) an **append-only log** that transcribes this doc's total at each version.
> The roadmap never independently re-derives a figure (`process.md` §7).
>
> **Versioned.** Every recalc writes a numbered baseline under
> [`estimates.versions/`](estimates.versions/) (snapshot-complete, doc-format §6.1), then appends one
> line to the roadmap's total-estimated log. Maintenance is the standing operational entrypoint
> **E11**.

## Methodology

*Implementor assumed: senior engineer, solo. Familiar with Lua and LÖVE2D. Has read the design and
spec documents. Three-point estimates per line; PERT = (O + 4M + P) / 6, where O = optimistic,
M = most-likely, P = pessimistic. Hours.*

## Current total roll-up

| Basis | Total (PERT) |
|---|---|
| Without LLM assistance | **≈ 77 h** |
| With LLM assistance | **≈ 45 h** |

Widest pessimistic tail: **M5c** (the consolidated dispatch-chain slice), then the landed
M4/`M4-0` (historical) and **M8**.

**Recalculated at the E29 Stage-3 re-cut (session 33, 2026-07-06):** the milestone set changed —
**M5a/M5b/M6 are superseded** by **M5c**, one consolidated dispatch-chain slice
([`spec/M5c-dispatch-chain.md`](spec/M5c-dispatch-chain.md)): the four-tier chain on all three
keyboard/text channels + submit/cancel chains + `oneshot` deletion + ruling-3 connection window
(reverting M4's `project_open` forwarding) + R7 pure wrap (deleting `native_split`) +
**turtle/maze migration moved in from M8** + the M4-pushback **QUALITY corrective** (~18
remarks, largely dissolved by the rework) + riding adjacents M6-01/M6-02. Sizing: M5c ≈ old
M5a+M5b+M6 (14.5 h) + chain rework of the landed M4 shape (≈ +1.5 h) + turtle/maze (≈ +1 h,
transferred) + QUALITY residue (≈ +1 h) − dissolved `push('userinput')` cancel-path work
(≈ −0.5 h) → **16.5 h** no-LLM / **10.3 h** LLM. **M8 trimmed** by the turtle/maze transfer
(8.3 → 7.2 no-LLM; 4.3 → 3.5 LLM). Net delta vs version03: **+≈ 1 h / ≈ 0 h**. Baseline:
[`estimates.versions/version04`](estimates.versions/version04.md); roadmap log appended.

**Recalculated at E11 (session 23):** two changes to the milestone set — **(1) M5 split** into M5a
(callbacks: `on_key_pressed` / `on_text_entered` / `framework_handlers` slot) and M5b (handlers
sugar: `handlers[combo]` + normalisation + dispatch, deferred) per the session-23 strategic
decision; and **(2) M6-02** (`compy.before_exit` project-stop hook, M6-family, small adjacent slice).
Net delta vs version02: **+≈ 2 h / +≈ 1 h**. M5 split is volume-neutral (M5a + M5b ≈ M5); M6-02
adds ≈ 2 h / ≈ 1 h. Baseline: [`estimates.versions/version03`](estimates.versions/version03.md);
roadmap log appended.

**Recalculated at E16 (session 21):** `M4-0` added — **+≈ 8 h / +≈ 5 h** over the frozen
design-phase baseline (≈ 66 h / ≈ 39 h). The M5/M6/M7 test-first acceptance steps are a
re-sequencing of existing Test-coverage volume; no delta there.

The frozen design-phase total is untouched. The next recalc (E11) repeats this cycle.

## Without LLM assistance

| Item | O | M | P | PERT |
|---|---|---|---|---|
| M1 `keys_pressed` table | 2 | 3 | 4 | 3.0 |
| M2 Singleton extraction | 3 | 6 | 9 | 6.0 |
| **M4-0 Characterization net + harness extension** | 4 | 7 | 12 | 7.3 |
| M4 ProjectInputController + gate removal | 4 | 8 | 14 | 8.3 |
| **M5c The dispatch chain** (consolidated E29: old M5a+M5b+M6 + M4-shape rework + turtle/maze + QUALITY corrective; M6-01 rides) | 10 | 16 | 25 | 16.5 |
| **M6-02** `compy.before_exit` project-stop hook (rides M5c) | 1 | 2 | 3 | 2.0 |
| M7 Extended API (+ cursor surface, model fix) | 3 | 6 | 9 | 6.0 |
| M8 Legacy removal + example migration (turtle/maze moved to M5c) | 3 | 7 | 12 | 7.2 |
| Documentation updates | 4 | 8 | 12 | 8.0 |
| Test coverage | 8 | 12 | 18 | 12.3 |
| **Total** | **42** | **75** | **118** | **≈ 77 h** |

Project PERT (O=42, M=75, P=118): `(42 + 4×75 + 118) / 6 ≈ 77 h`.

**E11 delta (+≈ 2 h vs. ≈ 74 h) — M5 split + M6-02.** **(1) M5 split** (session 23): M5 is replaced
by M5a (callbacks: `on_key_pressed`/`on_text_entered`/`framework_handlers` slot, small — O=1 M=2 P=3
→ 2.0 h) and M5b (handlers sugar: dispatch table + normalisation + `__matcher` + fresh-only keying,
deferred — O=2 M=3 P=6 → 3.3 h). Net M5 delta: 2.0 + 3.3 − 5.2 = **+0.1 h** (commissioning
overhead only; same surface area). **(2) M6-02** `compy.before_exit` project-stop hook (O=1 M=2 P=3
→ **2.0 h**): small adjacent slice in the M6-family named-hook infrastructure; keyboard is the
canonical consumer (T3 device-state cleanup). Total delta: **+≈ 2 h / +≈ 1 h** vs version02.

**E16 delta (+≈ 8 h vs. ≈ 66 h) — `M4-0`.** The characterization net + harness extension is genuinely
new scope (the design-phase plan had no feature-global net — `M4-0` is the renamed M3-revival). Sized
per E9's infra answer: a **modest** harness extension (a keypress-level driver on the raw-handler
pattern + two `mock` emitters — order-independent `textinput`, `isrepeat`/`scancode`; the three
production slots already exist) ≈ 3 h, plus the characterization suite (example flows + `keyboard` +
editor block-nav + D-9 + `maze`) ≈ 4 h. Wide pessimistic tail (P=12) for the P1 false-green infra risk.
The M5/M6/M7 **test-first** steps add **no volume** — the per-milestone acceptance tests were already
in the Test-coverage line; E16 only re-sequences them (red-first, before implementation), which is
ordering, not new hours.

**Round-2 delta (+≈ 3 h vs. ≈ 63 h).** The D-5 boundary extension (horizontal `left`/`right`
directions and the `'line'` scope on top of vertical whole-input) is new `is_at_limit` model work and
adds ≈ 2 h to M6; the extra `on_limit_reached` direction/scope cases add ≈ 1 h to test coverage. The
`show()` `force` flag (M2), the `ProjectInputController` rename (M4 file), and the read-indexable
`keys_pressed` proxy shape (M1) are each within estimating noise and are absorbed in the existing
M1/M2/M4 cells.

Note: discarding backward compatibility (D-1) *raised* the estimate. The old M3 facade layer (≈ 4 h)
is gone, but M8 — removing the legacy globals and migrating the examples — is larger (≈ 8 h), because
rewriting the example corpus is more work than wrapping the old calls. The net is +≈ 4 h vs. the
facade plan.

Confidence: moderate. M4 (gate removal, central dispatch path) remains the widest pessimistic tail;
M8 is the next-widest, as the per-example migration effort and the convert-or-exclude release
decision both vary with the owner's call.

## With LLM assistance

LLM helps most with boilerplate wiring (M1, M7), example rewriting (M8), new-file scaffolding (M5),
test scaffolding, and doc updates. It saves least on M2 (cross-component refactor verified by hand),
M4 (integration; the engineer must trace the dispatch paths), and M6 (ordering semantics).

| Item | O | M | P | PERT | LLM value |
|---|---|---|---|---|---|
| M1 `keys_pressed` table | 1 | 2 | 3 | 2.0 | High |
| M2 Singleton extraction | 2 | 4 | 6 | 4.0 | Low |
| **M4-0 Characterization net + harness extension** | 2 | 4 | 8 | 4.3 | Medium |
| M4 ProjectInputController + gate removal | 3 | 5 | 9 | 5.3 | Medium |
| **M5c The dispatch chain** (consolidated E29; M6-01 rides) | 6 | 10 | 16 | 10.3 | Medium |
| **M6-02** `compy.before_exit` project-stop hook (rides M5c) | 1 | 1 | 2 | 1.2 | High |
| M7 Extended API (+ cursor surface, model fix) | 2 | 4 | 6 | 4.0 | High |
| M8 Legacy removal + example migration (turtle/maze moved to M5c) | 2 | 3 | 7 | 3.5 | High |
| Documentation updates | 2 | 3 | 5 | 3.2 | High |
| Test coverage | 4 | 7 | 10 | 7.0 | High |
| **Total** | **25** | **43** | **72** | **≈ 45 h** |

Project PERT (O=25, M=43, P=72): `(25 + 4×43 + 72) / 6 ≈ 45 h`.

**E29 delta (LLM basis).** M5c's blended LLM value is **Medium**: the handler-table/dispatch
scaffolding, tests, and example migration are High-value mechanical work, but the ordering
semantics (submit/cancel chains), the ruling-3 revert on the live dispatch path, and the
per-remark QUALITY dispositions are verification-heavy — the same Low–medium character old
M6 carried.

**E11 delta (+≈ 1 h vs. ≈ 44 h) — M5 split + M6-02.** M5a (O=1 M=1 P=2 → 1.2 h, High — pure
wiring) + M5b (O=1 M=2 P=4 → 2.2 h, High — the dispatch table + normalisation are mechanical) replaces
M5 (3.2 h); net M5 delta ≈ +0.2 h. M6-02 `before_exit` (O=1 M=1 P=2 → **1.2 h**, High — one
slot + one fire point riding the M6 named-hook infrastructure). Total delta: **+≈ 1 h**.

**E16 delta (+≈ 5 h vs. ≈ 39 h) — `M4-0`.** LLM value is **Medium**: the harness extension carries
real design (driver shape, event-injection API — the engineer must trace the install/dispatch path),
while the characterization-suite scaffolding is the kind of mechanical assertion-writing the LLM does
well (High) — but oracle-capture against current behaviour needs care, so the blended value is Medium.
The Tier-2 test-first steps add no volume (re-sequenced from Test coverage); the cheaper-model
execution of those steps is captured by their existing High LLM-value, not a new line.

**Round-2 delta (+≈ 2 h vs. ≈ 37 h, design-phase).** The D-5 boundary extension (M6 model work) and its
extra test cases. The boundary model logic is verification-sensitive, so the LLM saving on it is only
moderate; the test cases are mechanical (High).

Confidence: moderate. The saving is largest for well-specified generative work (M1, M5, M7, M8,
tests, docs) — rewriting the small example files to the new API is exactly the kind of mechanical
edit an LLM does well, so M8's LLM value is High. The integration milestones (M2, M4) are serial and
verification-heavy, so LLM code generation speeds them up but the manual verification cost is largely
fixed.

## History

Per-recalc baselines are under [`estimates.versions/`](estimates.versions/) (snapshot-complete).
The roadmap's **total-estimated log** mirrors each baseline's total as a one-line dated entry; the
**frozen design-phase total** stays pinned at the top of the roadmap's estimates section as the
historical artifact.

| Version | Date | Total (no-LLM / LLM) | Trigger |
|---|---|---|---|
| `version01` | 2026-06-18 | ≈ 66 h / ≈ 39 h | Genesis baseline — extraction from `roadmap.md §Estimates` (E10). Figures carried verbatim from the design-phase roadmap; this is also the frozen design-phase total. |
| `version02` | 2026-06-23 | ≈ 74 h / ≈ 44 h | E16 recalc — `M4-0` (characterization net + harness extension) sized & added after the E9 infra answer; M5/M6/M7 test-first steps re-sequenced (no volume delta). `version01` frozen as a full snapshot on demotion. |
| `version03` | 2026-06-24 | ≈ 76 h / ≈ 45 h | E11 recalc — M5 split into M5a (callbacks) + M5b (handlers sugar, deferred); M6-02 (`compy.before_exit` project-stop hook) added. Net delta +≈ 2 h / +≈ 1 h. `version02` frozen as a full snapshot on demotion. |
| `version04` | 2026-07-06 | ≈ 77 h / ≈ 45 h | E29 Stage-3 re-cut (session 33) — M5a/M5b/M6 superseded → **M5c** consolidated dispatch-chain slice (+ QUALITY corrective + turtle/maze in; M8 trimmed). Net delta +≈ 1 h / ≈ 0 h. `version03` frozen as a full snapshot on demotion. |
