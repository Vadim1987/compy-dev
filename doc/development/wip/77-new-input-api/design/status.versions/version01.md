# status — version 01 (genesis baseline)
_Generated by LLM(Claude Opus 4.8): 2026-06-16 (E4); frozen by LLM(Claude Fable 5): 2026-07-06 (E29 wrap, session33); Approved by human?: NOT YET;_

> **FROZEN SNAPSHOT — demoted-by-freezing** (doc-format §6.1) at the E29 status refresh
> (session33, 2026-07-06). This is `status.md` exactly as it stood from the E4 enrollment
> until the E29 re-derivation. Canonical current content: [`../status.md`](../status.md);
> latest baseline: [`version02.md`](version02.md).

---

# Feature #77 (design) — Status
_Generated by LLM(Claude Opus 4.8): 2026-06-16 (E4); Approved by human?: NOT YET;_
Derived from: [`roadmap.md`](roadmap.md) + the whole chain (review roll-up)

> **The rolling chain dashboard** for the design lifecycle (SDLC `status` role). It is
> **versioned** ([`status.versions/`](status.versions/)) and **updateable**: on each reprocess
> pass, refresh the stabilization map + decision track here. It also hosts the **blocking-decisions
> track** (the ledger + resolutions). Contradiction/round detail of record lives in
> [`status/archive/`](status/archive/); the deep per-decision rationale in
> [`notes/decisions-record.md`](notes/decisions-record.md) + [`notes/decisions.md`](notes/decisions.md).

## Summary — CONVERGED

The design chain is **converged** (verified by E1, `sessions/.../session02/report.md`): D-1…D-7
are stakeholder-settled, D-8/D-9/D-10 are architect's discretion (owner-ruled to proceed). The
validation chain is at **PASS-WITH-NOTES** (VR3) with only trivial residue. **Implementation is
unblocked.** No sprint is activated yet (seeding `sprint01/` is entrypoint **E3**).

## Chain stabilization map

*(Per-stage convergence against the SDLC edge criteria; derived from the review history in
[`status/archive/`](status/archive/). "Stable" = no open contradiction on its inbound edge.)*

| Stage | Inbound edge | State | Note |
|---|---|---|---|
| `notes/` (ingest) | human/external | stable | input.md (SR1) + input/ (SR2) + analysis notes |
| `context.md` (context) | ← notes | stable | current-architecture assessment matrix |
| `requirements.md` | ← notes | stable | constraints folded §1 (split deferred) |
| `decisions` (track, below) | ← requirements + assessment | **converged** | D-1…D-10 all ruled (ledger below) |
| `design.md` | ← decisions + requirements + context | stable | routing-unification basis |
| `spec.md` + `spec/MN` | ← design | stable | per-milestone slices sliced (E4) |
| `roadmap.md` | ← spec | stable | M1,M2,M4–M8; ≈66 h / ≈39 h |
| outcome (= roadmap + spec/) | ← roadmap | referenced | consumed by sprints; built code = sprint commits |

**Residue (PASS-WITH-NOTES, non-blocking):** trivial wording-level items only; no open
contradiction. The one *contingency* is a quick re-fit round on D-8/D-9/D-10 **iff**
stakeholders object after seeing implementation (not an open task — see below).

## Blocking-decisions track

### Two "round" axes (do not conflate — see [`agents/process.md`](agents/process.md) §4)

- **Stakeholder rounds (SR)** — external approve/veto: **SR1** `notes/input.md` (2026-06-06);
  **SR2** `notes/input/stakeholder2_notes.md` (2026-06-10).
- **Validation rounds (VR)** — internal edge-coherence: **VR1** (FAIL) → **VR2** → **VR3**
  (PASS-WITH-NOTES), all 2026-06-08, archived under [`status/archive/validation/`](status/archive/validation/).

### Ledger (derived convergence roll-up)

| # | Ruling | Authority | Ruled where |
|---|---|---|---|
| D-1 | **Discarded** — no backward compat | Stakeholder consensus | SR1 |
| D-2 | Block by default; `show({force=true})` reconfigures | Stakeholder | SR2 |
| D-3 | Three-tier dispatch; scope ratified as an improvement | Stakeholder (+ architect clarification) | SR2; clarified VR1–VR2 |
| D-4 | Named `before_X → X → after_X` chains; framework owns middle | Stakeholder confirmed | SR2 |
| D-5 | `on_limit_reached(direction, scope)`; up/down **+ left/right**, input/line | Stakeholder | SR2 |
| D-6 | Two independent channels (`= D-3`); no suppression | Architect/design (consistent w/ D-3) | VR2 supersession |
| D-7 | Overlay context first | Stakeholder confirmed | SR2 |
| D-8 | 2D `(line,col)` cursor contract; restores FR-8/9/10 | **Architect's discretion — owner-ruled proceed** | origin VR1; owner ruling E1 |
| D-9 | Native handler coexistence (auto-provision wrapper) | **Architect's discretion — owner-ruled proceed** | origin VR1; owner ruling E1 |
| D-10 | `compy.input.*` namespace isolation | **Architect's discretion — owner-ruled proceed** (implicit SR2 endorsement) | origin VR2; owner ruling E1 |

> **Authority legend.** *Stakeholder* = settled by external approve/veto. *Architect's
> discretion* = an architect commitment the owner ruled to proceed on (DevX is the better
> feedback source; re-fit in a quick design round if objected to later). **D-8/D-9 are
> arguably non-discretionary** (restore a dropped requirement / honour an asserted guarantee);
> **D-10** is the one genuinely-discretionary call, and SR2 already writes `compy.input.show`
> (implicit endorsement). Full per-decision rationale: [`notes/decisions-record.md`](notes/decisions-record.md).

### Resolution track (the calls and where they landed)

The seven stakeholder questions and their resolutions; this is the "track of blocking decisions
and their resolutions" the dashboard carries (updateable as any re-fit lands):

- **D-1 Backward compat** → discarded; legacy globals removed, examples migrate (M8). D-9
  coexistence unaffected.
- **D-2 Second setup call** → `show()` no-op by default; `force=true` reconfigures; `configure()`
  is the mid-run live-update path.
- **D-3 Key event coverage** → three-tier dispatch; sink is the *default* of `on_key_pressed`;
  modifier-first generic-folded combos; metatable-normalised; overloadable matcher seam.
- **D-4 Cancel/submit** → named `before/after` chains; framework owns the middle; `oneshot`
  deleted in M6.
- **D-5 Cursor boundary** → `on_limit_reached(direction, scope)`; four directions × input/line.
- **D-6 Modifier+char** → two independent channels, no suppression (`= D-3` shape).
- **D-7 Rollout scope** → project overlay first; FR-11/FR-12 are expressiveness targets, not
  migrations here.
- **D-8/D-9/D-10** (architect-added) → 2D cursor surface / native coexistence / namespace
  isolation; owner-ruled proceed.

## Execution / sprint tracker

- **Roadmap milestones:** M1 · M2 · M4 · M5 · M6 · M7 · M8 (M3 removed). Detail + estimates in
  [`roadmap.md`](roadmap.md); per-milestone specs in [`spec/`](spec/).
- **Sprints:** none activated. Each `sprintNN/` is its own canonical SDLC instance consuming one
  milestone + its `spec/MN.md`. Seeding `sprint01/` = entrypoint **E3** (now unblocked; specs sliced).

## Open / residual

- **No open contradiction.** Chain at PASS-WITH-NOTES (residue only).
- **Contingency (not an open task):** a quick re-fit design round on D-8/D-9/D-10 **only if**
  stakeholders object after seeing implementation (DevX-led, per the owner's E1 ruling).
- **Deferred formalization** (later increment, not blocking): `objective`/`goal` node;
  `requirements`/`constraints`/`remarks` split + `.versions/` baseline. See
  [`agents/sdlc.md`](agents/sdlc.md) "deferred".
