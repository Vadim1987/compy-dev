# design (feature #77) — SDLC binding overlay
_Generated by LLM(Claude Opus 4.8): 2026-06-16; rev: estimates bound as a derived versioned node (session 10) 2026-06-18; rev: extraction landed — frozen-total/append-only-log (E10, session 11) 2026-06-18; rev: spec-slice taxonomy + two-tier test-first handoff + C1/C2 doc-rules bound (E16, session 21) 2026-06-23; Approved by human?: NOT YET;_

> Binds the canonical SDLC method (`/var/lib/brainlab/current/methods/sdlc.md`) to the
> **`design/` subtree** of topic `git/doc/development/wip/77-new-input-api`. Binds, does
> not shadow. This is the **design lifecycle** (SDLC-for-planning) — one of the topic's
> two chained lifecycles (the other being the `sprintNN/` implementation SDLCs). Its
> outcome = the converged roadmap + per-milestone specs that the sprints consume.
>
> **Presence-scoped, not topic-root.** This overlay sits under `design/`, so it governs
> the `design/` subtree only (nearest-ancestor resolution, topology §2). The **topic root
> deliberately carries no binding** → it stays on the default (brainstorm) and boots into
> the `entrypoints.md` menu, because the topic is a *container of two lifecycles*, not
> itself a single SDLC instance.
>
> **Custom-process note.** This design lifecycle is a predecessor ad-hoc process that
> predates canonical SDLC; it is bound here as a **custom process on top of SDLC** (the
> human's option (b)). The deliberate deviations and the deferred-formalization items are
> documented in [`process.md`](process.md) — read it alongside this binding.

## chain
- root: `.`  (the `design/` directory itself is the chain root)

## role → filename   (deviations from canonical only)
- **None.** All roles use canonical names: `notes/` (+`index.md`), `context.md`,
  `requirements.md`, `design.md`, `spec.md`+`spec/`, `roadmap.md`, `status.md`+`status.versions/`.
  *(E4 renamed the old `assessment.md` → `context.md`, removing the last name deviation — the
  lifecycle is now fully canonical in shape.)*

## in chain (walked on reprocess / revalidate)
- `notes/` (ingest, + `index.md`) → `context.md` + `requirements.md` → `design.md`
  → `spec.md` + `spec/MN.md` → `roadmap.md` → `estimates.md` (+ `estimates.versions/`, derived
  sizing); control: `status.md` (+ `status.versions/`).

## out of chain      (dirs the engine must NOT walk on reprocess / revalidate)
- `prompts/`         # process scaffolding — the ad-hoc progressive prompts that drove the work
- `status/archive/`  # frozen round-based review history (reevaluations/ + validation/) — audit trail
- `agents/`          # this engine layer

## decisions         (per-topic config)
- context: **inline** — `context.md` is a full requirement×code matrix (architecture assessment), not a thin external pointer.
- outcome: **referenced** — the design lifecycle's world-out is the **converged `roadmap.md` + `spec.md`/`spec/` pair**; the *built* code is the downstream sprints' outcome (commits in the project repo). No separate `outcome.md`.
- decisions: tracked in **`status.md`** (ledger + resolution track) with deep rationale in **`notes/decisions-record.md`** (+ working analysis `notes/decisions.md`). *(E4 dissolved the old `decisions.md` pipeline-node — this now aligns with canonical SDLC, where decisions/contradictions live in the control layer, not as a node. See [`process.md`](process.md) §3.)*
- estimates: a **derived, versioned node** off `roadmap`+`spec/` — `estimates.md`
  (+ `estimates.versions/`) holds per-milestone PERT and owns the current total; `roadmap.md` carries
  only the **frozen design-phase total** (historical artifact, never recalculated) + an
  **append-only total-estimated log** (one derived line per recalc, latest = live total) + a pointer.
  A topic-local refinement of canonical SDLC (which folds estimates into the roadmap node).
  Maintenance is a standing operational entrypoint (see [`process.md`](process.md) §7,
  [`../../entrypoints.md`](../../entrypoints.md)). *(Extracted — entrypoint **E10** landed, session 11,
  2026-06-18; `estimates.versions/version01` is the genesis baseline.)*
- review/status: the canonical edge-based `review`+`status` loop is **not run live** — the design is **converged** (E1, `sessions/.../session02/report.md`). The round-based history is frozen under `status/archive/`; `status.md` is the live, **versioned** dashboard and convergence is **derived** from its decision ledger + stabilization map.
- spec slices: the in-chain `spec/MN.md` slices are **frozen**; adjacent slices attach by suffix — **`MN-0`** = precondition/pre-net authored *before* the milestone (e.g. `M4-0`, the characterization net), **`MN-NN`** = corrective/closure authored *after* (e.g. `M2-02`). Future-milestone design refinements ride an adjacent slice or the milestone's test-first prompt — **never** an edit to the frozen `MN.md`. See [`process.md`](process.md) §9.1.
- test strategy + execution handoff: **two-tier** — Tier-1 feature-global characterization net (`M4-0`, oracle = current runtime) + Tier-2 per-milestone acceptance tests authored **test-first** against the frozen `spec/MN.md` (red → implementation green; the test step may run on a **cheaper model**). Each new-behaviour milestone (M5/M6/M7) splits into an ordered **test-prompt → implementation-prompt** pair; **M4 too is test-first** (reframed session 24) — its `M4-0` slice is now M4's **front-tests** (`spec/M4-0-01-front-tests.md`: regression smoke green + new-behaviour `pending` → M4 greens), plus manual 4-mode verification. See [`process.md`](process.md) §9.2.
- doc-rules: **C1** (semver-not-milestones — `0.1.0-mN` pre-release suffixes, not milestone ids), **C2** (warn-don't-swallow — silent drops `Log.warn` + contract test), and **C3** (no milestone/sprint ids in test names/files — name by behaviour; settled session 24) are in force; open/postponed questions carried as in-code `DEFERRED (0.1.0-mN)` markers. See [`process.md`](process.md) §9.3.

## deferred (not formalized in E4 — would be a later increment)
- `objective`/`goal` node — implicit in `notes/input.md` + `README.md` TL;DR; no placeholder created.
- `requirements`/`constraints`/`remarks` split + mandatory `.versions/` baseline — `constraints` are folded into `requirements.md §1`; versioning churn deferred (design is converged, low regression risk). Splitting these realises the human's "new inputs fan out across requirements/constraints/remarks" workflow.
