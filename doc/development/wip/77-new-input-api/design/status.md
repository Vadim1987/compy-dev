# Feature #77 (design) — Status
_Generated by LLM(Claude Fable 5): 2026-07-06 (E29 wrap, session33); Approved by human?: NOT YET;_
Derived from: [`roadmap.md`](roadmap.md) + the whole chain (review roll-up); supersedes the E4-era
dashboard (frozen: [`status.versions/version01.md`](status.versions/version01.md))

> **The rolling chain dashboard** for the design lifecycle (SDLC `status` role). It is
> **versioned** ([`status.versions/`](status.versions/)) and **updateable**: on each reprocess
> pass, refresh the stabilization map + decision track here. It also hosts the **blocking-decisions
> track** (the ledger + resolutions). Contradiction/round detail of record lives in
> [`status/archive/`](status/archive/); the deep per-decision rationale in
> [`notes/decisions-record.md`](notes/decisions-record.md) + [`notes/decisions.md`](notes/decisions.md);
> the E29 remark/ruling record in [`notes/talk/ratified-model-remarks-analysis.md`](notes/talk/ratified-model-remarks-analysis.md).

## Summary — RE-DERIVED (E29); Gate 3 pending

The M4 gate drew a heavy architect pushback (~48 `>> REVIEW` remarks); root cause (session32):
a **day-one derivation mutation** — `design.md`/`spec.md` silently negated the architect's core
solution (`notes/routing_unification.md`), and no pass ever diffed the two
([`../reviews/m4-architect-pushback.md`](../reviews/m4-architect-pushback.md)). E29 (session33)
resolved it in three gated stages:

- **Gate 1 — RATIFIED (2026-07-05):** core solution revalidated claim-by-claim against the
  platform (no core claim falsified); the one-page
  [`notes/ratified-model.md`](notes/ratified-model.md) is now the **canonical authority**
  (four-tier chain, R1–R14 precision remarks, five architect rulings folded).
- **Gate 2 — APPROVED (2026-07-06):** `design.md` + `spec.md` **re-derived from bounded
  inputs** (old docs = salvage, not authority) and approved at the review-surface level
  (design §8 claims-diff + §9 deviations, spec §11). Outgoing docs frozen as
  `design.versions/version01` + `spec.versions/version01`. **Closing scoping ruling:** no
  further architectural simplification in this pass — that round is scheduled for the
  console/editor migration, with stakeholder input on dev-facing facets.
- **Gate 3 — PRESENTED, awaiting approval:** plan re-cut **M5c → M7 → M8**
  ([`spec/M5c-dispatch-chain.md`](spec/M5c-dispatch-chain.md) supersedes M5/M5-01-split/M6 as
  implementation targets) + amended sweep mandate
  ([`../implementation/prompts/M5c-M8-sweep-mandate.md`](../implementation/prompts/M5c-M8-sweep-mandate.md),
  6 guardrails) + estimates recalc **≈ 77 h / ≈ 45 h** (`estimates.versions/version04`).

**The implementation sweep stays PAUSED until Gate 3 closes.**

## Chain stabilization map

*(Per-stage convergence against the SDLC edge criteria. "Stable" = no open contradiction on its
inbound edge.)*

| Stage | Inbound edge | State | Note |
|---|---|---|---|
| `notes/` (ingest) | human/external | stable | + **`ratified-model.md` (Gate 1)** — canonical authority; glossary binding |
| `context.md` | ← notes | stable | current-architecture assessment matrix |
| `requirements.md` | ← notes | stable | constraints folded §1 (split deferred) |
| decisions (track, below) | ← requirements + assessment | **converged, E29-overlaid** | D-1…D-10 + five rulings + R1–R14 + Gate-2 scoping ruling |
| `design.md` | ← ratified model + decisions + requirements | **re-derived (version02), approved Gate 2** | opens with the model verbatim (§0); claims-diff §8; deviations §9 |
| `spec.md` + `spec/MN` | ← design | **re-derived (version02), approved Gate 2** | M5c slice minted; M5/M5-01-split/M6 superseded; M7/M8 survive (spec.md wins on divergence) |
| `roadmap.md` | ← spec | **re-cut — Gate 3 in review** | M5c → M7 → M8; ≈ 77 h / ≈ 45 h (version04) |
| outcome (= roadmap + spec/) | ← roadmap | paused | consumed by the sweep under the amended mandate, after Gate 3 |

## Blocking-decisions track

### E29 overlay (2026-07-02 → 2026-07-06) — reads before the ledger

The five architect rulings ([`../notes/talk/m4-pushback-rulings-and-provenance.md`](../notes/talk/m4-pushback-rulings-and-provenance.md))
and the Gate-1 precision remarks R1–R14 (recorded in the ratified model + the remarks ledger)
**overlay** the D-ledger below; on any tension the ratified model wins:

- **Ruling 1 (chain ratified):** four tiers — `framework_handlers` → `handlers[combo]` →
  per-event generic callback (default **noop + log**) → sink (terminal, internal hidden-check);
  truthy = stop-propagation. *Refines D-3's "three-tier" wording; the "default IS the sink"
  reading is the removed mutation.*
- **Ruling 2 (meta):** universal/predictable beats ad-hoc; specials demote to variations.
- **Ruling 3:** project-route keyboard/text slots connected only while `'running'`
  (pointer stays hooked — sapper constraint).
- **Ruling 4:** `keyreleased` unified into the chain (the 2026-07-02 descope is superseded).
- **Ruling 5:** internal jargon as shorthand ok; as first-class routing decisions rejected.
- **R7 supersedes D-9's shape:** natives = **pure wrap** as initial tier-3 values; turtle/maze
  **break-and-fix** in M5c (no auto-provisioned truthy participant).
- **R1:** `on_text_entered` = widget output at submit; the per-char chain callback is
  `on_text_input`. **R12/R13/R14:** sink return carries no chain meaning; consuming ≠ removing;
  combo tables keyed event-type-first.
- **Gate-2 scoping ruling:** build the ratified model, nothing beyond it; next
  simplification/unification round = console/editor migration (stakeholder venue).

### Two "round" axes (do not conflate — see [`agents/process.md`](agents/process.md) §4)

- **Stakeholder rounds (SR):** SR1 `notes/input.md` (2026-06-06); SR2
  `notes/input/stakeholder2_notes.md` (2026-06-10).
- **Validation rounds (VR):** VR1 (FAIL) → VR2 → VR3 (PASS-WITH-NOTES), 2026-06-08, archived
  under [`status/archive/validation/`](status/archive/validation/).
- **E29 gates (session33):** Gate 1 ratification (2026-07-05) → Gate 2 approval (2026-07-06) →
  Gate 3 (in review).

### Ledger (derived convergence roll-up; read with the E29 overlay above)

| # | Ruling | Authority | Ruled where |
|---|---|---|---|
| D-1 | **Discarded** — no backward compat | Stakeholder consensus | SR1 |
| D-2 | Block by default; `show({force=true})` reconfigures | Stakeholder | SR2 |
| D-3 | Tiered dispatch → **four-tier chain per ruling 1** | Stakeholder + architect (E29 ratification) | SR2; VR1–VR2; Gate 1 |
| D-4 | Named `before_X → X → after_X` chains; framework owns middle | Stakeholder confirmed | SR2; submit sequence pinned Gate 2 (design §3, D-a) |
| D-5 | `on_limit_reached(direction, scope)`; up/down + left/right, input/line | Stakeholder | SR2 |
| D-6 | Two independent channels; no suppression | Architect/design | VR2 supersession |
| D-7 | Overlay context first | Stakeholder confirmed | SR2 |
| D-8 | 2D `(line,col)` cursor contract; restores FR-8/9/10 | Architect's discretion — owner-ruled proceed | VR1; owner ruling E1; → M7 |
| D-9 | Native coexistence — **superseded by R7: pure wrap + break-and-fix turtle/maze** | Architect ruling (Gate 1, R7) | VR1; E1; **Gate 1 2026-07-05** |
| D-10 | `compy.input.*` namespace isolation; **R3 mutable-boundary enumeration** | Architect's discretion + Gate-1 R3 | VR2; E1; Gate 1 |

## Execution / sprint tracker

- **Landed:** M0 · M1 · M2 (+M2-01/02) · M2a · M4-0 family (characterization net) · **M4**
  (project `c6b5b30`; pushback `c537909` — its `>> REVIEW` markers stay in-tree until the
  resolving slice lands, per the mandate).
- **Remaining:** **M5c → M7 → M8**, run as Fable-governed sweep sessions under
  [`../implementation/prompts/M5c-M8-sweep-mandate.md`](../implementation/prompts/M5c-M8-sweep-mandate.md)
  (hard human gate every boundary; 6 guardrails). Adjacents: M6-01 + M6-02 ride M5c;
  M7-01 rides M7; M8-01 rides M8.
- **Estimates:** ≈ 77 h no-LLM / ≈ 45 h LLM
  ([`estimates.md`](estimates.md), baseline [`estimates.versions/version04.md`](estimates.versions/version04.md)).

## Open / residual

- **Gate 3 approval** — the only blocker; sweep resumes on it (successor session's first task).
- **Manual M0 checks (human, non-blocking):** balloons continuous session + Ctrl+S/Ctrl+Q
  gateway shortcuts; optional sine/sapper/life ruling-3 census confirmation.
- **Scheduled venue (not an open task):** console/editor migration = the next
  simplification/unification round + stakeholder talks on dev-facing facets (Gate-2 ruling).
- **Deferred formalization** (later increment, not blocking): `objective`/`goal` node;
  `requirements`/`constraints`/`remarks` split. See [`agents/sdlc.md`](agents/sdlc.md) "deferred".
