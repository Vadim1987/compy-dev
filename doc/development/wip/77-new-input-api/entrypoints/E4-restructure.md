# E4 — design/ → canonical-SDLC restructure (resumable plan)
_Generated by LLM(Claude Opus 4.8): 2026-06-16 (session03); Approved by human?: YES (chain + both forks ratified);_

> **Purpose & resumability.** The ordered action chain backing entrypoint **E4**. If a
> session is interrupted mid-restructure, resume from here: the **status column is the source
> of truth** — read which steps are ☑ done, verify against the filesystem, continue from the
> first ☐. Each step is self-contained with its file moves + link-fix scope.
>
> Origin/context: `sessions/git/doc/development/wip/77-new-input-api/session03/{prompt,track}.md`;
> E1 finding `…/session02/report.md`; the formalized process `design/agents/process.md`.

## Scope (ratified session 03)

E4 = **establish the design lifecycle's process/rules** (this restructure) **+** slice the
spec per milestone. Beyond the initial "declare-don't-move", the human elected the fuller
**canonical end-state** reshape below.

**Forks ruled:**
- **A — decisions split:** `status.md` hosts the decision **ledger + resolution track**
  (rolling/updateable); the deep per-decision **rationale** consolidates into
  `notes/decisions.md`. status.md stays a dashboard, not a 700-line dump.
- **B — requirements/constraints/remarks split:** **deferred** to a later follow-on (not in
  this chain). `requirements.md` stays as-is (constraints folded in §1) for now.

## Target end-state

```
design/
  README.md  agents/{sdlc.md, process.md}
  notes/{index.md, input.md, stakeholder2_*.md, decisions.md(rationale), …analysis notes}
  assessment.md  requirements.md  design.md          ← each opens with its own ## Summary
  spec.md  spec/{M1…M8}.md
  roadmap.md
  status.md  status.versions/version01.md  status/archive/{reevaluations,validation}/
  (decisions.md, summaries/, input/  — gone)
```

## The chain (status = source of truth on resume)

| # | Step | Status | Does | Depends |
|---|---|---|---|---|
| 1 | **Consolidate ingest** | ☑ | `git mv input.md notes/input.md` + `git mv input notes/input`; created `notes/index.md`. Only real breaking link was `decisions.md:3` Derived-from → handled in Step 3 (decisions.md dissolves). Prose backtick name-mentions of `input.md` left as-is (resolved via `notes/index.md`). | — |
| 2 | **Re-merge summaries** | ☑ | Folded `## Summary` into requirements/assessment/design/spec/roadmap; `summaries/decisions.md` content carried into status.md (Step 3); `git rm -r summaries/`; README "Start here" repointed to the docs' own summaries + status.md. | — |
| 3 | **status.md + dissolve decisions** | ☑ | Created `status.md` (summary + stabilization map + decision ledger/track + sprint tracker) + `status.versions/version01.md` (genesis, refs up). `git mv decisions.md → notes/decisions-record.md` (formal per-decision rationale of record; header repointed to status.md); existing `notes/decisions.md` kept as working-analysis. Fixed breaking links: `spec/M1.md` Derived-from → status.md; `notes/index.md` lists both decision docs. Prose decision-ID refs left as-is. | 2 |
| 4 | **Archive round-history** | ☑ | `git mv` `reevaluations/` + `validation/` → `status/archive/`; added `status/archive/README.md`. No broken markdown links (only ref was `decisions-record.md:3`, repointed in Step 3). | 3 |
| 5 | **Update engine layer** | ☑ | `agents/sdlc.md`: corrected in-chain/out-of-chain (notes/ is ingest, not out-of-chain; dropped summaries/+input/; added status/archive/), decisions→status.md config, shrank "deferred". `agents/process.md`: flow diagram, §3 deviations (1 reconciled, 1 retired — now close to canonical), §5, §7. | 1–4 |
| 5b | **Rename context** | ☑ | (Added mid-run, human-ruled) `git mv assessment.md context.md` → **removes the last name deviation**; fixed links (README, decisions-record Derived-from) + active prose (sdlc.md, process.md, status.md). Frozen notes/prompts/archive prose left (rebuildable). | — |
| 6 | **Spec slices M2–M8** | ☑ | Created `spec/M2,M4,M5,M6,M7,M8.md` per the `M1.md` template (M3 removed). Link scan clean (only pre-existing false positive `notes/love2d_handler_layers.md→args`). | 2, M1 template |
| 7 | **Refresh entrypoints + README** | ☑ | `entrypoints.md`: E4 → ✓, E3 → ▶ (unblocked, points at `spec/M1.md`), Notes section de-staled. Topic `README.md` design/ section: SDLC-enrolled, points at status.md/agents/. | all |

## Already done in session 03 (pre-pivot Phase 1 — partially superseded)

- `design/agents/sdlc.md`, `design/agents/process.md` created (Step 5 will revise both).
- `decisions.md` header → derived status ledger + SR/VR round-axis split (Step 3 moves this
  ledger into `status.md`; the round-axis disambiguation carries forward).
- `spec.md` "Per-milestone slices" index + `spec/M1.md` template (Steps 2/6 build on these).

## Notes for the executor

- **Git is staging-level only** (`agents/git.md`): `git mv`/`git add` ok; commits belong to
  the human.
- **Link discipline:** every move must fix referrers (doc-format §2 referential integrity).
  Heavy referrers: `decisions.md`↔`validation/`, README→`summaries/`, cross-doc `input.md`.
- **Round-axis convention** (kept): SR1/SR2 (stakeholder) vs VR1/VR2/VR3 (validation) — see
  `agents/process.md` §4.
- **Checkpoint** with the human after Step 5 (structure reshaped) before the bulk Step 6.
