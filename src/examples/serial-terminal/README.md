# serial-terminal

Prints everything that arrives over USB serial to the
terminal. The console takes a Lua chunk and Enter sends it
down the line. With the Lua REPL firmware on the micro:bit,
code written here runs on the board.
