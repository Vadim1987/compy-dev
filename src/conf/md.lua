require("util.color")

return {
  default     = Color.black,
  emph        = Color.yellow,
  strong      = Color.red,
  heading     = Color.cyan,
  link        = Color.green,
  list_marker = Color.red + Color.bright,
  code        = Color.blue + Color.bright,
}
